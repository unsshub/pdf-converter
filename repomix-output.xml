This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
apps/
  api/
    outputs/
      How_I_Made_dollar_4000_in_2_Days_Using_Reddit-1778871951539-46e61y.docx
      merged-1778872012360.pdf
      preview-1778869433565.png
      preview-1778869433571.png
      preview-1778869433575.png
      preview-1778869433577.png
      preview-1778869433580.png
      preview-1778869433583.png
      preview-1778869433740.png
      preview-1778869433743.png
      preview-1778869433747.png
      preview-1778869433750.png
      preview-1778869433761.png
      preview-1778869433764.png
      preview-1778869433863.png
      preview-1778869433883.png
      preview-1778869433892.png
      preview-1778869433903.png
      preview-1778869433907.png
      preview-1778869433911.png
      preview-1778869434006.png
      preview-1778869434029.png
      preview-1778869434043.png
      preview-1778869434046.png
      preview-1778869434052.png
      preview-1778869434057.png
      preview-1778869434148.png
      preview-1778869434184.png
      preview-1778869434187.png
      preview-1778869434195.png
      preview-1778869434199.png
      preview-1778869434208.png
      preview-1778869434302.png
      preview-1778869434325.png
      preview-1778869434329.png
      preview-1778869434348.png
      preview-1778869434357.png
      preview-1778869434361.png
      preview-1778869434456.png
      preview-1778869434463.png
      preview-1778869434473.png
      preview-1778869434496.png
      preview-1778869434507.png
      preview-1778869434525.png
      preview-1778869434610.png
      preview-1778869434613.png
      preview-1778869434618.png
      preview-1778869434647.png
      preview-1778869434658.png
      preview-1778869434680.png
      preview-1778869434755.png
      preview-1778869434760.png
      preview-1778869434770.png
      preview-1778869434794.png
      preview-1778869434797.png
      preview-1778869434828.png
      preview-1778869434896.png
      preview-1778869434909.png
      preview-1778869434921.png
      preview-1778869434943.png
      preview-1778869434949.png
      preview-1778869434978.png
      preview-1778869435045.png
      preview-1778869435059.png
      preview-1778869435062.png
      preview-1778869435092.png
      preview-1778869435095.png
      preview-1778869435140.png
      preview-1778869435194.png
      preview-1778869435198.png
      preview-1778869435204.png
      preview-1778869435238.png
      preview-1778871758912.png
      preview-1778871758917.png
      preview-1778871758920.png
      preview-1778871758923.png
      preview-1778871758926.png
      preview-1778871758929.png
      preview-1778871759042.png
      preview-1778871759052.png
      preview-1778871759066.png
      preview-1778871759071.png
      preview-1778871759084.png
      preview-1778871759086.png
      preview-1778871759176.png
      preview-1778871759199.png
      preview-1778871759202.png
      preview-1778871759287.png
      preview-1778871759290.png
      preview-1778871759293.png
      preview-1778871759325.png
      preview-1778871759338.png
      preview-1778871759347.png
      preview-1778871759423.png
      preview-1778871759434.png
      preview-1778871759446.png
      preview-1778871759478.png
      preview-1778871759486.png
      preview-1778871759492.png
      preview-1778871759570.png
      preview-1778871759574.png
      preview-1778871759589.png
      preview-1778871759629.png
      preview-1778871759632.png
      preview-1778871759641.png
      preview-1778871759712.png
      preview-1778871759722.png
      preview-1778871759741.png
      preview-1778871759775.png
      preview-1778871759778.png
      preview-1778871759783.png
      preview-1778871759858.png
      preview-1778871759874.png
      preview-1778871759894.png
      preview-1778871759921.png
      preview-1778871759929.png
      preview-1778871759936.png
      preview-1778871760003.png
      preview-1778871760028.png
      preview-1778871760043.png
      preview-1778871760067.png
      preview-1778871760082.png
      preview-1778871760093.png
      preview-1778871760147.png
      preview-1778871760169.png
      preview-1778871760191.png
      preview-1778871760205.png
      preview-1778871760233.png
      preview-1778871760236.png
      preview-1778871760290.png
      preview-1778871760314.png
      preview-1778871760335.png
      preview-1778871760350.png
      preview-1778871760373.png
      preview-1778871760382.png
      preview-1778871760431.png
      preview-1778871760460.png
      preview-1778871760478.png
      preview-1778871760498.png
      preview-1778871760512.png
      preview-1778871760519.png
      preview-1778871760572.png
      preview-1778871816263.png
      preview-1778871816268.png
      preview-1778871816273.png
      preview-1778871816278.png
      preview-1778871816282.png
      preview-1778871816286.png
      preview-1778871816447.png
      preview-1778871816467.png
      preview-1778871816611.png
      preview-1778871816616.png
      preview-1778871816625.png
      preview-1778871816827.png
      preview-1778871853114.png
      preview-1778871853120.png
      preview-1778871853126.png
      preview-1778871853131.png
      preview-1778871853136.png
      preview-1778871853140.png
      preview-1778871853246.png
      preview-1778871853256.png
      preview-1778871853266.png
      preview-1778871853278.png
      preview-1778871853377.png
      preview-1778871853381.png
      preview-1778871853384.png
      preview-1778871853400.png
      preview-1778871853406.png
      preview-1778871853428.png
      preview-1778871853518.png
      preview-1778871853529.png
      preview-1778871853534.png
      preview-1778871853547.png
      preview-1778871853556.png
      preview-1778871853570.png
      preview-1778871853672.png
      preview-1778871853687.png
      preview-1778871853692.png
      preview-1778871853708.png
      preview-1778871853712.png
      preview-1778871853717.png
      preview-1778871853815.png
      preview-1778871853834.png
      preview-1778871853868.png
      preview-1778871853872.png
      preview-1778871853876.png
      preview-1778871853880.png
      preview-1778871853965.png
      preview-1778871854059.png
      preview-1778871854063.png
      preview-1778871854067.png
      preview-1778871854070.png
      preview-1778871854074.png
      preview-1778871854115.png
      preview-1778871854205.png
      preview-1778871854210.png
      preview-1778871854217.png
      preview-1778871854230.png
      preview-1778871854243.png
      preview-1778871854262.png
      preview-1778871854345.png
      preview-1778871854352.png
      preview-1778871854357.png
      preview-1778871854377.png
      preview-1778871854382.png
      preview-1778871854417.png
      preview-1778871854487.png
      preview-1778871854499.png
      preview-1778871854513.png
      preview-1778871854522.png
      preview-1778871854532.png
      preview-1778871854575.png
      preview-1778871854636.png
      preview-1778871854649.png
      preview-1778871854660.png
      preview-1778871854673.png
      preview-1778871854697.png
      preview-1778871854717.png
      preview-1778871854780.png
      preview-1778871854802.png
      preview-1778871854807.png
      preview-1778871854812.png
      preview-1778871854844.png
      preview-1778872142445.png
      preview-1778872142450.png
      preview-1778872142455.png
      preview-1778872142460.png
      preview-1778872142464.png
      preview-1778872142468.png
      preview-1778872143692.png
      preview-1778872143761.png
      preview-1778872143786.png
      preview-1778872143799.png
      preview-1778872143859.png
      preview-1778872143869.png
      preview-1778872144975.png
      preview-1778872145113.png
      preview-1778872145129.png
      preview-1778872145332.png
      preview-1778872145337.png
      preview-1778872145406.png
      preview-1778872146313.png
      preview-1778872146321.png
      preview-1778872450446.png
      preview-1778872450456.png
      preview-1778872450461.png
      preview-1778872450466.png
      preview-1778872450470.png
      preview-1778872450475.png
      preview-1778872451696.png
      preview-1778872451778.png
      preview-1778872451811.png
      preview-1778872451834.png
      preview-1778872451916.png
      preview-1778872451920.png
      preview-1778872452967.png
      preview-1778872453150.png
      preview-1778872453163.png
      preview-1778872453209.png
      preview-1778872453215.png
      preview-1778872453267.png
      preview-1778872454328.png
      preview-1778872454348.png
      split-1778868460981-1-How_I_Made_dollar_4000_in_2_Days_Using_Reddit_pages_1-12.pdf
      split-1778868487780-1-How_I_Made_dollar_4000_in_2_Days_Using_Reddit_pages_1-10.pdf
      split-1778868487805-2-How_I_Made_dollar_4000_in_2_Days_Using_Reddit_pages_11-20.pdf
      split-1778868487821-3-How_I_Made_dollar_4000_in_2_Days_Using_Reddit_pages_21-30.pdf
      split-1778868487829-4-How_I_Made_dollar_4000_in_2_Days_Using_Reddit_pages_31-35.pdf
      split-1778868613597-1-03-1 paid traffic strategies_pages_1-5.pdf
      split-1778868658485-1-7_pages_1-15.pdf
      split-1778868687183-1-7_pages_1-15.pdf
      split-1778868687191-2-7_pages_16-29.pdf
      split-1778869539402-1-How_I_Made_dollar_4000_in_2_Days_Using_Reddit_pages_1-12.pdf
      split-1778869539416-2-How_I_Made_dollar_4000_in_2_Days_Using_Reddit_pages_13-24.pdf
      split-1778869539435-3-How_I_Made_dollar_4000_in_2_Days_Using_Reddit_pages_25-35.pdf
      split-1778869572775-1-How_I_Made_dollar_4000_in_2_Days_Using_Reddit_pages_1-12.pdf
      split-1778869572778-2-How_I_Made_dollar_4000_in_2_Days_Using_Reddit_page_13.pdf
      split-1778869660094-1-books-library.online-12230107Cq6V9_pages_1-12.pdf
      split-1778869660103-2-books-library.online-12230107Cq6V9_pages_13-24.pdf
      split-1778869660112-3-books-library.online-12230107Cq6V9_pages_25-36.pdf
      split-1778869660119-4-books-library.online-12230107Cq6V9_pages_37-48.pdf
      split-1778869660126-5-books-library.online-12230107Cq6V9_pages_49-60.pdf
      split-1778869660131-6-books-library.online-12230107Cq6V9_pages_61-72.pdf
      split-1778869660136-7-books-library.online-12230107Cq6V9_pages_73-84.pdf
      split-1778869660141-8-books-library.online-12230107Cq6V9_pages_85-96.pdf
      split-1778869660146-9-books-library.online-12230107Cq6V9_pages_97-108.pdf
      split-1778869660150-10-books-library.online-12230107Cq6V9_pages_109-120.pdf
      split-1778869660156-11-books-library.online-12230107Cq6V9_pages_121-132.pdf
      split-1778869660160-12-books-library.online-12230107Cq6V9_pages_133-144.pdf
      split-1778869660165-13-books-library.online-12230107Cq6V9_pages_145-156.pdf
      split-1778869660170-14-books-library.online-12230107Cq6V9_pages_157-168.pdf
      split-1778869660175-15-books-library.online-12230107Cq6V9_pages_169-180.pdf
      split-1778869660177-16-books-library.online-12230107Cq6V9_pages_181-182.pdf
      split-1778869703127-1-how_i_made_dollar_4000_in_2_days_using_reddit_pages_1-10.pdf
      split-1778871797909-1-How_I_Made_dollar_4000_in_2_Days_Using_Reddit_pages_1-32.pdf
      split-1778871881588-1-how_i_made_dollar_4000_in_2_days_using_reddit_pages_1-12.pdf
      split-1778871881599-2-how_i_made_dollar_4000_in_2_days_using_reddit_pages_13-24.pdf
      split-1778871881609-3-how_i_made_dollar_4000_in_2_days_using_reddit_pages_25-35.pdf
      split-1778872442034-1-IG Algorithm Changed. Experts Secrets_pages_1-7.pdf
      split-1778872442054-2-IG Algorithm Changed. Experts Secrets_pages_8-10.pdf
    src/
      __tests__/
        conversion.service.test.ts
        upload.test.ts
      config/
        index.ts
      middleware/
        error.middleware.ts
        upload.middleware.ts
      routes/
        cloud.ts
        convert.ts
        download.ts
        dropbox-auth.ts
        merge.ts
        split.ts
        upload.ts
      services/
        conversion.service.ts
        file.service.ts
        merge.service.ts
        split.service.ts
      index.ts
    jest.config.js
    package.json
    tsconfig.json
  web/
    src/
      app/
        dropbox-callback/
          page.tsx
        merge-pdf/
          page.tsx
        pdf-to-excel/
          page.tsx
        pdf-to-word/
          page.tsx
        split-pdf/
          page.tsx
        globals.css
        layout.tsx
        page.tsx
      components/
        CloudStorageButtons.tsx
        ConversionProgress.tsx
        DownloadButton.tsx
        DropboxFilePicker.tsx
        FileUploader.tsx
        Footer.tsx
        Header.tsx
        PdfPagePreview.tsx
        ToolCard.tsx
      hooks/
        useConversion.ts
        useMerge.ts
      lib/
        api.ts
        cloud-storage.ts
    .env.local
    next-env.d.ts
    next.config.js
    package.json
    postcss.config.js
    tailwind.config.js
    tsconfig.json
packages/
  shared/
    src/
      constants.ts
      index.ts
      types.ts
    package.json
prisma/
  schema.prisma
.env.example
.gitignore
convert_to_excel.py
convert.py
docker-compose.yml
package.json
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="apps/api/src/__tests__/conversion.service.test.ts">
import { ConversionStatus } from '@pdf-converter/shared';

// Mock the prisma client
const mockPrisma = {
  conversion: {
    create: jest.fn(),
    findUnique: jest.fn(),
    update: jest.fn(),
    delete: jest.fn(),
  },
};

jest.mock('../../node_modules/.prisma/client', () => ({
  PrismaClient: jest.fn(() => mockPrisma),
}));

// Import after mocking
let conversionService: typeof import('../services/conversion.service')['conversionService'];

beforeAll(() => {
  conversionService = require('../services/conversion.service').conversionService;
});

describe('ConversionService', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  describe('createConversion', () => {
    it('should create a new conversion record with PENDING status', async () => {
      const input = {
        originalName: 'test.pdf',
        originalSize: 1024,
        originalPath: '/uploads/test-123.pdf',
      };

      const expected = {
        id: 'clx123',
        ...input,
        userId: null,
        outputPath: null,
        status: 'PENDING',
        progress: 0,
        errorMessage: null,
        createdAt: new Date(),
        updatedAt: new Date(),
      };

      mockPrisma.conversion.create.mockResolvedValue(expected);

      const result = await conversionService.createConversion(input);

      expect(mockPrisma.conversion.create).toHaveBeenCalledWith({
        data: {
          ...input,
          status: 'PENDING',
          progress: 0,
        },
      });
      expect(result.status).toBe('PENDING');
      expect(result.originalName).toBe('test.pdf');
    });
  });

  describe('getConversion', () => {
    it('should return conversion by id', async () => {
      const expected = {
        id: 'clx123',
        originalName: 'test.pdf',
        originalSize: 1024,
        originalPath: '/uploads/test-123.pdf',
        outputPath: null,
        status: 'COMPLETED',
        progress: 100,
        errorMessage: null,
        userId: null,
        createdAt: new Date(),
        updatedAt: new Date(),
      };

      mockPrisma.conversion.findUnique.mockResolvedValue(expected);

      const result = await conversionService.getConversion('clx123');

      expect(mockPrisma.conversion.findUnique).toHaveBeenCalledWith({
        where: { id: 'clx123' },
      });
      expect(result?.status).toBe('COMPLETED');
    });

    it('should return null for non-existent id', async () => {
      mockPrisma.conversion.findUnique.mockResolvedValue(null);

      const result = await conversionService.getConversion('nonexistent');

      expect(result).toBeNull();
    });
  });

  describe('updateConversion', () => {
    it('should update conversion status and progress', async () => {
      const expected = {
        id: 'clx123',
        originalName: 'test.pdf',
        originalSize: 1024,
        originalPath: '/uploads/test-123.pdf',
        outputPath: null,
        status: 'PROCESSING',
        progress: 50,
        errorMessage: null,
        userId: null,
        createdAt: new Date(),
        updatedAt: new Date(),
      };

      mockPrisma.conversion.update.mockResolvedValue(expected);

      const result = await conversionService.updateConversion('clx123', {
        status: 'PROCESSING',
        progress: 50,
      });

      expect(mockPrisma.conversion.update).toHaveBeenCalledWith({
        where: { id: 'clx123' },
        data: { status: 'PROCESSING', progress: 50 },
      });
      expect(result.progress).toBe(50);
    });

    it('should update to COMPLETED with outputPath', async () => {
      const expected = {
        id: 'clx123',
        originalName: 'test.pdf',
        originalSize: 1024,
        originalPath: '/uploads/test-123.pdf',
        outputPath: '/outputs/test-123.docx',
        status: 'COMPLETED',
        progress: 100,
        errorMessage: null,
        userId: null,
        createdAt: new Date(),
        updatedAt: new Date(),
      };

      mockPrisma.conversion.update.mockResolvedValue(expected);

      const result = await conversionService.updateConversion('clx123', {
        status: 'COMPLETED',
        progress: 100,
        outputPath: '/outputs/test-123.docx',
      });

      expect(result.status).toBe('COMPLETED');
      expect(result.outputPath).toBe('/outputs/test-123.docx');
    });

    it('should update to FAILED with errorMessage', async () => {
      const expected = {
        id: 'clx123',
        originalName: 'test.pdf',
        originalSize: 1024,
        originalPath: '/uploads/test-123.pdf',
        outputPath: null,
        status: 'FAILED',
        progress: 0,
        errorMessage: 'Conversion engine error',
        userId: null,
        createdAt: new Date(),
        updatedAt: new Date(),
      };

      mockPrisma.conversion.update.mockResolvedValue(expected);

      const result = await conversionService.updateConversion('clx123', {
        status: 'FAILED',
        errorMessage: 'Conversion engine error',
      });

      expect(result.status).toBe('FAILED');
      expect(result.errorMessage).toBe('Conversion engine error');
    });
  });

  describe('deleteConversion', () => {
    it('should delete a conversion record', async () => {
      const expected = {
        id: 'clx123',
        originalName: 'test.pdf',
        originalSize: 1024,
        originalPath: '/uploads/test-123.pdf',
        outputPath: null,
        status: 'PENDING',
        progress: 0,
        errorMessage: null,
        userId: null,
        createdAt: new Date(),
        updatedAt: new Date(),
      };

      mockPrisma.conversion.delete.mockResolvedValue(expected);

      await conversionService.deleteConversion('clx123');

      expect(mockPrisma.conversion.delete).toHaveBeenCalledWith({
        where: { id: 'clx123' },
      });
    });
  });
});
</file>

<file path="apps/api/src/__tests__/upload.test.ts">
import request from 'supertest';
import express from 'express';
import { ConversionStatus } from '@pdf-converter/shared';

// Mock the conversion service
const mockConversionService = {
  createConversion: jest.fn(),
  getConversion: jest.fn(),
  updateConversion: jest.fn(),
  deleteConversion: jest.fn(),
};

jest.mock('../services/conversion.service', () => ({
  conversionService: mockConversionService,
}));

// Mock multer
jest.mock('../middleware/upload.middleware', () => ({
  uploadMiddleware: (req: any, res: any, next: any) => {
    req.file = {
      fieldname: 'file',
      originalname: 'test.pdf',
      size: 1024,
      path: '/uploads/test-123.pdf',
      mimetype: 'application/pdf',
    } as Express.Multer.File;
    next();
  },
}));

let app: express.Application;
let uploadRoute: typeof import('../routes/upload');

beforeAll(() => {
  uploadRoute = require('../routes/upload');
  app = express();
  app.use(express.json());
  app.use('/api/upload', uploadRoute.uploadRouter);
});

describe('POST /api/upload', () => {
  beforeEach(() => {
    jest.clearAllMocks();
  });

  it('should upload a PDF and return conversion record', async () => {
    const mockConversion = {
      id: 'clx123',
      originalName: 'test.pdf',
      originalSize: 1024,
      originalPath: '/uploads/test-123.pdf',
      status: 'PENDING',
      progress: 0,
    };

    mockConversionService.createConversion.mockResolvedValue(mockConversion);

    const response = await request(app)
      .post('/api/upload')
      .attach('file', Buffer.from('fake pdf content'), 'test.pdf')
      .expect(200);

    expect(response.body).toMatchObject({
      id: 'clx123',
      originalName: 'test.pdf',
      originalSize: 1024,
      status: 'PENDING',
    });

    expect(mockConversionService.createConversion).toHaveBeenCalledWith(
      expect.objectContaining({
        originalName: 'test.pdf',
        originalSize: 1024,
      })
    );
  });

  it('should return 400 when no file is provided', async () => {
    // Override the mock for this test to simulate no file
    jest.doMock('../middleware/upload.middleware', () => ({
      uploadMiddleware: (req: any, res: any, next: any) => {
        req.file = undefined;
        next();
      },
    }));

    const response = await request(app)
      .post('/api/upload')
      .expect(500); // Will fail because no file - our error handling should catch this
  });
});
</file>

<file path="apps/api/src/middleware/error.middleware.ts">
import { Request, Response, NextFunction } from 'express';
import { ApiError } from '@pdf-converter/shared';

export class AppError extends Error {
  public statusCode: number;
  public details?: string;

  constructor(message: string, statusCode: number, details?: string) {
    super(message);
    this.statusCode = statusCode;
    this.details = details;
    Object.setPrototypeOf(this, AppError.prototype);
  }
}

export function errorHandler(
  err: Error | AppError,
  _req: Request,
  res: Response,
  _next: NextFunction
) {
  console.error('Error:', err);

  if (err instanceof AppError) {
    const response: ApiError = {
      message: err.message,
      statusCode: err.statusCode,
      details: err.details,
    };
    return res.status(err.statusCode).json(response);
  }

  // Multer file size error
  if (err.message?.includes('File too large')) {
    const response: ApiError = {
      message: 'File size exceeds the maximum allowed size (50MB)',
      statusCode: 413,
    };
    return res.status(413).json(response);
  }

  // Multer file type error
  if (err.message?.includes('Only PDF files are allowed')) {
    const response: ApiError = {
      message: err.message,
      statusCode: 400,
    };
    return res.status(400).json(response);
  }

  // Default server error
  const response: ApiError = {
    message: 'Internal server error',
    statusCode: 500,
  };
  return res.status(500).json(response);
}
</file>

<file path="apps/api/src/middleware/upload.middleware.ts">
import multer from 'multer';
import path from 'path';
import { config } from '../config';
import { fileService } from '../services/file.service';

const storage = multer.diskStorage({
  destination: (_req, _file, cb) => {
    cb(null, config.uploadDir);
  },
  filename: (_req, file, cb) => {
    const uniqueName = fileService.generateUniqueFilename(file.originalname);
    cb(null, uniqueName);
  },
});

const fileFilter = (_req: Express.Request, file: Express.Multer.File, cb: multer.FileFilterCallback) => {
  const allowedMimes = ['application/pdf'];
  const allowedExts = ['.pdf'];
  const ext = path.extname(file.originalname).toLowerCase();

  if (allowedMimes.includes(file.mimetype) && allowedExts.includes(ext)) {
    cb(null, true);
  } else {
    cb(new Error('Only PDF files are allowed'));
  }
};

export const uploadMiddleware = multer({
  storage,
  fileFilter,
  limits: {
    fileSize: config.maxFileSize,
  },
});
</file>

<file path="apps/api/src/routes/cloud.ts">
import { Router, Request, Response } from 'express';
import { conversionService } from '../services/conversion.service';
import { fileService } from '../services/file.service';
import { AppError } from '../middleware/error.middleware';
import path from 'path';
import fs from 'fs';

export const cloudRouter = Router();

// POST /api/cloud/download/drive - Download file from Google Drive
cloudRouter.post('/download/drive', async (req: Request, res: Response, next: Function) => {
  try {
    const { accessToken, fileId, fileName } = req.body;

    if (!accessToken || !fileId || !fileName) {
      throw new AppError('accessToken, fileId, and fileName are required', 400);
    }

    console.log(`☁️  Downloading from Google Drive: ${fileName} (${fileId})`);

    // Use the Drive API v3 with alt=media to get the actual file content
    const driveUrl = `https://www.googleapis.com/drive/v3/files/${fileId}?alt=media&supportsAllDrives=true`;
    const response = await fetch(driveUrl, {
      headers: { Authorization: `Bearer ${accessToken}` },
    });

    if (!response.ok) {
      const errorText = await response.text();
      let errorMessage = `Google Drive download failed: ${response.status}`;
      try {
        const errorJson = JSON.parse(errorText);
        errorMessage = errorJson.error?.message || errorMessage;

        // Handle specific error codes
        if (response.status === 403) {
          errorMessage = 'Google Drive access denied. The file may be shared differently or the token has expired. Please try selecting the file again.';
        } else if (response.status === 404) {
          errorMessage = 'File not found on Google Drive. It may have been moved or deleted.';
        }
      } catch {}
      console.error(`   ❌ Google Drive error: ${errorMessage}`);
      throw new AppError(errorMessage, 502);
    }

    // Validate PDF header
    const arrayBuffer = await response.arrayBuffer();
    const buffer = Buffer.from(arrayBuffer);
    const header = buffer.slice(0, 5).toString('ascii');

    if (header !== '%PDF-') {
      console.error(`   ❌ Invalid PDF from Google Drive: header="${header}"`);
      throw new AppError('Downloaded file is not a valid PDF.', 502);
    }

    const uniqueName = fileService.generateUniqueFilename(fileName);
    const uploadDir = fileService.getAbsoluteUploadDir();
    const filePath = path.join(uploadDir, uniqueName);

    fs.writeFileSync(filePath, buffer);
    console.log(`   ✅ Saved: ${filePath} (${(buffer.length / 1024).toFixed(1)} KB)`);

    res.status(200).json({
      filePath,
      fileName,
      fileSize: buffer.length,
    });
  } catch (error) {
    next(error);
  }
});

// POST /api/cloud/download/dropbox - Download file from Dropbox (legacy Chooser)
cloudRouter.post('/download/dropbox', async (req: Request, res: Response, next: Function) => {
  try {
    const { link, fileName } = req.body;

    if (!link || !fileName) {
      throw new AppError('link and fileName are required', 400);
    }

    console.log(`☁️  Downloading from Dropbox: ${fileName}`);

    const downloadUrl = link.replace(/\?dl=\d/, '?dl=1');
    const response = await fetch(downloadUrl);

    if (!response.ok) {
      throw new AppError(`Dropbox download failed: ${response.status}`, 502);
    }

    const arrayBuffer = await response.arrayBuffer();
    const buffer = Buffer.from(arrayBuffer);
    const header = buffer.slice(0, 5).toString('ascii');

    if (header !== '%PDF-') {
      throw new AppError('Downloaded file is not a valid PDF.', 502);
    }

    const uniqueName = fileService.generateUniqueFilename(fileName);
    const uploadDir = fileService.getAbsoluteUploadDir();
    const filePath = path.join(uploadDir, uniqueName);

    fs.writeFileSync(filePath, buffer);
    console.log(`   ✅ Saved: ${filePath} (${(buffer.length / 1024).toFixed(1)} KB)`);

    res.status(200).json({
      filePath,
      fileName,
      fileSize: buffer.length,
    });
  } catch (error) {
    next(error);
  }
});

// POST /api/cloud/convert/drive - Download from Google Drive + start conversion
cloudRouter.post('/convert/drive', async (req: Request, res: Response, next: Function) => {
  try {
    const { accessToken, fileId, fileName } = req.body;

    if (!accessToken || !fileId || !fileName) {
      throw new AppError('accessToken, fileId, and fileName are required', 400);
    }

    console.log(`☁️  Google Drive → Convert: ${fileName}`);

    const driveUrl = `https://www.googleapis.com/drive/v3/files/${fileId}?alt=media&supportsAllDrives=true`;
    const driveResponse = await fetch(driveUrl, {
      headers: { Authorization: `Bearer ${accessToken}` },
    });

    if (!driveResponse.ok) {
      const errorText = await driveResponse.text();
      let errorMessage = `Google Drive download failed: ${driveResponse.status}`;
      try {
        const errorJson = JSON.parse(errorText);
        errorMessage = errorJson.error?.message || errorMessage;
        if (driveResponse.status === 403) {
          errorMessage = 'Google Drive access denied. Please try selecting the file again.';
        }
      } catch {}
      throw new AppError(errorMessage, 502);
    }

    const arrayBuffer = await driveResponse.arrayBuffer();
    const buffer = Buffer.from(arrayBuffer);
    const header = buffer.slice(0, 5).toString('ascii');

    if (header !== '%PDF-') {
      throw new AppError('Downloaded file is not a valid PDF.', 502);
    }

    const uniqueName = fileService.generateUniqueFilename(fileName);
    const uploadDir = fileService.getAbsoluteUploadDir();
    const filePath = path.join(uploadDir, uniqueName);

    fs.writeFileSync(filePath, buffer);

    const fileSize = buffer.length;

    const conversion = await conversionService.createConversion({
      originalName: fileName,
      originalSize: fileSize,
      originalPath: filePath,
    });

    res.status(200).json({
      id: conversion.id,
      originalName: conversion.originalName,
      originalSize: conversion.originalSize,
      status: conversion.status,
    });
  } catch (error) {
    next(error);
  }
});

// POST /api/cloud/convert/dropbox - Download from Dropbox + start conversion
cloudRouter.post('/convert/dropbox', async (req: Request, res: Response, next: Function) => {
  try {
    const { link, fileName } = req.body;

    if (!link || !fileName) {
      throw new AppError('link and fileName are required', 400);
    }

    console.log(`☁️  Dropbox → Convert: ${fileName}`);

    const downloadUrl = link.replace(/\?dl=\d/, '?dl=1');
    const dropboxResponse = await fetch(downloadUrl);

    if (!dropboxResponse.ok) {
      throw new AppError(`Dropbox download failed: ${dropboxResponse.status}`, 502);
    }

    const arrayBuffer = await dropboxResponse.arrayBuffer();
    const buffer = Buffer.from(arrayBuffer);
    const header = buffer.slice(0, 5).toString('ascii');

    if (header !== '%PDF-') {
      throw new AppError('Downloaded file is not a valid PDF.', 502);
    }

    const uniqueName = fileService.generateUniqueFilename(fileName);
    const uploadDir = fileService.getAbsoluteUploadDir();
    const filePath = path.join(uploadDir, uniqueName);

    fs.writeFileSync(filePath, buffer);

    const fileSize = buffer.length;

    const conversion = await conversionService.createConversion({
      originalName: fileName,
      originalSize: fileSize,
      originalPath: filePath,
    });

    res.status(200).json({
      id: conversion.id,
      originalName: conversion.originalName,
      originalSize: conversion.originalSize,
      status: conversion.status,
    });
  } catch (error) {
    next(error);
  }
});
</file>

<file path="apps/api/src/routes/convert.ts">
import { Router, Request, Response } from 'express';
import { conversionService } from '../services/conversion.service';
import { AppError } from '../middleware/error.middleware';
import { config } from '../config';
import { fileService } from '../services/file.service';
import { exec } from 'child_process';
import { promisify } from 'util';
import path from 'path';
import fs from 'fs';

const execAsync = promisify(exec);

export const convertRouter = Router();

let conversionQueue: any = null;
let conversionWorker: any = null;
let redisAvailable = false;

async function getQueue() {
  if (!conversionQueue && redisAvailable) {
    try {
      const { Queue } = await import('bullmq');
      conversionQueue = new Queue('pdf-conversion', {
        connection: config.redisConnection,
      });
    } catch (err) {
      redisAvailable = false;
      throw new AppError('Conversion service unavailable', 503);
    }
  }
  return conversionQueue;
}

async function initializeWorker() {
  try {
    const { Worker } = await import('bullmq');
    conversionWorker = new Worker(
      'pdf-conversion',
      async (job: any) => {
        const { conversionId, originalPath, originalName, outputFormat } = job.data;
        return await processConversion(conversionId, originalPath, originalName, outputFormat);
      },
      {
        connection: config.redisConnection,
        concurrency: 2,
      }
    );
    conversionWorker.on('failed', (job: any, err: Error) => {
      console.error(`❌ Job ${job?.id} failed:`, err.message);
    });
    conversionWorker.on('completed', (job: any) => {
      console.log(`✅ Job ${job?.id} completed successfully`);
    });
    redisAvailable = true;
    console.log('✅ BullMQ Worker connected to Redis');
  } catch (err) {
    console.warn('⚠️  Redis unavailable — using direct conversion mode');
    redisAvailable = false;
  }
}

initializeWorker();

function findOutputFile(outputPath: string): string | null {
  if (fs.existsSync(outputPath) && fs.statSync(outputPath).size > 0) {
    return outputPath;
  }
  return null;
}

function getPythonCommand(): string {
  const projectRoot = path.resolve(process.cwd(), '..', '..');
  const venvPath = path.join(projectRoot, '.venv', 'bin', 'python3');
  if (fs.existsSync(venvPath)) return venvPath;
  const cwdVenv = path.resolve(process.cwd(), '.venv', 'bin', 'python3');
  if (fs.existsSync(cwdVenv)) return cwdVenv;
  return 'python3';
}

function getScriptPath(scriptName: string): string {
  const projectRoot = path.resolve(process.cwd(), '..', '..');
  const scriptPath = path.join(projectRoot, scriptName);
  if (fs.existsSync(scriptPath)) return scriptPath;
  const cwdScript = path.resolve(process.cwd(), scriptName);
  if (fs.existsSync(cwdScript)) return cwdScript;
  return path.resolve(process.cwd(), '..', '..', scriptName);
}

// ===== PDF to Word (docx) conversion =====
async function convertToDocx(
  conversionId: string,
  absoluteInputPath: string,
  absoluteOutputDir: string,
  basename: string
): Promise<string> {
  const absoluteOutputPath = path.join(absoluteOutputDir, `${basename}.docx`);
  let conversionSucceeded = false;

  // Method 1: Python pdf2docx
  console.log(`   Method 1: Python pdf2docx...`);
  const pythonCmd = getPythonCommand();
  const scriptPath = getScriptPath('convert.py');

  if (fs.existsSync(scriptPath)) {
    try {
      await conversionService.updateConversion(conversionId, { progress: 20 });
      const pyCommand = `${pythonCmd} "${scriptPath}" "${absoluteInputPath}" "${absoluteOutputPath}"`;
      console.log(`   Command: ${pyCommand}`);
      const { stdout, stderr } = await execAsync(pyCommand, { timeout: 180000 });
      if (stdout) console.log(`   stdout: ${stdout.trim()}`);
      if (stderr) console.log(`   stderr: ${stderr.trim()}`);
      const found = findOutputFile(absoluteOutputPath);
      if (found) { conversionSucceeded = true; console.log(`   ✅ pdf2docx succeeded`); }
    } catch (err: any) { console.warn(`   ⚠️ pdf2docx failed: ${err.message}`); }
  }

  // Method 2: LibreOffice with explicit filter
  if (!conversionSucceeded) {
    console.log(`   Method 2: LibreOffice explicit filter...`);
    try {
      await conversionService.updateConversion(conversionId, { progress: 30 });
      const command = `libreoffice --headless --infilter="writer_pdf_import" --convert-to "docx:MS Word 2007 XML" --outdir "${absoluteOutputDir}" "${absoluteInputPath}"`;
      const { stdout, stderr } = await execAsync(command, { timeout: 120000 });
      const found = findOutputFile(absoluteOutputPath);
      if (found) { conversionSucceeded = true; console.log(`   ✅ LibreOffice explicit filter succeeded`); }
    } catch (err: any) { console.warn(`   ⚠️ LibreOffice explicit failed: ${err.message}`); }
  }

  // Method 3: LibreOffice basic
  if (!conversionSucceeded) {
    console.log(`   Method 3: LibreOffice basic...`);
    try {
      await conversionService.updateConversion(conversionId, { progress: 40 });
      const command = `libreoffice --headless --convert-to docx --outdir "${absoluteOutputDir}" "${absoluteInputPath}"`;
      await execAsync(command, { timeout: 120000 });
      const found = findOutputFile(absoluteOutputPath);
      if (found) { conversionSucceeded = true; console.log(`   ✅ LibreOffice basic succeeded`); }
    } catch (err: any) { console.warn(`   ⚠️ LibreOffice basic failed: ${err.message}`); }
  }

  // Method 4: soffice fallback
  if (!conversionSucceeded) {
    console.log(`   Method 4: soffice...`);
    try {
      const command = `soffice --headless --infilter="writer_pdf_import" --convert-to "docx:MS Word 2007 XML" --outdir "${absoluteOutputDir}" "${absoluteInputPath}"`;
      await execAsync(command, { timeout: 120000 });
      const found = findOutputFile(absoluteOutputPath);
      if (found) { conversionSucceeded = true; console.log(`   ✅ soffice succeeded`); }
    } catch (err: any) { console.warn(`   ⚠️ soffice failed: ${err.message}`); }
  }

  if (!conversionSucceeded) throw new Error('All DOCX conversion methods failed');
  return absoluteOutputPath;
}

// ===== PDF to Excel (xlsx) conversion =====
async function convertToXlsx(
  conversionId: string,
  absoluteInputPath: string,
  absoluteOutputDir: string,
  basename: string
): Promise<string> {
  const absoluteOutputPath = path.join(absoluteOutputDir, `${basename}.xlsx`);
  let conversionSucceeded = false;

  // Method 1: Python pdfplumber + openpyxl
  console.log(`   Method 1: Python pdfplumber + openpyxl...`);
  const pythonCmd = getPythonCommand();
  const scriptPath = getScriptPath('convert_to_excel.py');

  if (fs.existsSync(scriptPath)) {
    try {
      await conversionService.updateConversion(conversionId, { progress: 20 });
      const pyCommand = `${pythonCmd} "${scriptPath}" "${absoluteInputPath}" "${absoluteOutputPath}"`;
      console.log(`   Command: ${pyCommand}`);
      const { stdout, stderr } = await execAsync(pyCommand, { timeout: 180000 });
      if (stdout) console.log(`   stdout: ${stdout.trim()}`);
      if (stderr) console.log(`   stderr: ${stderr.trim()}`);
      const found = findOutputFile(absoluteOutputPath);
      if (found) { conversionSucceeded = true; console.log(`   ✅ pdfplumber succeeded`); }
    } catch (err: any) { console.warn(`   ⚠️ pdfplumber failed: ${err.message}`); }
  }

  // Method 2: LibreOffice with Excel filter
  if (!conversionSucceeded) {
    console.log(`   Method 2: LibreOffice Excel filter...`);
    try {
      await conversionService.updateConversion(conversionId, { progress: 30 });
      const command = `libreoffice --headless --infilter="writer_pdf_import" --convert-to "xlsx:Calc MS Excel 2007 XML" --outdir "${absoluteOutputDir}" "${absoluteInputPath}"`;
      console.log(`   Command: ${command}`);
      const { stdout, stderr } = await execAsync(command, { timeout: 120000 });
      if (stdout) console.log(`   stdout: ${stdout.trim()}`);
      if (stderr) console.log(`   stderr: ${stderr.trim()}`);
      const found = findOutputFile(absoluteOutputPath);
      if (found) { conversionSucceeded = true; console.log(`   ✅ LibreOffice Excel filter succeeded`); }
    } catch (err: any) { console.warn(`   ⚠️ LibreOffice Excel filter failed: ${err.message}`); }
  }

  // Method 3: LibreOffice with generic calc filter
  if (!conversionSucceeded) {
    console.log(`   Method 3: LibreOffice calc filter...`);
    try {
      await conversionService.updateConversion(conversionId, { progress: 40 });
      const command = `libreoffice --headless --convert-to xlsx --outdir "${absoluteOutputDir}" "${absoluteInputPath}"`;
      console.log(`   Command: ${command}`);
      const { stdout, stderr } = await execAsync(command, { timeout: 120000 });
      if (stdout) console.log(`   stdout: ${stdout.trim()}`);
      if (stderr) console.log(`   stderr: ${stderr.trim()}`);
      const found = findOutputFile(absoluteOutputPath);
      if (found) { conversionSucceeded = true; console.log(`   ✅ LibreOffice calc filter succeeded`); }
    } catch (err: any) { console.warn(`   ⚠️ LibreOffice calc filter failed: ${err.message}`); }
  }

  // Method 4: soffice fallback
  if (!conversionSucceeded) {
    console.log(`   Method 4: soffice fallback...`);
    try {
      const command = `soffice --headless --convert-to xlsx --outdir "${absoluteOutputDir}" "${absoluteInputPath}"`;
      await execAsync(command, { timeout: 120000 });
      const found = findOutputFile(absoluteOutputPath);
      if (found) { conversionSucceeded = true; console.log(`   ✅ soffice xlsx succeeded`); }
    } catch (err: any) { console.warn(`   ⚠️ soffice failed: ${err.message}`); }
  }

  if (!conversionSucceeded) throw new Error('All XLSX conversion methods failed');
  return absoluteOutputPath;
}

// ===== Main processConversion function =====
async function processConversion(
  conversionId: string,
  originalPath: string,
  originalName: string,
  outputFormat: string = 'docx'
) {
  const absoluteInputPath = path.resolve(originalPath);
  const absoluteOutputDir = fileService.getAbsoluteOutputDir();
  const basename = path.basename(absoluteInputPath, path.extname(absoluteInputPath));

  console.log(`🔄 Starting conversion: ${originalName} → ${outputFormat.toUpperCase()}`);
  console.log(`   Input: ${absoluteInputPath}`);

  try {
    if (!fs.existsSync(absoluteInputPath)) {
      throw new Error(`Input file not found: ${absoluteInputPath}`);
    }

    await conversionService.updateConversion(conversionId, {
      status: 'PROCESSING',
      progress: 10,
    });

    let absoluteOutputPath: string;

    if (outputFormat === 'xlsx') {
      absoluteOutputPath = await convertToXlsx(conversionId, absoluteInputPath, absoluteOutputDir, basename);
    } else {
      absoluteOutputPath = await convertToDocx(conversionId, absoluteInputPath, absoluteOutputDir, basename);
    }

    await conversionService.updateConversion(conversionId, { progress: 90 });

    const fileStats = fs.statSync(absoluteOutputPath);
    console.log(`   ✅ Output: ${absoluteOutputPath} (${(fileStats.size / 1024).toFixed(1)} KB)`);

    await conversionService.updateConversion(conversionId, {
      status: 'COMPLETED',
      progress: 100,
      outputPath: absoluteOutputPath,
    });

    return { success: true, outputPath: absoluteOutputPath };
  } catch (error: any) {
    console.error(`   ❌ Conversion failed: ${error.message}`);
    await conversionService.updateConversion(conversionId, {
      status: 'FAILED',
      errorMessage: error.message || 'Conversion failed',
      progress: 0,
    });
    throw error;
  }
}

// POST /api/convert/:id - Trigger conversion
convertRouter.post('/:id', async (req: Request, res: Response, next: Function) => {
  try {
    const { id } = req.params;
    const conversion = await conversionService.getConversion(id);

    if (!conversion) throw new AppError('Conversion not found', 404);
    if (conversion.status === 'PROCESSING') throw new AppError('Conversion is already in progress', 409);
    if (conversion.status === 'COMPLETED') throw new AppError('Conversion already completed', 409);

    const outputFormat = conversion.outputFormat || 'docx';

    if (redisAvailable) {
      try {
        const queue = await getQueue();
        if (queue) {
          await queue.add('convert-pdf', {
            conversionId: id,
            originalPath: conversion.originalPath,
            originalName: conversion.originalName,
            outputFormat,
          }, { attempts: 3, backoff: { type: 'exponential', delay: 5000 } });
        }
      } catch (queueErr) {
        processConversion(id, conversion.originalPath, conversion.originalName, outputFormat).catch((err) => {
          console.error('Direct conversion error:', err.message);
        });
      }
    } else {
      processConversion(id, conversion.originalPath, conversion.originalName, outputFormat).catch((err) => {
        console.error('Direct conversion error:', err.message);
      });
    }

    await conversionService.updateConversion(id, { status: 'PROCESSING', progress: 0 });

    res.status(200).json({ id, status: 'PROCESSING', progress: 0, outputFormat, message: 'Conversion started' });
  } catch (error) {
    next(error);
  }
});

// GET /api/convert/status/:id - Check conversion status
convertRouter.get('/status/:id', async (req: Request, res: Response, next: Function) => {
  try {
    const { id } = req.params;
    const conversion = await conversionService.getConversion(id);

    if (!conversion) throw new AppError('Conversion not found', 404);

    const response: Record<string, unknown> = {
      id: conversion.id,
      status: conversion.status,
      progress: conversion.progress,
      outputFormat: conversion.outputFormat,
    };

    if (conversion.status === 'COMPLETED' && conversion.outputPath) {
      response.downloadUrl = `/api/download/${conversion.id}`;
    }

    if (conversion.status === 'FAILED' && conversion.errorMessage) {
      response.errorMessage = conversion.errorMessage;
    }

    res.status(200).json(response);
  } catch (error) {
    next(error);
  }
});
</file>

<file path="apps/api/src/routes/download.ts">
import { Router, Request, Response } from 'express';
import path from 'path';
import fs from 'fs';
import { conversionService } from '../services/conversion.service';
import { AppError } from '../middleware/error.middleware';

export const downloadRouter = Router();

downloadRouter.get('/:id', async (req: Request, res: Response, next: Function) => {
  try {
    const { id } = req.params;
    const conversion = await conversionService.getConversion(id);

    if (!conversion) {
      throw new AppError('Conversion not found', 404);
    }

    if (conversion.status !== 'COMPLETED') {
      throw new AppError('Conversion not completed yet', 400);
    }

    if (!conversion.outputPath) {
      throw new AppError('Output file not available', 404);
    }

    const absolutePath = path.resolve(conversion.outputPath);
    
    if (!fs.existsSync(absolutePath)) {
      throw new AppError('Output file no longer exists', 404);
    }

    const originalName = path.basename(
      conversion.originalName,
      path.extname(conversion.originalName)
    );
    const outputFormat = conversion.outputFormat || 'docx';
    const downloadName = `${originalName}.${outputFormat}`;

    res.download(absolutePath, downloadName, (err) => {
      if (err) {
        console.error('Download error:', err);
        if (!res.headersSent) {
          next(new AppError('Download failed', 500));
        }
      }
    });
  } catch (error) {
    next(error);
  }
});
</file>

<file path="apps/api/src/routes/dropbox-auth.ts">
import { Router, Request, Response } from 'express';
import { AppError } from '../middleware/error.middleware';
import path from 'path';
import fs from 'fs';

export const dropboxAuthRouter = Router();

const DROPBOX_CLIENT_ID = process.env.DROPBOX_CLIENT_ID || '';
const DROPBOX_CLIENT_SECRET = process.env.DROPBOX_CLIENT_SECRET || '';
const REDIRECT_URI = process.env.DROPBOX_REDIRECT_URI || 'http://localhost:3001/api/dropbox-auth/callback';

// GET /api/dropbox-auth/url - Get the Dropbox authorization URL
dropboxAuthRouter.get('/url', (_req: Request, res: Response, next: Function) => {
  try {
    if (!DROPBOX_CLIENT_ID) {
      throw new AppError('Dropbox not configured on server', 500);
    }

    const state = Math.random().toString(36).substring(2, 15);
    const authUrl = `https://www.dropbox.com/oauth2/authorize?client_id=${DROPBOX_CLIENT_ID}&response_type=code&redirect_uri=${encodeURIComponent(REDIRECT_URI)}&state=${state}&token_access_type=offline`;

    res.json({ authUrl, state });
  } catch (error) {
    next(error);
  }
});

// GET /api/dropbox-auth/callback - OAuth callback
dropboxAuthRouter.get('/callback', async (req: Request, res: Response, next: Function) => {
  try {
    const { code, error: oauthError, error_description } = req.query;

    if (oauthError) {
      return res.redirect(`http://localhost:3000/dropbox-callback?error=${encodeURIComponent(error_description as string || oauthError as string)}`);
    }

    if (!code) {
      return res.redirect(`http://localhost:3000/dropbox-callback?error=No+authorization+code+received`);
    }

    const tokenResponse = await fetch('https://api.dropboxapi.com/oauth2/token', {
      method: 'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
      body: new URLSearchParams({
        code: code as string,
        grant_type: 'authorization_code',
        client_id: DROPBOX_CLIENT_ID,
        client_secret: DROPBOX_CLIENT_SECRET,
        redirect_uri: REDIRECT_URI,
      }).toString(),
    });

    if (!tokenResponse.ok) {
      const errData = await tokenResponse.json().catch(() => ({}));
      console.error('Token exchange failed:', errData);
      return res.redirect(`http://localhost:3000/dropbox-callback?error=${encodeURIComponent('Failed to get access token')}`);
    }

    const tokenData = await tokenResponse.json();
    const redirectUrl = `http://localhost:3000/dropbox-callback?access_token=${encodeURIComponent(tokenData.access_token)}`;

    res.redirect(redirectUrl);
  } catch (error) {
    console.error('Dropbox OAuth callback error:', error);
    res.redirect(`http://localhost:3000/dropbox-callback?error=${encodeURIComponent('OAuth callback failed')}`);
  }
});

// POST /api/dropbox-auth/files - List PDF files
dropboxAuthRouter.post('/files', async (req: Request, res: Response, next: Function) => {
  try {
    const { accessToken, path: filePath = '' } = req.body;

    if (!accessToken) {
      throw new AppError('Access token is required', 400);
    }

    const response = await fetch('https://api.dropboxapi.com/2/files/list_folder', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${accessToken}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        path: filePath || '',
        limit: 100,
      }),
    });

    if (!response.ok) {
      const errData = await response.json().catch(() => ({}));
      throw new AppError(errData.error_summary || 'Failed to list Dropbox files', 502);
    }

    const data = await response.json();

    const items = data.entries
      .filter((entry: any) => entry['.tag'] === 'folder' || (entry.name && entry.name.toLowerCase().endsWith('.pdf')))
      .map((entry: any) => ({
        id: entry.id,
        name: entry.name,
        type: entry['.tag'],
        path: entry.path_lower,
        size: entry.size || 0,
        modified: entry.server_modified || entry.client_modified,
      }));

    res.json({ items, hasMore: data.has_more, cursor: data.cursor });
  } catch (error) {
    next(error);
  }
});

// POST /api/dropbox-auth/files/continue - Pagination
dropboxAuthRouter.post('/files/continue', async (req: Request, res: Response, next: Function) => {
  try {
    const { accessToken, cursor } = req.body;

    if (!accessToken || !cursor) {
      throw new AppError('accessToken and cursor are required', 400);
    }

    const response = await fetch('https://api.dropboxapi.com/2/files/list_folder/continue', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${accessToken}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ cursor }),
    });

    if (!response.ok) {
      const errData = await response.json().catch(() => ({}));
      throw new AppError(errData.error_summary || 'Failed to list more files', 502);
    }

    const data = await response.json();

    const items = data.entries
      .filter((entry: any) => entry['.tag'] === 'folder' || (entry.name && entry.name.toLowerCase().endsWith('.pdf')))
      .map((entry: any) => ({
        id: entry.id,
        name: entry.name,
        type: entry['.tag'],
        path: entry.path_lower,
        size: entry.size || 0,
        modified: entry.server_modified || entry.client_modified,
      }));

    res.json({ items, hasMore: data.has_more, cursor: data.cursor });
  } catch (error) {
    next(error);
  }
});

// POST /api/dropbox-auth/download - Download a file from Dropbox
dropboxAuthRouter.post('/download', async (req: Request, res: Response, next: Function) => {
  try {
    const { accessToken, path: filePath } = req.body;

    if (!accessToken || !filePath) {
      throw new AppError('accessToken and path are required', 400);
    }

    console.log(`☁️  Downloading from Dropbox API: ${filePath}`);

    const response = await fetch('https://content.dropboxapi.com/2/files/download', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${accessToken}`,
        'Dropbox-API-Arg': JSON.stringify({ path: filePath }),
      },
    });

    if (!response.ok) {
      const errText = await response.text();
      let errMsg = `Dropbox download failed: ${response.status}`;
      try {
        const errJson = JSON.parse(errText);
        errMsg = errJson.error_summary || errMsg;
      } catch {}
      console.error(`   ❌ Dropbox download error: ${errMsg}`);
      throw new AppError(errMsg, 502);
    }

    // Validate that the response is actually a PDF
    const contentType = response.headers.get('content-type') || '';
    const arrayBuffer = await response.arrayBuffer();
    const buffer = Buffer.from(arrayBuffer);

    // Check PDF header — every valid PDF starts with %PDF
    const header = buffer.slice(0, 5).toString('ascii');
    if (header !== '%PDF-') {
      console.error(`   ❌ Invalid PDF header: "${header}" (expected "%PDF-")`);
      console.error(`   Response content-type: ${contentType}`);
      console.error(`   Response size: ${buffer.length} bytes`);
      console.error(`   First 200 bytes: ${buffer.slice(0, 200).toString('utf-8')}`);
      throw new AppError('Downloaded file is not a valid PDF. The Dropbox download may have returned an error page.', 502);
    }

    // Save to uploads directory
    const { fileService } = require('../services/file.service');
    const fileName = filePath.split('/').pop() || 'file.pdf';
    const uniqueName = fileService.generateUniqueFilename(fileName);
    const uploadDir = fileService.getAbsoluteUploadDir();
    const fullPath = path.join(uploadDir, uniqueName);

    fs.writeFileSync(fullPath, buffer);

    console.log(`   ✅ Saved: ${fullPath} (${(buffer.length / 1024).toFixed(1)} KB)`);

    res.json({
      filePath: fullPath,
      fileName,
      fileSize: buffer.length,
    });
  } catch (error) {
    next(error);
  }
});
</file>

<file path="apps/api/src/routes/merge.ts">
import { Router, Request, Response } from 'express';
import { mergeService } from '../services/merge.service';
import { uploadMiddleware } from '../middleware/upload.middleware';
import { AppError } from '../middleware/error.middleware';
import fs from 'fs';
import path from 'path';

export const mergeRouter = Router();

// POST /api/merge/upload - Upload PDF files (even just 1 — cloud files don't need upload)
mergeRouter.post('/upload', (req: Request, res: Response, next: Function) => {
  uploadMiddleware.array('files', 20)(req, res, (err) => {
    if (err) return next(err);
    next();
  });
}, async (req: Request, res: Response, next: Function) => {
  try {
    const files = req.files as Express.Multer.File[];

    if (!files || files.length === 0) {
      throw new AppError('No files provided', 400);
    }

    const uploadedFiles = files.map((file, index) => ({
      id: `file-${Date.now()}-${index}`,
      filePath: path.resolve(file.path),
      fileName: file.originalname,
      fileSize: file.size,
      order: index,
    }));

    res.status(200).json({
      files: uploadedFiles,
      message: `${files.length} file${files.length !== 1 ? 's' : ''} uploaded successfully`,
    });
  } catch (error) {
    next(error);
  }
});

// POST /api/merge - Start a merge job
mergeRouter.post('/', async (req: Request, res: Response, next: Function) => {
  try {
    const { files } = req.body;

    if (!files || !Array.isArray(files) || files.length < 2) {
      throw new AppError('At least 2 files are required to merge', 400);
    }

    // Verify all files exist on the server
    for (const file of files) {
      const absolutePath = path.resolve(file.filePath);
      if (!fs.existsSync(absolutePath)) {
        throw new AppError(`File not found on server: ${file.fileName}`, 400);
      }
    }

    const mergeJob = await mergeService.createMergeJob({
      files: files.map((f: any, index: number) => ({
        filePath: f.filePath,
        fileName: f.fileName,
        fileSize: f.fileSize,
        order: index,
      })),
    });

    // Start processing in background
    mergeService.processMerge(mergeJob.id).catch((err) => {
      console.error('Merge processing error:', err.message);
    });

    res.status(200).json({
      id: mergeJob.id,
      status: 'PROCESSING',
      progress: 0,
      fileCount: mergeJob.fileCount,
      message: 'Merge job started',
    });
  } catch (error) {
    next(error);
  }
});

// GET /api/merge/status/:id - Check merge job status
mergeRouter.get('/status/:id', async (req: Request, res: Response, next: Function) => {
  try {
    const { id } = req.params;
    const mergeJob = await mergeService.getMergeJob(id);

    if (!mergeJob) {
      throw new AppError('Merge job not found', 404);
    }

    const response: Record<string, unknown> = {
      id: mergeJob.id,
      status: mergeJob.status,
      progress: mergeJob.progress,
      fileCount: mergeJob.fileCount,
    };

    if (mergeJob.status === 'COMPLETED' && mergeJob.outputPath) {
      response.downloadUrl = `/api/merge/download/${mergeJob.id}`;
    }

    if (mergeJob.status === 'FAILED' && mergeJob.errorMessage) {
      response.errorMessage = mergeJob.errorMessage;
    }

    res.status(200).json(response);
  } catch (error) {
    next(error);
  }
});

// GET /api/merge/download/:id - Download merged PDF
mergeRouter.get('/download/:id', async (req: Request, res: Response, next: Function) => {
  try {
    const { id } = req.params;
    const mergeJob = await mergeService.getMergeJob(id);

    if (!mergeJob) {
      throw new AppError('Merge job not found', 404);
    }

    if (mergeJob.status !== 'COMPLETED') {
      throw new AppError('Merge not completed yet', 400);
    }

    if (!mergeJob.outputPath) {
      throw new AppError('Output file not available', 404);
    }

    const absolutePath = path.resolve(mergeJob.outputPath);

    if (!fs.existsSync(absolutePath)) {
      throw new AppError('Output file no longer exists', 404);
    }

    const downloadName = `merged-${mergeJob.fileCount}-files.pdf`;

    res.download(absolutePath, downloadName, (err) => {
      if (err) {
        console.error('Download error:', err);
        if (!res.headersSent) {
          next(new AppError('Download failed', 500));
        }
      }
    });
  } catch (error) {
    next(error);
  }
});
</file>

<file path="apps/api/src/routes/split.ts">
import { Router, Request, Response } from 'express';
import { splitService } from '../services/split.service';
import { uploadMiddleware } from '../middleware/upload.middleware';
import { AppError } from '../middleware/error.middleware';
import { fileService } from '../services/file.service';
import fs from 'fs';
import path from 'path';

export const splitRouter = Router();

// POST /api/split/upload - Upload a PDF for splitting
splitRouter.post('/upload', (req: Request, res: Response, next: Function) => {
  uploadMiddleware.single('file')(req, res, (err) => {
    if (err) return next(err);
    next();
  });
}, async (req: Request, res: Response, next: Function) => {
  try {
    if (!req.file) throw new AppError('No file provided', 400);

    const filePath = path.resolve(req.file.path);
    const { PDFDocument } = require('pdf-lib');
    const fileBytes = fs.readFileSync(filePath);
    const pdf = await PDFDocument.load(fileBytes, { ignoreEncryption: true });
    const totalPages = pdf.getPageCount();

    res.status(200).json({
      filePath,
      fileName: req.file.originalname,
      fileSize: req.file.size,
      totalPages,
    });
  } catch (error) { next(error); }
});

// POST /api/split/analyze - Get page count for a file already on server (cloud files)
splitRouter.post('/analyze', async (req: Request, res: Response, next: Function) => {
  try {
    const { filePath } = req.body;
    if (!filePath) throw new AppError('filePath is required', 400);

    const absolutePath = path.resolve(filePath);
    if (!fs.existsSync(absolutePath)) throw new AppError('File not found on server', 404);

    const { PDFDocument } = require('pdf-lib');
    const fileBytes = fs.readFileSync(absolutePath);
    const pdf = await PDFDocument.load(fileBytes, { ignoreEncryption: true });
    const totalPages = pdf.getPageCount();
    const fileSize = fs.statSync(absolutePath).size;

    res.status(200).json({ totalPages, fileSize, filePath: absolutePath });
  } catch (error) { next(error); }
});

// POST /api/split - Start a split job
splitRouter.post('/', async (req: Request, res: Response, next: Function) => {
  try {
    const { originalPath, originalName, fileSize, splitMode, ranges, fixedSize, pagesPerFile } = req.body;
    if (!originalPath || !originalName) throw new AppError('originalPath and originalName are required', 400);
    if (!fs.existsSync(path.resolve(originalPath))) throw new AppError('File not found on server', 400);

    const splitJob = await splitService.createSplitJob({
      originalPath, originalName, fileSize,
      splitMode: splitMode || 'range',
      ranges, fixedSize, pagesPerFile,
    });

    splitService.processSplit(splitJob.id).catch((err) => {
      console.error('Split processing error:', err.message);
    });

    res.status(200).json({
      id: splitJob.id, status: 'PROCESSING', progress: 0,
      totalPages: splitJob.totalPages, splitMode: splitJob.splitMode,
      message: 'Split job started',
    });
  } catch (error) { next(error); }
});

// GET /api/split/status/:id
splitRouter.get('/status/:id', async (req: Request, res: Response, next: Function) => {
  try {
    const { id } = req.params;
    const splitJob = await splitService.getSplitJob(id);
    if (!splitJob) throw new AppError('Split job not found', 404);

    const response: Record<string, unknown> = {
      id: splitJob.id, status: splitJob.status, progress: splitJob.progress,
      totalPages: splitJob.totalPages, splitMode: splitJob.splitMode,
    };

    if (splitJob.status === 'COMPLETED') {
      const outputFiles = await splitService.getSplitOutputFiles(id);
      response.outputFiles = outputFiles;
      response.fileCount = outputFiles.length;
    }
    if (splitJob.status === 'FAILED' && splitJob.errorMessage) response.errorMessage = splitJob.errorMessage;

    res.status(200).json(response);
  } catch (error) { next(error); }
});

// GET /api/split/download/:jobId/:fileIndex
splitRouter.get('/download/:jobId/:fileIndex', async (req: Request, res: Response, next: Function) => {
  try {
    const { jobId, fileIndex } = req.params;
    const outputFiles = await splitService.getSplitOutputFiles(jobId);
    const index = parseInt(fileIndex, 10);
    if (index < 0 || index >= outputFiles.length) throw new AppError('File not found', 404);

    const file = outputFiles[index];
    const absolutePath = path.resolve(file.filePath);
    if (!fs.existsSync(absolutePath)) throw new AppError('File no longer exists', 404);

    res.download(absolutePath, file.fileName, (err) => {
      if (err && !res.headersSent) next(new AppError('Download failed', 500));
    });
  } catch (error) { next(error); }
});

// GET /api/split/download-all/:jobId - Download all as ZIP
splitRouter.get('/download-all/:jobId', async (req: Request, res: Response, next: Function) => {
  try {
    const { jobId } = req.params;
    const outputFiles = await splitService.getSplitOutputFiles(jobId);
    if (outputFiles.length === 0) throw new AppError('No output files found', 404);

    if (outputFiles.length === 1) {
      const file = outputFiles[0];
      const absolutePath = path.resolve(file.filePath);
      if (fs.existsSync(absolutePath)) return res.download(absolutePath, file.fileName);
    }

    const { exec } = require('child_process');
    const { promisify } = require('util');
    const execAsync = promisify(exec);

    const outputDir = fileService.getAbsoluteOutputDir();
    const zipPath = path.join(outputDir, `split-${jobId}.zip`);
    const filePaths = outputFiles.map((f: any) => `"${path.resolve(f.filePath)}"`).join(' ');

    try {
      await execAsync(`zip -j "${zipPath}" ${filePaths}`, { timeout: 60000 });
    } catch {
      try {
        const filesJson = JSON.stringify(outputFiles.map((f: any) => path.resolve(f.filePath)));
        const pythonScript = `
import zipfile, os, json
files = json.loads('${filesJson.replace(/'/g, "\\'")}')
with zipfile.ZipFile("${zipPath}", 'w', zipfile.ZIP_DEFLATED) as zf:
    for fp in files:
        zf.write(fp, os.path.basename(fp))
`;
        await execAsync(`python3 -c '${pythonScript}'`, { timeout: 60000 });
      } catch { throw new AppError('Failed to create ZIP archive', 500); }
    }

    res.download(zipPath, `split-${outputFiles.length}-files.zip`, (err) => {
      setTimeout(() => { try { if (fs.existsSync(zipPath)) fs.unlinkSync(zipPath); } catch {} }, 5000);
      if (err && !res.headersSent) next(new AppError('Download failed', 500));
    });
  } catch (error) { next(error); }
});

// POST /api/split/render-page - Render a PDF page as PNG
splitRouter.post('/render-page', async (req: Request, res: Response, next: Function) => {
  try {
    const { filePath, pageNum } = req.body;
    if (!filePath || !pageNum) throw new AppError('filePath and pageNum are required', 400);

    const absolutePath = path.resolve(filePath);
    if (!fs.existsSync(absolutePath)) throw new AppError('File not found', 404);

    const { exec } = require('child_process');
    const { promisify } = require('util');
    const execAsync = promisify(exec);

    const outputDir = fileService.getAbsoluteOutputDir();
    const outputPrefix = path.join(outputDir, `preview-${Date.now()}`);
    const imagePath = `${outputPrefix}-${pageNum}.png`;
    let success = false;

    try {
      await execAsync(`pdftoppm -png -f ${pageNum} -l ${pageNum} -r 72 -singlefile "${absolutePath}" "${outputPrefix}"`, { timeout: 30000 });
      if (fs.existsSync(imagePath)) success = true;
    } catch {}

    if (!success) {
      try {
        await execAsync(`gs -dNOPAUSE -dBATCH -sDEVICE=png16m -r72 -dFirstPage=${pageNum} -dLastPage=${pageNum} -sOutputFile="${imagePath}" "${absolutePath}"`, { timeout: 30000 });
        if (fs.existsSync(imagePath)) success = true;
      } catch {}
    }

    if (success && fs.existsSync(imagePath)) {
      const imageBuffer = fs.readFileSync(imagePath);
      try { fs.unlinkSync(imagePath); } catch {}
      res.type('image/png').send(imageBuffer);
    } else {
      throw new AppError('Failed to render page', 500);
    }
  } catch (error) { next(error); }
});
</file>

<file path="apps/api/src/routes/upload.ts">
import { Router, Request, Response } from 'express';
import { uploadMiddleware } from '../middleware/upload.middleware';
import { conversionService } from '../services/conversion.service';
import { AppError } from '../middleware/error.middleware';
import fs from 'fs';
import path from 'path';

export const uploadRouter = Router();

// POST /api/upload - Upload a file
uploadRouter.post('/', (req: Request, res: Response, next: Function) => {
  uploadMiddleware.single('file')(req, res, (err) => {
    if (err) {
      return next(err);
    }
    next();
  });
}, async (req: Request, res: Response, next: Function) => {
  try {
    if (!req.file) {
      throw new AppError('No file provided', 400);
    }

    const outputFormat = req.body.outputFormat || 'docx';

    const conversion = await conversionService.createConversion({
      originalName: req.file.originalname,
      originalSize: req.file.size,
      originalPath: req.file.path,
      outputFormat,
    });

    res.status(200).json({
      id: conversion.id,
      originalName: conversion.originalName,
      originalSize: conversion.originalSize,
      status: conversion.status,
      outputFormat: conversion.outputFormat,
    });
  } catch (error) {
    next(error);
  }
});

// POST /api/upload/convert-from-path - Create conversion from file already on server
uploadRouter.post('/convert-from-path', async (req: Request, res: Response, next: Function) => {
  try {
    const { filePath, fileName, fileSize, outputFormat } = req.body;

    if (!filePath || !fileName) {
      throw new AppError('filePath and fileName are required', 400);
    }

    const absolutePath = path.resolve(filePath);
    if (!fs.existsSync(absolutePath)) {
      throw new AppError('File not found on server', 404);
    }

    const actualSize = fileSize || fs.statSync(absolutePath).size;

    const conversion = await conversionService.createConversion({
      originalName: fileName,
      originalSize: actualSize,
      originalPath: absolutePath,
      outputFormat: outputFormat || 'docx',
    });

    res.status(200).json({
      id: conversion.id,
      originalName: conversion.originalName,
      originalSize: conversion.originalSize,
      status: conversion.status,
      outputFormat: conversion.outputFormat,
    });
  } catch (error) {
    next(error);
  }
});
</file>

<file path="apps/api/src/services/conversion.service.ts">
import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

interface CreateConversionInput {
  originalName: string;
  originalSize: number;
  originalPath: string;
  userId?: string;
  outputFormat?: string;
}

interface UpdateConversionInput {
  status?: string;
  progress?: number;
  outputPath?: string;
  errorMessage?: string;
}

class ConversionService {
  async createConversion(input: CreateConversionInput) {
    return prisma.conversion.create({
      data: {
        originalName: input.originalName,
        originalSize: input.originalSize,
        originalPath: input.originalPath,
        userId: input.userId || null,
        outputFormat: input.outputFormat || 'docx',
        status: 'PENDING',
        progress: 0,
      },
    });
  }

  async getConversion(id: string) {
    return prisma.conversion.findUnique({
      where: { id },
    });
  }

  async updateConversion(id: string, input: UpdateConversionInput) {
    const data: Record<string, unknown> = {};
    
    if (input.status !== undefined) data.status = input.status;
    if (input.progress !== undefined) data.progress = input.progress;
    if (input.outputPath !== undefined) data.outputPath = input.outputPath;
    if (input.errorMessage !== undefined) data.errorMessage = input.errorMessage;

    return prisma.conversion.update({
      where: { id },
      data,
    });
  }

  async deleteConversion(id: string) {
    return prisma.conversion.delete({
      where: { id },
    });
  }
}

export const conversionService = new ConversionService();
</file>

<file path="apps/api/src/services/file.service.ts">
import fs from 'fs';
import path from 'path';
import { config } from '../config';

class FileService {
  private uploadDir: string;
  private outputDir: string;

  constructor() {
    this.uploadDir = path.resolve(config.uploadDir);
    this.outputDir = path.resolve(config.outputDir);
    this.ensureDirectories();
  }

  private ensureDirectories() {
    const dirs = [this.uploadDir, this.outputDir];
    dirs.forEach((dir) => {
      if (!fs.existsSync(dir)) {
        fs.mkdirSync(dir, { recursive: true });
      }
    });
  }

  async deleteFile(filePath: string): Promise<void> {
    try {
      if (fs.existsSync(filePath)) {
        fs.unlinkSync(filePath);
      }
    } catch (error) {
      console.error(`Failed to delete file: ${filePath}`, error);
    }
  }

  async fileExists(filePath: string): Promise<boolean> {
    return fs.existsSync(filePath);
  }

  getFileExtension(filename: string): string {
    return path.extname(filename).toLowerCase();
  }

  generateOutputPath(originalPath: string, outputFormat: string = 'docx'): string {
    const basename = path.basename(originalPath, path.extname(originalPath));
    return path.join(this.outputDir, `${basename}.${outputFormat}`);
  }

  generateUniqueFilename(originalName: string): string {
    const ext = path.extname(originalName);
    const name = path.basename(originalName, ext);
    const timestamp = Date.now();
    const random = Math.random().toString(36).substring(2, 8);
    return `${name}-${timestamp}-${random}${ext}`;
  }

  getAbsoluteUploadDir(): string {
    return this.uploadDir;
  }

  getAbsoluteOutputDir(): string {
    return this.outputDir;
  }

  resolveUploadPath(filename: string): string {
    return path.resolve(this.uploadDir, filename);
  }

  resolveOutputPath(filename: string): string {
    return path.resolve(this.outputDir, filename);
  }
}

export const fileService = new FileService();
</file>

<file path="apps/api/src/services/merge.service.ts">
import { PrismaClient } from '@prisma/client';
import fs from 'fs';
import path from 'path';
import { PDFDocument } from 'pdf-lib';
import { exec } from 'child_process';
import { promisify } from 'util';
import { fileService } from './file.service';

const prisma = new PrismaClient();
const execAsync = promisify(exec);

interface CreateMergeJobInput {
  files: Array<{
    filePath: string;
    fileName: string;
    fileSize: number;
    order: number;
  }>;
}

class MergeService {
  async createMergeJob(input: CreateMergeJobInput) {
    const mergeJob = await prisma.mergeJob.create({
      data: {
        status: 'PENDING',
        progress: 0,
        fileCount: input.files.length,
        files: {
          create: input.files.map((f) => ({
            filePath: f.filePath,
            fileName: f.fileName,
            fileSize: f.fileSize,
            order: f.order,
          })),
        },
      },
      include: { files: true },
    });

    return mergeJob;
  }

  async getMergeJob(id: string) {
    return prisma.mergeJob.findUnique({
      where: { id },
      include: { files: { orderBy: { order: 'asc' } } },
    });
  }

  async updateMergeJob(
    id: string,
    data: {
      status?: string;
      progress?: number;
      outputPath?: string;
      errorMessage?: string;
    }
  ) {
    const updateData: Record<string, unknown> = {};
    if (data.status !== undefined) updateData.status = data.status;
    if (data.progress !== undefined) updateData.progress = data.progress;
    if (data.outputPath !== undefined) updateData.outputPath = data.outputPath;
    if (data.errorMessage !== undefined) updateData.errorMessage = data.errorMessage;

    return prisma.mergeJob.update({
      where: { id },
      data: updateData,
    });
  }

  async deleteMergeJob(id: string) {
    return prisma.mergeJob.delete({
      where: { id },
    });
  }

  // Check if a tool is available on the system
  private async hasTool(toolName: string): Promise<boolean> {
    try {
      await execAsync(`which ${toolName}`);
      return true;
    } catch {
      return false;
    }
  }

  async processMerge(mergeJobId: string) {
    const mergeJob = await this.getMergeJob(mergeJobId);
    if (!mergeJob) {
      throw new Error('Merge job not found');
    }

    const outputDir = fileService.getAbsoluteOutputDir();
    const outputPath = path.join(outputDir, `merged-${Date.now()}.pdf`);

    try {
      await this.updateMergeJob(mergeJobId, {
        status: 'PROCESSING',
        progress: 10,
      });

      // Verify all input files exist and are valid PDFs
      const validFiles: string[] = [];
      for (let i = 0; i < mergeJob.files.length; i++) {
        const file = mergeJob.files[i];
        const absolutePath = path.resolve(file.filePath);

        if (!fs.existsSync(absolutePath)) {
          throw new Error(`File not found: ${file.fileName}`);
        }

        // Validate PDF header
        const header = Buffer.alloc(5);
        const fd = fs.openSync(absolutePath, 'r');
        fs.readSync(fd, header, 0, 5, 0);
        fs.closeSync(fd);

        if (header.toString('ascii') !== '%PDF-') {
          throw new Error(`Invalid PDF file: ${file.fileName}`);
        }

        validFiles.push(absolutePath);
      }

      await this.updateMergeJob(mergeJobId, { progress: 20 });

      // Try merging methods in order of reliability

      // METHOD 1: pdftk (most reliable for complex PDFs)
      let mergeSucceeded = false;

      if (!mergeSucceeded) {
        const hasPdftk = await this.hasTool('pdftk');
        if (hasPdftk) {
          console.log('   Trying pdftk merge...');
          try {
            const inputArgs = validFiles.map((f) => `"${f}"`).join(' ');
            const command = `pdftk ${inputArgs} cat output "${outputPath}"`;
            console.log(`   Command: ${command}`);
            await execAsync(command, { timeout: 120000 });

            if (fs.existsSync(outputPath) && fs.statSync(outputPath).size > 0) {
              mergeSucceeded = true;
              console.log('   ✅ pdftk merge succeeded');
            }
          } catch (err: any) {
            console.warn(`   ⚠️ pdftk failed: ${err.message}`);
            // Clean up partial output
            if (fs.existsSync(outputPath)) fs.unlinkSync(outputPath);
          }
        }
      }

      // METHOD 2: Ghostscript (very reliable, preserves formatting)
      if (!mergeSucceeded) {
        const hasGs = await this.hasTool('gs');
        if (hasGs) {
          console.log('   Trying Ghostscript merge...');
          try {
            const inputArgs = validFiles.map((f) => `"${f}"`).join(' ');
            const command = `gs -dNOPAUSE -dBATCH -sDEVICE=pdfwrite -sOutputFile="${outputPath}" ${inputArgs}`;
            console.log(`   Command: ${command}`);
            await execAsync(command, { timeout: 120000 });

            if (fs.existsSync(outputPath) && fs.statSync(outputPath).size > 0) {
              mergeSucceeded = true;
              console.log('   ✅ Ghostscript merge succeeded');
            }
          } catch (err: any) {
            console.warn(`   ⚠️ Ghostscript failed: ${err.message}`);
            if (fs.existsSync(outputPath)) fs.unlinkSync(outputPath);
          }
        }
      }

      // METHOD 3: pdf-lib (JavaScript fallback — may produce empty pages for some PDFs)
      if (!mergeSucceeded) {
        console.log('   Trying pdf-lib merge...');
        try {
          const mergedPdf = await PDFDocument.create();
          const totalFiles = validFiles.length;

          for (let i = 0; i < totalFiles; i++) {
            const filePath = validFiles[i];
            const fileBytes = fs.readFileSync(filePath);

            let donorPdf: PDFDocument;
            try {
              donorPdf = await PDFDocument.load(fileBytes, {
                ignoreEncryption: true,
                updateMetadata: false,
              });
            } catch (loadErr: any) {
              throw new Error(`Failed to load PDF "${path.basename(filePath)}": ${loadErr.message}`);
            }

            const pageCount = donorPdf.getPageCount();
            console.log(`   File ${i + 1}: ${path.basename(filePath)} — ${pageCount} pages`);

            const pages = await mergedPdf.copyPages(
              donorPdf,
              donorPdf.getPageIndices()
            );
            pages.forEach((page) => mergedPdf.addPage(page));

            const prog = 20 + Math.round(((i + 1) / totalFiles) * 60);
            await this.updateMergeJob(mergeJobId, { progress: prog });
          }

          const mergedBytes = await mergedPdf.save();
          fs.writeFileSync(outputPath, mergedBytes);

          if (fs.existsSync(outputPath) && fs.statSync(outputPath).size > 0) {
            mergeSucceeded = true;
            console.log('   ✅ pdf-lib merge succeeded');
          }
        } catch (err: any) {
          console.warn(`   ⚠️ pdf-lib failed: ${err.message}`);
          if (fs.existsSync(outputPath)) fs.unlinkSync(outputPath);
        }
      }

      // METHOD 4: Python PyPDF2/pypdf fallback
      if (!mergeSucceeded) {
        console.log('   Trying Python pypdf merge...');
        try {
          const projectRoot = path.resolve(process.cwd(), '..', '..');
          const venvPython = path.join(projectRoot, '.venv', 'bin', 'python3');
          const pythonCmd = fs.existsSync(venvPython) ? venvPython : 'python3';

          // Install pypdf if needed
          try {
            await execAsync(`${pythonCmd} -c "import pypdf"`, { timeout: 30000 });
          } catch {
            console.log('   Installing pypdf...');
            await execAsync(`${pythonCmd} -m pip install pypdf -q`, { timeout: 60000 });
          }

          const inputArgs = validFiles.map((f) => `"${f}"`).join(',');
          const script = `
import sys
from pypdf import PdfWriter, PdfReader

writer = PdfWriter()
files = [${inputArgs}]
total = len(files)
for i, f in enumerate(files):
    reader = PdfReader(f)
    for page in reader.pages:
        writer.add_page(page)
    print(f"  File {i+1}/{total}: {len(reader.pages)} pages")

with open("${outputPath}", "wb") as out:
    writer.write(out)
print("SUCCESS")
`;
          const { stdout, stderr } = await execAsync(`${pythonCmd} -c '${script.replace(/'/g, "'\\''")}'`, { timeout: 180000 });
          console.log(`   Python stdout: ${stdout.trim()}`);
          if (stderr) console.log(`   Python stderr: ${stderr.trim()}`);

          if (fs.existsSync(outputPath) && fs.statSync(outputPath).size > 0) {
            mergeSucceeded = true;
            console.log('   ✅ Python pypdf merge succeeded');
          }
        } catch (err: any) {
          console.warn(`   ⚠️ Python pypdf failed: ${err.message}`);
          if (fs.existsSync(outputPath)) fs.unlinkSync(outputPath);
        }
      }

      if (!mergeSucceeded) {
        throw new Error('All merge methods failed. Please install pdftk or ghostscript for reliable PDF merging.');
      }

      const fileStats = fs.statSync(outputPath);
      console.log(`   ✅ Final output: ${outputPath} (${(fileStats.size / 1024).toFixed(1)} KB)`);

      await this.updateMergeJob(mergeJobId, {
        status: 'COMPLETED',
        progress: 100,
        outputPath,
      });

      return { success: true, outputPath };
    } catch (error: any) {
      console.error(`   ❌ Merge failed: ${error.message}`);
      await this.updateMergeJob(mergeJobId, {
        status: 'FAILED',
        errorMessage: error.message || 'Merge failed',
        progress: 0,
      });
      throw error;
    }
  }
}

export const mergeService = new MergeService();
</file>

<file path="apps/api/src/services/split.service.ts">
import { PrismaClient } from '@prisma/client';
import fs from 'fs';
import path from 'path';
import { PDFDocument } from 'pdf-lib';
import { fileService } from './file.service';
import { exec } from 'child_process';
import { promisify } from 'util';

const prisma = new PrismaClient();
const execAsync = promisify(exec);

interface SplitRange {
  from: number;
  to: number;
}

interface CreateSplitJobInput {
  originalPath: string;
  originalName: string;
  fileSize: number;
  splitMode: 'range' | 'pages' | 'size';
  ranges?: SplitRange[];
  fixedSize?: number;
  sizeLimit?: number;
  pagesPerFile?: number;
}

class SplitService {
  async createSplitJob(input: CreateSplitJobInput) {
    // Get total page count
    const absolutePath = path.resolve(input.originalPath);
    const fileBytes = fs.readFileSync(absolutePath);
    const pdf = await PDFDocument.load(fileBytes, { ignoreEncryption: true });
    const totalPages = pdf.getPageCount();

    const splitJob = await prisma.splitJob.create({
      data: {
        originalPath: input.originalPath,
        originalName: input.originalName,
        fileSize: input.fileSize,
        totalPages,
        splitMode: input.splitMode,
        ranges: input.ranges ? JSON.stringify(input.ranges) : null,
        fixedSize: input.fixedSize || null,
        sizeLimit: input.sizeLimit || null,
        pagesPerFile: input.pagesPerFile || null,
        status: 'PENDING',
        progress: 0,
      },
    });

    return splitJob;
  }

  async getSplitJob(id: string) {
    return prisma.splitJob.findUnique({ where: { id } });
  }

  async updateSplitJob(
    id: string,
    data: {
      status?: string;
      progress?: number;
      outputFiles?: string;
      errorMessage?: string;
    }
  ) {
    const updateData: Record<string, unknown> = {};
    if (data.status !== undefined) updateData.status = data.status;
    if (data.progress !== undefined) updateData.progress = data.progress;
    if (data.outputFiles !== undefined) updateData.outputFiles = data.outputFiles;
    if (data.errorMessage !== undefined) updateData.errorMessage = data.errorMessage;

    return prisma.splitJob.update({ where: { id }, data: updateData });
  }

  async processSplit(splitJobId: string) {
    const splitJob = await this.getSplitJob(splitJobId);
    if (!splitJob) throw new Error('Split job not found');

    const outputDir = fileService.getAbsoluteOutputDir();
    const absolutePath = path.resolve(splitJob.originalPath);

    try {
      await this.updateSplitJob(splitJobId, { status: 'PROCESSING', progress: 5 });

      if (!fs.existsSync(absolutePath)) {
        throw new Error(`File not found: ${splitJob.originalName}`);
      }

      const fileBytes = fs.readFileSync(absolutePath);
      const sourcePdf = await PDFDocument.load(fileBytes, { ignoreEncryption: true });
      const totalPages = sourcePdf.getPageCount();
      const baseName = path.basename(splitJob.originalName, path.extname(splitJob.originalName));

      let splitRanges: SplitRange[] = [];

      if (splitJob.splitMode === 'range') {
        // Custom ranges or fixed size
        if (splitJob.ranges) {
          splitRanges = JSON.parse(splitJob.ranges as string);
        } else if (splitJob.fixedSize) {
          // Generate fixed-size ranges
          const size = splitJob.fixedSize;
          for (let i = 1; i <= totalPages; i += size) {
            splitRanges.push({
              from: i,
              to: Math.min(i + size - 1, totalPages),
            });
          }
        }
      } else if (splitJob.splitMode === 'pages') {
        // Split every N pages
        const pagesPerFile = splitJob.pagesPerFile || 1;
        for (let i = 1; i <= totalPages; i += pagesPerFile) {
          splitRanges.push({
            from: i,
            to: Math.min(i + pagesPerFile - 1, totalPages),
          });
        }
      }
      // size mode will be added later

      if (splitRanges.length === 0) {
        throw new Error('No split ranges specified');
      }

      console.log(`✂️  Splitting "${splitJob.originalName}" (${totalPages} pages) into ${splitRanges.length} files`);

      const outputFiles: Array<{ fileName: string; filePath: string; pageCount: number; size: number }> = [];

      for (let i = 0; i < splitRanges.length; i++) {
        const range = splitRanges[i];
        const fromIndex = Math.max(0, range.from - 1);
        const toIndex = Math.min(totalPages - 1, range.to - 1);

        const newPdf = await PDFDocument.create();
        
        // Copy pages from source to new PDF
        const pageIndices = [];
        for (let p = fromIndex; p <= toIndex; p++) {
          pageIndices.push(p);
        }

        if (pageIndices.length > 0) {
          const copiedPages = await newPdf.copyPages(sourcePdf, pageIndices);
          copiedPages.forEach((page) => newPdf.addPage(page));
        }

        const pdfBytes = await newPdf.save();

        let fileName: string;
        if (range.from === range.to) {
          fileName = `${baseName}_page_${range.from}.pdf`;
        } else {
          fileName = `${baseName}_pages_${range.from}-${range.to}.pdf`;
        }

        const outputPath = path.join(outputDir, `split-${Date.now()}-${i + 1}-${fileName}`);
        fs.writeFileSync(outputPath, pdfBytes);

        const fileStats = fs.statSync(outputPath);
        outputFiles.push({
          fileName,
          filePath: outputPath,
          pageCount: pageIndices.length,
          size: fileStats.size,
        });

        const progress = 10 + Math.round(((i + 1) / splitRanges.length) * 80);
        await this.updateSplitJob(splitJobId, { progress });

        console.log(`   ✅ ${fileName} (${pageIndices.length} pages, ${(fileStats.size / 1024).toFixed(1)} KB)`);
      }

      await this.updateSplitJob(splitJobId, {
        status: 'COMPLETED',
        progress: 100,
        outputFiles: JSON.stringify(outputFiles),
      });

      console.log(`   ✅ Split complete: ${outputFiles.length} files created`);
      return { success: true, outputFiles };
    } catch (error: any) {
      console.error(`   ❌ Split failed: ${error.message}`);
      await this.updateSplitJob(splitJobId, {
        status: 'FAILED',
        errorMessage: error.message || 'Split failed',
        progress: 0,
      });
      throw error;
    }
  }

  async getSplitOutputFiles(id: string) {
    const splitJob = await this.getSplitJob(id);
    if (!splitJob || !splitJob.outputFiles) return [];

    try {
      return JSON.parse(splitJob.outputFiles as string);
    } catch {
      return [];
    }
  }
}

export const splitService = new SplitService();
</file>

<file path="apps/api/src/index.ts">
import express from 'express';
import cors from 'cors';
import { config } from './config';
import { uploadRouter } from './routes/upload';
import { convertRouter } from './routes/convert';
import { downloadRouter } from './routes/download';
import { mergeRouter } from './routes/merge';
import { splitRouter } from './routes/split';
import { cloudRouter } from './routes/cloud';
import { dropboxAuthRouter } from './routes/dropbox-auth';
import { errorHandler } from './middleware/error.middleware';

const app = express();

app.use(cors({ origin: config.corsOrigin, credentials: true }));
app.use(express.json());
app.use(express.urlencoded({ extended: true }));

app.get('/api/health', (_req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});

app.use('/api/upload', uploadRouter);
app.use('/api/convert', convertRouter);
app.use('/api/download', downloadRouter);
app.use('/api/merge', mergeRouter);
app.use('/api/split', splitRouter);
app.use('/api/cloud', cloudRouter);
app.use('/api/dropbox-auth', dropboxAuthRouter);

app.use(errorHandler);

const server = app.listen(config.port, () => {
  console.log(`🚀 API Server running on port ${config.port}`);
  console.log(`📍 Environment: ${config.nodeEnv}`);
  console.log(`🔗 Health check: http://localhost:${config.port}/api/health`);
});

process.on('SIGTERM', async () => {
  console.log('SIGTERM received. Shutting down gracefully...');
  server.close();
  const { PrismaClient } = require('@prisma/client');
  await new PrismaClient().$disconnect();
  process.exit(0);
});

process.on('SIGINT', async () => {
  console.log('SIGINT received. Shutting down gracefully...');
  server.close();
  const { PrismaClient } = require('@prisma/client');
  await new PrismaClient().$disconnect();
  process.exit(0);
});

export default app;
</file>

<file path="apps/api/jest.config.js">
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  roots: ['<rootDir>/src'],
  testMatch: ['**/__tests__/**/*.test.ts'],
  moduleFileExtensions: ['ts', 'tsx', 'js', 'jsx', 'json'],
  collectCoverageFrom: [
    'src/**/*.ts',
    '!src/**/*.d.ts',
    '!src/index.ts',
    '!src/**/__tests__/**',
  ],
  coverageThreshold: {
    global: {
      branches: 50,
      functions: 50,
      lines: 50,
      statements: 50,
    },
  },
};
</file>

<file path="apps/api/package.json">
{
  "name": "@pdf-converter/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "jest --coverage",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@pdf-converter/shared": "*",
    "@prisma/client": "^5.10.0",
    "bullmq": "^5.5.0",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.12",
    "@types/multer": "^1.4.11",
    "@types/node": "^20.11.0",
    "@types/uuid": "^9.0.7",
    "jest": "^29.7.0",
    "prisma": "^5.10.0",
    "supertest": "^6.3.4",
    "ts-jest": "^29.1.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
</file>

<file path="apps/api/tsconfig.json">
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "**/__tests__/**"]
}
</file>

<file path="apps/web/src/app/dropbox-callback/page.tsx">
'use client';

import { useEffect, useState, Suspense } from 'react';
import { useSearchParams } from 'next/navigation';
import DropboxFilePicker from '@/components/DropboxFilePicker';

function DropboxCallbackContent() {
  const searchParams = useSearchParams();
  const [accessToken, setAccessToken] = useState<string | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [mode, setMode] = useState<'single' | 'multi'>('single');

  useEffect(() => {
    const token = searchParams.get('access_token');
    const err = searchParams.get('error');
    const m = searchParams.get('mode') || 'single';

    if (err) {
      setError(err);
    } else if (token) {
      setAccessToken(token);
      setMode(m as 'single' | 'multi');
    } else {
      setError('No access token received');
    }
  }, [searchParams]);

  const handleFileSelect = (file: any) => {
    if (window.opener) {
      window.opener.postMessage(
        {
          type: 'dropbox-file-selected',
          files: [{ ...file, accessToken }],
          mode: 'single',
        },
        '*'
      );
      window.close();
    }
  };

  const handleFilesSelect = (files: any[]) => {
    if (window.opener) {
      window.opener.postMessage(
        {
          type: 'dropbox-file-selected',
          files: files.map((f) => ({ ...f, accessToken })),
          mode: 'multi',
        },
        '*'
      );
      window.close();
    }
  };

  if (error) {
    return (
      <div className="min-h-screen flex items-center justify-center bg-slate-50">
        <div className="bg-white rounded-xl p-8 shadow-sm border border-red-200 text-center max-w-sm">
          <div className="w-12 h-12 mx-auto mb-4 rounded-full bg-red-50 flex items-center justify-center">
            <svg className="w-6 h-6 text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
              <path strokeLinecap="round" strokeLinejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </div>
          <h2 className="text-lg font-bold text-slate-800 mb-2">Dropbox Error</h2>
          <p className="text-slate-500 text-sm">{error}</p>
          <button onClick={() => window.close()} className="mt-4 px-4 py-2 text-sm font-medium text-slate-600 hover:text-slate-800">Close</button>
        </div>
      </div>
    );
  }

  if (!accessToken) {
    return (
      <div className="min-h-screen flex items-center justify-center bg-slate-50">
        <div className="animate-spin w-8 h-8 border-4 border-blue-400 border-t-transparent rounded-full" />
      </div>
    );
  }

  return (
    <div className="min-h-screen flex items-center justify-center bg-slate-50 p-4">
      <DropboxFilePicker
        accessToken={accessToken}
        onFileSelect={handleFileSelect}
        onFilesSelect={mode === 'multi' ? handleFilesSelect : undefined}
        multiSelect={mode === 'multi'}
        accentColor="#0061FF"
      />
    </div>
  );
}

export default function DropboxCallbackPage() {
  return (
    <Suspense fallback={
      <div className="min-h-screen flex items-center justify-center bg-slate-50">
        <div className="animate-spin w-8 h-8 border-4 border-blue-400 border-t-transparent rounded-full" />
      </div>
    }>
      <DropboxCallbackContent />
    </Suspense>
  );
}
</file>

<file path="apps/web/src/app/merge-pdf/page.tsx">
'use client';

import { useState, useCallback, useRef } from 'react';
import Header from '@/components/Header';
import Footer from '@/components/Footer';
import CloudStorageButtons from '@/components/CloudStorageButtons';
import { useMerge, MergeFile } from '@/hooks/useMerge';
import { apiClient } from '@/lib/api';

const ACCENT_COLOR = '#E67E22';

export default function MergePdfPage() {
  const { files, status, progress, mergeJobId, error, isProcessing, addFiles, addCloudFiles, removeFile, reorderFiles, startMerge, reset } = useMerge();

  const [dragOverIndex, setDragOverIndex] = useState<number | null>(null);
  const [localDragOver, setLocalDragOver] = useState(false);
  const fileInputRef = useRef<HTMLInputElement>(null);
  const dragItemRef = useRef<number | null>(null);

  const handleDragOver = useCallback((e: React.DragEvent) => { e.preventDefault(); setLocalDragOver(true); }, []);
  const handleDragLeave = useCallback((e: React.DragEvent) => { e.preventDefault(); setLocalDragOver(false); }, []);
  const handleDrop = useCallback((e: React.DragEvent) => { e.preventDefault(); setLocalDragOver(false); const dropped = Array.from(e.dataTransfer.files).filter(f => f.type === 'application/pdf'); if (dropped.length > 0) addFiles(dropped); }, [addFiles]);
  const handleFileInput = useCallback((e: React.ChangeEvent<HTMLInputElement>) => { const selected = Array.from(e.target.files || []); if (selected.length > 0) addFiles(selected); e.target.value = ''; }, [addFiles]);

  const handleItemDragStart = (index: number) => { dragItemRef.current = index; };
  const handleItemDragOver = (e: React.DragEvent, index: number) => { e.preventDefault(); setDragOverIndex(index); };
  const handleItemDrop = (e: React.DragEvent, toIndex: number) => { e.preventDefault(); setDragOverIndex(null); const fromIndex = dragItemRef.current; if (fromIndex !== null && fromIndex !== toIndex) reorderFiles(fromIndex, toIndex); dragItemRef.current = null; };
  const handleItemDragEnd = () => { setDragOverIndex(null); dragItemRef.current = null; };

  const formatFileSize = (bytes: number): string => { if (bytes === 0) return '0 B'; const k = 1024; const sizes = ['B', 'KB', 'MB', 'GB']; const i = Math.floor(Math.log(bytes) / Math.log(k)); return parseFloat((bytes / Math.pow(k, i)).toFixed(1)) + ' ' + sizes[i]; };

  const handleDownload = () => {
    if (mergeJobId) {
      const url = apiClient.getMergeDownloadUrl(mergeJobId);
      const link = document.createElement('a');
      link.href = url; link.download = `merged-${files.length}-files.pdf`;
      document.body.appendChild(link); link.click(); document.body.removeChild(link);
    }
  };

  const handleCloudFilesReady = useCallback((cloudFiles: Array<{ filePath: string; fileName: string; fileSize: number }>) => { addCloudFiles(cloudFiles); }, [addCloudFiles]);

  const showUpload = files.length === 0 && !isProcessing && status !== 'COMPLETED' && status !== 'FAILED';

  return (
    <>
      <Header />
      <main className="flex-1 py-6 md:py-8">
        <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
          {showUpload && (
            <div className="text-center mb-8 pt-4">
              <div className="inline-flex items-center gap-3 mb-3">
                <div className="w-14 h-14 rounded-xl flex items-center justify-center" style={{ backgroundColor: `${ACCENT_COLOR}15` }}>
                  <svg className="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke={ACCENT_COLOR} strokeWidth={1.5}>
                    <path strokeLinecap="round" strokeLinejoin="round" d="M8 4H5a2 2 0 00-2 2v3m18 0V6a2 2 0 00-2-2h-3m0 14h3a2 2 0 002-2v-3M3 16v3a2 2 0 002 2h3" />
                    <path strokeLinecap="round" strokeLinejoin="round" d="M9 12h6M12 9v6" />
                  </svg>
                </div>
              </div>
              <h1 className="text-3xl font-bold text-slate-800 mb-2">Merge PDF</h1>
              <p className="text-slate-500 max-w-md mx-auto text-sm">Combine PDFs in the order you want with the easiest PDF merger available.</p>
            </div>
          )}

          {/* Processing */}
          {isProcessing && (
            <div className="max-w-xl mx-auto bg-white rounded-2xl shadow-sm border border-slate-200 p-8 text-center">
              <div className="w-16 h-16 mx-auto mb-4 rounded-full flex items-center justify-center" style={{ backgroundColor: `${ACCENT_COLOR}15` }}>
                <svg className="w-8 h-8 animate-spin" style={{ color: ACCENT_COLOR }} fill="none" viewBox="0 0 24 24">
                  <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
                  <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z" />
                </svg>
              </div>
              <h3 className="text-xl font-bold text-slate-800 mb-2">{status === 'UPLOADING' ? 'Uploading files...' : 'Merging PDFs...'}</h3>
              <p className="text-slate-500 mb-4">Merging {files.length} PDF files</p>
              <div className="w-full bg-slate-100 rounded-full h-2.5 overflow-hidden">
                <div className="h-full rounded-full transition-all duration-500" style={{ width: `${progress}%`, backgroundColor: ACCENT_COLOR }} />
              </div>
            </div>
          )}

          {/* Completed */}
          {status === 'COMPLETED' && (
            <div className="max-w-xl mx-auto bg-white rounded-2xl shadow-sm border border-slate-200 p-8 text-center">
              <div className="w-16 h-16 mx-auto mb-4 rounded-full bg-emerald-50 flex items-center justify-center">
                <svg className="w-8 h-8 text-emerald-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
              </div>
              <h3 className="text-xl font-bold text-slate-800 mb-2">Merge Complete!</h3>
              <p className="text-slate-500 mb-6">{files.length} PDFs merged.</p>
              <button onClick={handleDownload} className="px-8 py-3.5 text-white font-semibold rounded-xl text-base shadow-lg hover:shadow-xl transition-all" style={{ backgroundColor: ACCENT_COLOR }}>
                <span className="flex items-center gap-2 justify-center">
                  <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" /></svg>
                  Download Merged PDF
                </span>
              </button>
              <button onClick={reset} className="block mx-auto text-slate-500 hover:text-slate-700 text-sm font-medium mt-3">Merge more files</button>
            </div>
          )}

          {/* Failed */}
          {status === 'FAILED' && (
            <div className="max-w-xl mx-auto bg-white rounded-2xl shadow-sm border border-red-200 p-8 text-center">
              <div className="w-16 h-16 mx-auto mb-4 rounded-full bg-red-50 flex items-center justify-center">
                <svg className="w-8 h-8 text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
              </div>
              <h3 className="text-xl font-bold text-slate-800 mb-2">Merge Failed</h3>
              <p className="text-slate-500 mb-6">{error || 'An unexpected error occurred.'}</p>
              <button onClick={reset} className="px-6 py-2.5 text-white font-semibold rounded-xl text-sm shadow-lg transition-all" style={{ backgroundColor: ACCENT_COLOR }}>Try Again</button>
            </div>
          )}

          {/* File grid */}
          {files.length > 0 && !isProcessing && status !== 'COMPLETED' && status !== 'FAILED' && (
            <>
              <div className="flex items-center justify-between mb-4">
                <h3 className="text-sm font-semibold text-slate-600 uppercase tracking-wider">{files.length} file{files.length !== 1 ? 's' : ''} selected</h3>
                <span className="text-xs text-slate-400">Drag to reorder</span>
              </div>
              <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">
                {files.map((file, index) => (
                  <div key={file.id} draggable onDragStart={() => handleItemDragStart(index)} onDragOver={(e) => handleItemDragOver(e, index)} onDrop={(e) => handleItemDrop(e, index)} onDragEnd={handleItemDragEnd}
                    className={`relative bg-white rounded-xl border-2 p-3 cursor-grab active:cursor-grabbing transition-all group ${dragOverIndex === index ? 'border-orange-400 bg-orange-50 shadow-lg scale-105' : 'border-slate-200 hover:border-slate-300 hover:shadow-md'}`}>
                    <div className="absolute -top-2 -left-2 w-6 h-6 rounded-full flex items-center justify-center text-white text-xs font-bold shadow-sm z-10" style={{ backgroundColor: ACCENT_COLOR }}>{index + 1}</div>
                    <button onClick={() => removeFile(file.id)} className="absolute -top-2 -right-2 w-6 h-6 rounded-full bg-red-500 text-white flex items-center justify-center text-xs opacity-0 group-hover:opacity-100 transition-opacity hover:bg-red-600 z-10 shadow-sm">✕</button>
                    <div className="w-full aspect-[3/4] rounded-lg flex flex-col items-center justify-center mb-2" style={{ backgroundColor: `${ACCENT_COLOR}08` }}>
                      <svg className="w-10 h-10 mb-1" viewBox="0 0 24 24" fill="none" stroke={ACCENT_COLOR} strokeWidth={1}><path strokeLinecap="round" strokeLinejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" /></svg>
                      <span className="text-xs font-bold" style={{ color: ACCENT_COLOR }}>PDF</span>
                      {file.isCloud && <span className="text-[10px] text-blue-500 mt-0.5">☁️</span>}
                    </div>
                    <p className="text-xs font-medium text-slate-700 truncate">{file.fileName}</p>
                    <p className="text-xs text-slate-400">{formatFileSize(file.fileSize)}</p>
                  </div>
                ))}
                <div onClick={() => fileInputRef.current?.click()} className="border-2 border-dashed border-slate-300 rounded-xl p-4 flex flex-col items-center justify-center cursor-pointer hover:border-slate-400 hover:bg-slate-50 transition-all min-h-[160px]">
                  <svg className="w-8 h-8 text-slate-400 mb-2" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5}><path strokeLinecap="round" strokeLinejoin="round" d="M12 4v16m8-8H4" /></svg>
                  <span className="text-sm text-slate-500 font-medium">Add more</span>
                </div>
              </div>

              <div className="mt-4 flex justify-center">
                <CloudStorageButtons onFilesReady={handleCloudFilesReady} multiSelect={true} size="sm" />
              </div>

              {files.length >= 2 && (
                <div className="mt-6 text-center">
                  <button onClick={startMerge} className="px-12 py-4 text-white font-bold rounded-xl text-lg shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 transition-all" style={{ backgroundColor: ACCENT_COLOR }}>
                    <span className="flex items-center gap-2 justify-center">
                      <svg className="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M8 4H5a2 2 0 00-2 2v3m18 0V6a2 2 0 00-2-2h-3m0 14h3a2 2 0 002-2v-3M3 16v3a2 2 0 002 2h3" /></svg>
                      Merge PDF
                    </span>
                  </button>
                  <p className="text-slate-400 text-xs mt-2">{files.length} files • {formatFileSize(files.reduce((sum, f) => sum + f.fileSize, 0))} total</p>
                </div>
              )}
            </>
          )}

          {/* Drop zone when no files */}
          {showUpload && (
            <>
              <div onDragOver={handleDragOver} onDragLeave={handleDragLeave} onDrop={handleDrop} onClick={() => fileInputRef.current?.click()}
                className={`rounded-2xl border-2 border-dashed p-14 text-center cursor-pointer transition-all ${localDragOver ? 'border-orange-400 bg-orange-50 scale-[1.02]' : 'border-slate-300 bg-white hover:border-slate-400 hover:bg-slate-50'}`}>
                <div className="flex flex-col items-center gap-3">
                  <div className="w-16 h-16 rounded-full flex items-center justify-center" style={{ backgroundColor: `${ACCENT_COLOR}15` }}>
                    <svg className="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke={ACCENT_COLOR} strokeWidth={1.5}><path strokeLinecap="round" strokeLinejoin="round" d="M9 13h6m-3-3v6m5 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" /></svg>
                  </div>
                  <button type="button" className="px-8 py-3 text-white font-semibold rounded-xl text-base shadow-lg hover:shadow-xl transition-all" style={{ backgroundColor: ACCENT_COLOR }}>Select PDF files</button>
                  <p className="text-slate-400 text-sm">or drop PDFs here</p>
                </div>
              </div>
              <div className="mt-4 flex justify-center">
                <CloudStorageButtons onFilesReady={handleCloudFilesReady} multiSelect={true} size="md" />
              </div>
            </>
          )}

          <input ref={fileInputRef} type="file" accept=".pdf,application/pdf" multiple className="hidden" onChange={handleFileInput} />
        </div>
      </main>
      <Footer />
    </>
  );
}
</file>

<file path="apps/web/src/app/pdf-to-excel/page.tsx">
'use client';

import FileUploader from '@/components/FileUploader';
import ConversionProgress from '@/components/ConversionProgress';
import DownloadButton from '@/components/DownloadButton';
import { useConversion } from '@/hooks/useConversion';
import Header from '@/components/Header';
import Footer from '@/components/Footer';

const ACCENT_COLOR = '#217346';

export default function PdfToExcelPage() {
  const { file, status, progress, conversionId, error, isProcessing, fileName, fileSize, handleFileSelect, handleCloudFileReady, reset } = useConversion({ outputFormat: 'xlsx' });
  const displayName = fileName || file?.name || '';
  const displaySize = fileSize || file?.size || 0;

  const showUpload = !file && !fileName;

  return (
    <>
      <Header />
      <main className="flex-1 py-6 md:py-8">
        <div className="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
          {showUpload && (
            <div className="text-center mb-8 pt-4">
              <div className="inline-flex items-center gap-3 mb-3">
                <div className="w-14 h-14 rounded-xl flex items-center justify-center" style={{ backgroundColor: `${ACCENT_COLOR}15` }}>
                  <svg className="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke={ACCENT_COLOR} strokeWidth={1.5}>
                    <path strokeLinecap="round" strokeLinejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
                    <path strokeLinecap="round" strokeLinejoin="round" d="M8 11l2 2 4-4" />
                  </svg>
                </div>
              </div>
              <h1 className="text-3xl font-bold text-slate-800 mb-2">PDF to Excel</h1>
              <p className="text-slate-500 max-w-md mx-auto text-sm">Pull data straight from PDFs into Excel spreadsheets. The converted XLSX file preserves table data with incredible accuracy.</p>
            </div>
          )}

          {status === 'COMPLETED' && conversionId && (
            <DownloadButton conversionId={conversionId} fileName={displayName} accentColor={ACCENT_COLOR} onReset={reset} />
          )}

          {status === 'FAILED' && (
            <div className="bg-white rounded-2xl shadow-sm border border-red-200 p-8 text-center animate-fade-in-up">
              <div className="w-16 h-16 mx-auto mb-4 rounded-full bg-red-50 flex items-center justify-center">
                <svg className="w-8 h-8 text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
              </div>
              <h3 className="text-xl font-bold text-slate-800 mb-2">Conversion Failed</h3>
              <p className="text-slate-500 mb-6">{error || 'An unexpected error occurred. Please try again.'}</p>
              <button onClick={reset} className="px-6 py-2.5 text-white font-semibold rounded-xl text-sm shadow-lg hover:shadow-xl transition-all" style={{ backgroundColor: ACCENT_COLOR }}>Try Again</button>
            </div>
          )}

          {(showUpload || status === 'PROCESSING' || status === 'UPLOADING' || status === 'UPLOADED') && status !== 'FAILED' && status !== 'COMPLETED' && (
            showUpload ? (
              <FileUploader onFileSelect={handleFileSelect} onCloudFileReady={handleCloudFileReady} isProcessing={false} accentColor={ACCENT_COLOR} />
            ) : (
              <ConversionProgress progress={progress} status={status} fileName={displayName} fileSize={displaySize} accentColor={ACCENT_COLOR} />
            )
          )}
        </div>
      </main>
      <Footer />
    </>
  );
}
</file>

<file path="apps/web/src/app/pdf-to-word/page.tsx">
'use client';

import FileUploader from '@/components/FileUploader';
import ConversionProgress from '@/components/ConversionProgress';
import DownloadButton from '@/components/DownloadButton';
import { useConversion } from '@/hooks/useConversion';
import Header from '@/components/Header';
import Footer from '@/components/Footer';

const ACCENT_COLOR = '#2B579A';

export default function PdfToWordPage() {
  const { file, status, progress, conversionId, error, isProcessing, fileName, fileSize, handleFileSelect, handleCloudFileReady, reset } = useConversion({ outputFormat: 'docx' });
  const displayName = fileName || file?.name || '';
  const displaySize = fileSize || file?.size || 0;

  const showUpload = !file && !fileName;

  return (
    <>
      <Header />
      <main className="flex-1 py-6 md:py-8">
        <div className="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
          {showUpload && (
            <div className="text-center mb-8 pt-4">
              <div className="inline-flex items-center gap-3 mb-3">
                <div className="w-14 h-14 rounded-xl flex items-center justify-center" style={{ backgroundColor: `${ACCENT_COLOR}15` }}>
                  <svg className="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke={ACCENT_COLOR} strokeWidth={1.5}>
                    <path strokeLinecap="round" strokeLinejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
                    <path strokeLinecap="round" strokeLinejoin="round" d="M9 13h6M9 9h6M9 17h4" />
                  </svg>
                </div>
              </div>
              <h1 className="text-3xl font-bold text-slate-800 mb-2">PDF to Word</h1>
              <p className="text-slate-500 max-w-md mx-auto text-sm">Easily convert your PDF files into easy to edit DOC and DOCX documents. The converted file is fully editable with incredible accuracy.</p>
            </div>
          )}

          {status === 'COMPLETED' && conversionId && (
            <DownloadButton conversionId={conversionId} fileName={displayName} accentColor={ACCENT_COLOR} onReset={reset} />
          )}

          {status === 'FAILED' && (
            <div className="bg-white rounded-2xl shadow-sm border border-red-200 p-8 text-center animate-fade-in-up">
              <div className="w-16 h-16 mx-auto mb-4 rounded-full bg-red-50 flex items-center justify-center">
                <svg className="w-8 h-8 text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
              </div>
              <h3 className="text-xl font-bold text-slate-800 mb-2">Conversion Failed</h3>
              <p className="text-slate-500 mb-6">{error || 'An unexpected error occurred. Please try again.'}</p>
              <button onClick={reset} className="px-6 py-2.5 text-white font-semibold rounded-xl text-sm shadow-lg hover:shadow-xl transition-all" style={{ backgroundColor: ACCENT_COLOR }}>Try Again</button>
            </div>
          )}

          {(showUpload || status === 'PROCESSING' || status === 'UPLOADING' || status === 'UPLOADED') && status !== 'FAILED' && status !== 'COMPLETED' && (
            showUpload ? (
              <FileUploader onFileSelect={handleFileSelect} onCloudFileReady={handleCloudFileReady} isProcessing={false} accentColor={ACCENT_COLOR} />
            ) : (
              <ConversionProgress progress={progress} status={status} fileName={displayName} fileSize={displaySize} accentColor={ACCENT_COLOR} />
            )
          )}
        </div>
      </main>
      <Footer />
    </>
  );
}
</file>

<file path="apps/web/src/app/split-pdf/page.tsx">
'use client';

import { useState, useCallback, useRef } from 'react';
import Header from '@/components/Header';
import Footer from '@/components/Footer';
import CloudStorageButtons from '@/components/CloudStorageButtons';
import PdfPagePreview from '@/components/PdfPagePreview';
import { apiClient } from '@/lib/api';

const ACCENT_COLOR = '#9B59B6';

interface PageRange { id: string; from: number; to: number }
interface SplitOutputFile { fileName: string; filePath: string; pageCount: number; size: number }

export default function SplitPdfPage() {
  const [file, setFile] = useState<File | null>(null);
  const [filePath, setFilePath] = useState<string>('');
  const [fileName, setFileName] = useState<string>('');
  const [fileSize, setFileSize] = useState<number>(0);
  const [totalPages, setTotalPages] = useState<number>(0);

  const [splitMode, setSplitMode] = useState<'range' | 'pages' | 'size'>('range');
  const [rangeMode, setRangeMode] = useState<'custom' | 'fixed'>('fixed');
  const [ranges, setRanges] = useState<PageRange[]>([{ id: '1', from: 1, to: 1 }]);
  const [fixedSize, setFixedSize] = useState<number>(1);
  const [pagesPerFile, setPagesPerFile] = useState<number>(1);

  const [status, setStatus] = useState<string>('IDLE');
  const [progress, setProgress] = useState<number>(0);
  const [error, setError] = useState<string | null>(null);
  const [splitJobId, setSplitJobId] = useState<string | null>(null);
  const [outputFiles, setOutputFiles] = useState<SplitOutputFile[]>([]);

  const fileInputRef = useRef<HTMLInputElement>(null);
  const [localDragOver, setLocalDragOver] = useState(false);

  const formatFileSize = (bytes: number): string => {
    if (bytes === 0) return '0 B';
    const k = 1024;
    const sizes = ['B', 'KB', 'MB', 'GB'];
    const i = Math.floor(Math.log(bytes) / Math.log(k));
    return parseFloat((bytes / Math.pow(k, i)).toFixed(1)) + ' ' + sizes[i];
  };

  const handleFileSelect = useCallback(async (selectedFile: File) => {
    if (selectedFile.type !== 'application/pdf') { setError('Only PDF files are allowed'); return; }
    setFile(selectedFile);
    setFileName(selectedFile.name);
    setFileSize(selectedFile.size);
    setError(null);
    setStatus('UPLOADING');

    try {
      const result = await apiClient.uploadSplitFile(selectedFile);
      setFilePath(result.filePath);
      setTotalPages(result.totalPages);
      setRanges([{ id: '1', from: 1, to: result.totalPages }]);
      setFixedSize(Math.max(1, Math.ceil(result.totalPages / 3)));
      setPagesPerFile(1);
      setStatus('READY');
    } catch (err: any) {
      setError(err.message || 'Failed to upload file');
      setStatus('IDLE');
    }
  }, []);

  const handleCloudFileReady = useCallback(async (cloudFile: { filePath: string; fileName: string; fileSize: number }) => {
    setFile(null);
    setFileName(cloudFile.fileName);
    setFileSize(cloudFile.fileSize);
    setFilePath(cloudFile.filePath);
    setError(null);
    setStatus('ANALYZING');

    try {
      const result = await apiClient.analyzeSplitFile(cloudFile.filePath);
      setTotalPages(result.totalPages);
      setFileSize(result.fileSize || cloudFile.fileSize);
      setRanges([{ id: '1', from: 1, to: result.totalPages }]);
      setFixedSize(Math.max(1, Math.ceil(result.totalPages / 3)));
      setPagesPerFile(1);
      setStatus('READY');
    } catch (err: any) {
      setError(err.message || 'Failed to analyze PDF');
      setStatus('IDLE');
    }
  }, []);

  const handleDrop = useCallback((e: React.DragEvent) => {
    e.preventDefault();
    setLocalDragOver(false);
    const dropped = Array.from(e.dataTransfer.files).filter(f => f.type === 'application/pdf');
    if (dropped.length > 0) handleFileSelect(dropped[0]);
  }, [handleFileSelect]);

  const addRange = () => {
    const newId = String(Date.now());
    const lastRange = ranges[ranges.length - 1];
    const from = lastRange ? Math.min(lastRange.to + 1, totalPages || 1) : 1;
    setRanges([...ranges, { id: newId, from, to: Math.min(from, totalPages || 1) }]);
  };

  const removeRange = (id: string) => {
    if (ranges.length <= 1) return;
    setRanges(ranges.filter(r => r.id !== id));
  };

  const updateRange = (id: string, field: 'from' | 'to', value: number) => {
    setRanges(ranges.map(r => r.id === id ? { ...r, [field]: value } : r));
  };

  const getFixedSplitCount = (): number => {
    if (!fixedSize || fixedSize <= 0 || totalPages <= 0) return 0;
    return Math.ceil(totalPages / fixedSize);
  };

  const startSplit = async () => {
    if (!filePath) { setError('Please select a PDF file first'); return; }
    setError(null);
    setStatus('PROCESSING');
    setProgress(0);
    setOutputFiles([]);

    try {
      let splitData: any = { originalPath: filePath, originalName: fileName, fileSize, splitMode };

      if (splitMode === 'range') {
        if (rangeMode === 'custom') {
          const validRanges = ranges.filter(r => r.from > 0 && r.to >= r.from);
          if (validRanges.length === 0) throw new Error('Please specify at least one valid page range');
          splitData.ranges = validRanges;
        } else {
          if (!fixedSize || fixedSize <= 0) throw new Error('Please enter a valid page count per file');
          splitData.fixedSize = fixedSize;
        }
      } else if (splitMode === 'pages') {
        if (!pagesPerFile || pagesPerFile <= 0) throw new Error('Please enter a valid number');
        splitData.pagesPerFile = pagesPerFile;
      }

      const result = await apiClient.startSplit(splitData);
      setSplitJobId(result.id);
      if (result.totalPages) setTotalPages(result.totalPages);

      const pollInterval = setInterval(async () => {
        try {
          const statusResult = await apiClient.getSplitStatus(result.id);
          setStatus(statusResult.status);
          setProgress(statusResult.progress);
          if (statusResult.status === 'COMPLETED') {
            clearInterval(pollInterval);
            setOutputFiles(statusResult.outputFiles || []);
          }
          if (statusResult.status === 'FAILED') {
            clearInterval(pollInterval);
            setError(statusResult.errorMessage || 'Split failed');
          }
        } catch (err: any) {
          clearInterval(pollInterval);
          setError(err.message);
        }
      }, 2000);
    } catch (err: any) {
      setError(err.message || 'Failed to start split');
      setStatus('READY');
    }
  };

  const reset = () => {
    setFile(null); setFilePath(''); setFileName(''); setFileSize(0); setTotalPages(0);
    setStatus('IDLE'); setProgress(0); setError(null); setSplitJobId(null); setOutputFiles([]);
    setRanges([{ id: '1', from: 1, to: 1 }]); setFixedSize(1); setPagesPerFile(1);
  };

  const hasFile = status !== 'IDLE';

  return (
    <>
      <Header />
      <main className="flex-1 py-6 md:py-8">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

          {!hasFile && (
            <div className="max-w-xl mx-auto pt-8">
              <div className="text-center mb-8">
                <div className="inline-flex items-center gap-3 mb-3">
                  <div className="w-14 h-14 rounded-xl flex items-center justify-center" style={{ backgroundColor: `${ACCENT_COLOR}15` }}>
                    <svg className="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke={ACCENT_COLOR} strokeWidth={1.5}>
                      <path strokeLinecap="round" strokeLinejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                    </svg>
                  </div>
                </div>
                <h1 className="text-3xl font-bold text-slate-800 mb-2">Split PDF</h1>
                <p className="text-slate-500 max-w-md mx-auto text-sm">Separate one page or a whole set for easy conversion into independent PDF files.</p>
              </div>

              <div
                onDragOver={(e) => { e.preventDefault(); setLocalDragOver(true); }}
                onDragLeave={(e) => { e.preventDefault(); setLocalDragOver(false); }}
                onDrop={handleDrop}
                onClick={() => fileInputRef.current?.click()}
                className={`rounded-2xl border-2 border-dashed p-14 text-center cursor-pointer transition-all duration-300 ${localDragOver ? 'border-purple-400 bg-purple-50 scale-[1.02]' : 'border-slate-300 bg-white hover:border-slate-400 hover:bg-slate-50'}`}
              >
                <div className="flex flex-col items-center gap-4">
                  <div className="w-20 h-20 rounded-full flex items-center justify-center" style={{ backgroundColor: `${ACCENT_COLOR}15` }}>
                    <svg className="w-10 h-10" fill="none" viewBox="0 0 24 24" stroke={ACCENT_COLOR} strokeWidth={1.5}>
                      <path strokeLinecap="round" strokeLinejoin="round" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12" />
                    </svg>
                  </div>
                  <button type="button" className="px-8 py-3.5 text-white font-semibold rounded-xl text-base shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 transition-all" style={{ backgroundColor: ACCENT_COLOR }}>Select PDF file</button>
                  <p className="text-slate-400 text-sm">or drop PDF here</p>
                </div>
              </div>
              <div className="mt-4">
                <CloudStorageButtons onFilesReady={(files) => { if (files.length > 0) handleCloudFileReady(files[0]); }} multiSelect={false} size="md" />
              </div>
              <input ref={fileInputRef} type="file" accept=".pdf,application/pdf" className="hidden" onChange={(e) => { const f = e.target.files?.[0]; if (f) handleFileSelect(f); e.target.value = ''; }} />
            </div>
          )}

          {hasFile && (
            <div className="max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-5 gap-5">

              {/* LEFT: Page Preview */}
              <div className="lg:col-span-3">
                <div className="bg-white rounded-2xl border border-slate-200 overflow-hidden">
                  {/* File bar */}
                  <div className="px-4 py-2.5 border-b border-slate-200 flex items-center gap-3" style={{ backgroundColor: `${ACCENT_COLOR}05` }}>
                    <div className="w-7 h-7 rounded-lg flex items-center justify-center" style={{ backgroundColor: `${ACCENT_COLOR}15` }}>
                      <svg className="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke={ACCENT_COLOR} strokeWidth={1.5}>
                        <path strokeLinecap="round" strokeLinejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
                      </svg>
                    </div>
                    <div className="flex-1 min-w-0">
                      <p className="text-sm font-medium text-slate-800 truncate">{fileName}</p>
                      <p className="text-[11px] text-slate-400">{formatFileSize(fileSize)}{totalPages > 0 ? ` • ${totalPages} pages` : ''}</p>
                    </div>
                    <button onClick={reset} className="text-[11px] text-slate-400 hover:text-red-500 transition-colors font-medium px-2 py-1 rounded hover:bg-red-50">Change file</button>
                  </div>

                  {status === 'PROCESSING' && (
                    <div className="p-8 text-center">
                      <svg className="w-10 h-10 animate-spin mx-auto mb-4" style={{ color: ACCENT_COLOR }} fill="none" viewBox="0 0 24 24">
                        <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
                        <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z" />
                      </svg>
                      <h3 className="text-lg font-bold text-slate-800 mb-2">Splitting PDF...</h3>
                      <div className="w-full max-w-xs mx-auto bg-slate-100 rounded-full h-2.5 overflow-hidden mt-4">
                        <div className="h-full rounded-full transition-all duration-500" style={{ width: `${progress}%`, backgroundColor: ACCENT_COLOR }} />
                      </div>
                    </div>
                  )}

                  {status === 'COMPLETED' && (
                    <div className="p-5">
                      <div className="flex items-center justify-between mb-4">
                        <h3 className="text-sm font-semibold text-slate-800">✅ {outputFiles.length} file{outputFiles.length !== 1 ? 's' : ''} created</h3>
                        {splitJobId && outputFiles.length > 1 && (
                          <a href={apiClient.getSplitDownloadAllUrl(splitJobId)} className="text-xs font-medium px-3 py-1.5 rounded-lg text-white hover:opacity-90" style={{ backgroundColor: ACCENT_COLOR }}>Download All (ZIP)</a>
                        )}
                      </div>
                      <div className="grid grid-cols-2 sm:grid-cols-3 gap-3">
                        {outputFiles.map((f, i) => (
                          <div key={i} className="p-3 bg-slate-50 rounded-xl border border-slate-200 hover:border-slate-300 transition-colors">
                            <div className="w-full aspect-[3/4] rounded-lg mb-2 flex flex-col items-center justify-center" style={{ backgroundColor: `${ACCENT_COLOR}08` }}>
                              <svg className="w-8 h-8 mb-1" fill="none" viewBox="0 0 24 24" stroke={ACCENT_COLOR} strokeWidth={1}><path strokeLinecap="round" strokeLinejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" /></svg>
                              <span className="text-xs font-bold" style={{ color: ACCENT_COLOR }}>{f.pageCount}p</span>
                            </div>
                            <p className="text-xs font-medium text-slate-700 truncate">{f.fileName}</p>
                            <div className="flex items-center justify-between mt-1">
                              <span className="text-[10px] text-slate-400">{formatFileSize(f.size)}</span>
                              {splitJobId && <a href={apiClient.getSplitDownloadUrl(splitJobId, i)} className="text-[10px] font-medium hover:underline" style={{ color: ACCENT_COLOR }}>Download</a>}
                            </div>
                          </div>
                        ))}
                      </div>
                      <button onClick={reset} className="block mx-auto text-slate-500 hover:text-slate-700 text-sm font-medium mt-5">Split another PDF</button>
                    </div>
                  )}

                  {status === 'READY' && (
                    <div className="p-4">
                      <PdfPagePreview totalPages={totalPages} ranges={ranges} rangeMode={rangeMode} fixedSize={fixedSize} splitMode={splitMode} pagesPerFile={pagesPerFile} accentColor={ACCENT_COLOR} filePath={filePath} onRangeUpdate={setRanges} />
                    </div>
                  )}
                </div>
              </div>

              {/* RIGHT: Split Options */}
              <div className="lg:col-span-2">
                <div className="bg-white rounded-2xl border border-slate-200 overflow-hidden sticky top-20">
                  <div className="flex border-b border-slate-200">
                    {(['range', 'pages', 'size'] as const).map((mode) => (
                      <button key={mode} onClick={() => setSplitMode(mode)} className={`flex-1 py-3 text-xs font-medium transition-colors relative ${splitMode === mode ? 'text-slate-800' : 'text-slate-400 hover:text-slate-600'}`}>
                        {mode === 'range' ? 'By Range' : mode === 'pages' ? 'By Pages' : 'By Size'}
                        {splitMode === mode && <div className="absolute bottom-0 left-0 right-0 h-0.5" style={{ backgroundColor: ACCENT_COLOR }} />}
                      </button>
                    ))}
                  </div>

                  <div className="p-5">
                    {splitMode === 'range' && (
                      <div>
                        <div className="flex bg-slate-100 rounded-lg p-1 mb-5">
                          <button onClick={() => setRangeMode('custom')} className={`flex-1 py-2 text-xs font-medium rounded-md transition-all ${rangeMode === 'custom' ? 'bg-white text-slate-800 shadow-sm' : 'text-slate-500'}`}>Custom Ranges</button>
                          <button onClick={() => setRangeMode('fixed')} className={`flex-1 py-2 text-xs font-medium rounded-md transition-all ${rangeMode === 'fixed' ? 'bg-white text-slate-800 shadow-sm' : 'text-slate-500'}`}>Fixed Size</button>
                        </div>

                        {rangeMode === 'custom' && (
                          <div>
                            <div className="space-y-3">
                              {ranges.map((range, index) => (
                                <div key={range.id} className="flex items-center gap-2">
                                  <span className="text-xs text-slate-400 w-5 text-right">{index + 1}.</span>
                                  <div className="flex items-center gap-2 flex-1">
                                    <div className="flex-1">
                                      <label className="text-[10px] text-slate-400 mb-0.5 block">From page</label>
                                      <input type="number" min={1} max={totalPages || 1} value={range.from} onChange={(e) => updateRange(range.id, 'from', parseInt(e.target.value) || 1)} className="w-full px-2.5 py-1.5 border border-slate-200 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-purple-300" />
                                    </div>
                                    <div className="flex-1">
                                      <label className="text-[10px] text-slate-400 mb-0.5 block">To page</label>
                                      <input type="number" min={1} max={totalPages || 1} value={range.to} onChange={(e) => updateRange(range.id, 'to', parseInt(e.target.value) || 1)} className="w-full px-2.5 py-1.5 border border-slate-200 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-purple-300" />
                                    </div>
                                  </div>
                                  {ranges.length > 1 && (
                                    <button onClick={() => removeRange(range.id)} className="p-1.5 text-slate-400 hover:text-red-500 mt-4">
                                      <svg className="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" /></svg>
                                    </button>
                                  )}
                                </div>
                              ))}
                            </div>
                            <button onClick={addRange} className="mt-3 w-full py-2 border-2 border-dashed border-slate-300 rounded-lg text-xs font-medium text-slate-500 hover:border-slate-400 hover:text-slate-600 hover:bg-slate-50 transition-all flex items-center justify-center gap-1">
                              <svg className="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M12 4v16m8-8H4" /></svg>
                              Add Range
                            </button>
                            {totalPages > 0 && (
                              <div className="mt-4 p-2.5 rounded-lg text-xs" style={{ backgroundColor: `${ACCENT_COLOR}08` }}>
                                <p style={{ color: ACCENT_COLOR }}>{ranges.length} PDF{ranges.length !== 1 ? 's' : ''} will be created</p>
                              </div>
                            )}
                          </div>
                        )}

                        {rangeMode === 'fixed' && (
                          <div>
                            <label className="text-sm text-slate-600 font-medium mb-2 block">Split into page ranges of:</label>
                            <input type="number" min={1} max={totalPages || 1} value={fixedSize} onChange={(e) => setFixedSize(parseInt(e.target.value) || 1)} className="w-full px-3 py-2.5 border border-slate-200 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-purple-300" />
                            {totalPages > 0 && fixedSize > 0 && (
                              <div className="mt-4 p-2.5 rounded-lg text-xs" style={{ backgroundColor: `${ACCENT_COLOR}08` }}>
                                <p style={{ color: ACCENT_COLOR }}>
                                  This PDF will be split into files of <strong>{fixedSize} page{fixedSize !== 1 ? 's' : ''}</strong>.{' '}
                                  <strong>{getFixedSplitCount()} PDF{getFixedSplitCount() !== 1 ? 's' : ''}</strong> will be created.
                                </p>
                              </div>
                            )}
                          </div>
                        )}
                      </div>
                    )}

                    {splitMode === 'pages' && (
                      <div>
                        <label className="text-sm text-slate-600 font-medium mb-2 block">Extract every N pages:</label>
                        <input type="number" min={1} max={totalPages || 1} value={pagesPerFile} onChange={(e) => setPagesPerFile(parseInt(e.target.value) || 1)} className="w-full px-3 py-2.5 border border-slate-200 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-purple-300" />
                        {totalPages > 0 && pagesPerFile > 0 && (
                          <div className="mt-4 p-2.5 rounded-lg text-xs" style={{ backgroundColor: `${ACCENT_COLOR}08` }}>
                            <p style={{ color: ACCENT_COLOR }}>
                              Every <strong>{pagesPerFile} page{pagesPerFile !== 1 ? 's' : ''}</strong> → separate file.{' '}
                              <strong>{Math.ceil(totalPages / pagesPerFile)} PDF{Math.ceil(totalPages / pagesPerFile) !== 1 ? 's' : ''}</strong> will be created.
                            </p>
                          </div>
                        )}
                      </div>
                    )}

                    {splitMode === 'size' && (
                      <div className="text-center py-8">
                        <div className="w-12 h-12 mx-auto mb-3 rounded-full bg-slate-100 flex items-center justify-center">
                          <svg className="w-6 h-6 text-slate-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5}>
                            <path strokeLinecap="round" strokeLinejoin="round" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.066 2.573c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.573 1.066c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.066-2.573c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
                            <path strokeLinecap="round" strokeLinejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                          </svg>
                        </div>
                        <p className="text-sm text-slate-400">Coming soon</p>
                      </div>
                    )}

                    {splitMode !== 'size' && (
                      <button onClick={startSplit} disabled={status === 'PROCESSING'} className="w-full mt-5 py-3.5 text-white font-bold rounded-xl text-base shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 transition-all disabled:opacity-60 disabled:cursor-not-allowed disabled:transform-none" style={{ backgroundColor: ACCENT_COLOR }}>
                        <span className="flex items-center justify-center gap-2">
                          <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}><path strokeLinecap="round" strokeLinejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" /></svg>
                          Split PDF
                        </span>
                      </button>
                    )}
                  </div>
                </div>
              </div>
            </div>
          )}

          {error && status !== 'FAILED' && (
            <div className="mt-4 max-w-xl mx-auto p-3 bg-red-50 border border-red-200 rounded-lg text-red-700 text-sm text-center animate-fade-in-up">
              <span className="font-medium">Error:</span> {error}
            </div>
          )}
        </div>
      </main>
      <Footer />
    </>
  );
}
</file>

<file path="apps/web/src/app/globals.css">
@tailwind base;
@tailwind components;
@tailwind utilities;

@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap');

:root {
  --gradient-primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  --gradient-accent: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: 'Inter', system-ui, -apple-system, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  background-color: #f8fafc;
}

::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: #f1f5f9;
}

::-webkit-scrollbar-thumb {
  background: #94a3b8;
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: #64748b;
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes pulse-glow {
  0%, 100% {
    box-shadow: 0 0 20px rgba(59, 130, 246, 0.3);
  }
  50% {
    box-shadow: 0 0 40px rgba(59, 130, 246, 0.6);
  }
}

@keyframes float {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-10px);
  }
}

.animate-fade-in-up {
  animation: fadeInUp 0.6s ease-out forwards;
}

.animate-pulse-glow {
  animation: pulse-glow 2s ease-in-out infinite;
}

.animate-float {
  animation: float 3s ease-in-out infinite;
}

.stagger-1 { animation-delay: 0.1s; }
.stagger-2 { animation-delay: 0.2s; }
.stagger-3 { animation-delay: 0.3s; }
.stagger-4 { animation-delay: 0.4s; }
.stagger-5 { animation-delay: 0.5s; }
.stagger-6 { animation-delay: 0.6s; }
.stagger-7 { animation-delay: 0.7s; }
.stagger-8 { animation-delay: 0.8s; }
</file>

<file path="apps/web/src/app/layout.tsx">
import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: 'PDF Converter - Your PDF Editor',
  description: 'Convert PDF files to Word, Excel, PowerPoint and more with incredible accuracy.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="min-h-screen flex flex-col">
        {children}
      </body>
    </html>
  );
}
</file>

<file path="apps/web/src/app/page.tsx">
import Header from '@/components/Header';
import Footer from '@/components/Footer';
import ToolCard from '@/components/ToolCard';
import { TOOLS } from '@pdf-converter/shared';

export default function HomePage() {
  return (
    <>
      <Header />
      
      <main className="flex-1">
        <section className="relative overflow-hidden bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 py-20 md:py-28">
          <div className="absolute inset-0 overflow-hidden">
            <div className="absolute -top-40 -right-40 w-80 h-80 bg-red-500/10 rounded-full blur-3xl" />
            <div className="absolute -bottom-40 -left-40 w-80 h-80 bg-blue-500/10 rounded-full blur-3xl" />
            <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-purple-500/5 rounded-full blur-3xl" />
          </div>

          <div className="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <h1 className="text-4xl md:text-6xl font-black text-white mb-4 tracking-tight animate-fade-in-up">
              Every Tool You Need to
              <br />
              <span className="bg-gradient-to-r from-red-400 to-orange-400 bg-clip-text text-transparent">
                Work with PDFs
              </span>
            </h1>
            <p className="text-lg md:text-xl text-slate-300 max-w-2xl mx-auto mb-8 animate-fade-in-up stagger-2">
              Convert, merge, split, compress, and edit your PDF files with incredible accuracy. 
              Fast, secure, and completely free.
            </p>

            <div className="max-w-xl mx-auto animate-fade-in-up stagger-3">
              <div className="relative">
                <svg className="absolute left-4 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
                  <path strokeLinecap="round" strokeLinejoin="round" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                </svg>
                <input
                  type="text"
                  placeholder="Search for a tool... (e.g., PDF to Word)"
                  className="w-full pl-12 pr-4 py-4 bg-white/10 backdrop-blur-sm border border-white/20 rounded-2xl text-white placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-red-500/50 focus:border-transparent text-base"
                />
              </div>
            </div>
          </div>
        </section>

        <section className="py-16 md:py-20">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div className="text-center mb-12">
              <h2 className="text-2xl md:text-3xl font-bold text-slate-800 mb-2">
                All PDF Tools in One Place
              </h2>
              <p className="text-slate-500 max-w-lg mx-auto">
                Select a tool below to get started. Every tool is free and works right in your browser.
              </p>
            </div>

            <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5">
              {TOOLS.map((tool, index) => (
                <ToolCard
                  key={tool.id}
                  title={tool.title}
                  description={tool.description}
                  icon={tool.icon}
                  color={tool.color}
                  href={tool.href}
                  index={index}
                />
              ))}
            </div>
          </div>
        </section>

        <section className="py-12 bg-slate-50 border-t border-slate-200">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
            <p className="text-sm text-slate-400 mb-4">Powered by</p>
            <p className="text-lg font-semibold text-slate-600">
              Solid Documents Conversion Engine
            </p>
            <p className="text-sm text-slate-400 mt-2 max-w-md mx-auto">
              Industry-leading PDF conversion technology delivering incredible accuracy and preserving formatting, layouts, and fonts.
            </p>
          </div>
        </section>
      </main>

      <Footer />
    </>
  );
}
</file>

<file path="apps/web/src/components/CloudStorageButtons.tsx">
'use client';

import { useState, useEffect } from 'react';
import {
  openGoogleDrivePicker,
  openDropboxOAuth,
  preloadGoogleSDK,
  getCloudConfig,
} from '@/lib/cloud-storage';

interface CloudStorageButtonsProps {
  onFilesReady: (files: Array<{ filePath: string; fileName: string; fileSize: number }>) => void;
  multiSelect?: boolean;
  size?: 'sm' | 'md';
}

export default function CloudStorageButtons({
  onFilesReady,
  multiSelect = false,
  size = 'md',
}: CloudStorageButtonsProps) {
  const [loading, setLoading] = useState<'drive' | 'dropbox' | null>(null);
  const [error, setError] = useState<string | null>(null);
  const config = getCloudConfig();

  useEffect(() => {
    if (config.googleDriveEnabled) {
      preloadGoogleSDK().catch(() => {});
    }
  }, []);

  const handleGoogleDrive = async () => {
    if (!config.googleDriveEnabled) {
      setError('Google Drive not configured');
      return;
    }

    setLoading('drive');
    setError(null);

    try {
      const driveFiles = await openGoogleDrivePicker(config.googleClientId, config.googleApiKey, multiSelect);

      if (driveFiles.length === 0) { setLoading(null); return; }

      const downloadedFiles = [];
      for (const driveFile of driveFiles) {
        const { downloadFromDrive } = await import('@/lib/cloud-storage');
        const result = await downloadFromDrive(driveFile.accessToken, driveFile.fileId, driveFile.fileName);
        downloadedFiles.push(result);
      }

      onFilesReady(downloadedFiles);
    } catch (err: any) {
      console.error('Google Drive error:', err);
      setError(err.message || 'Failed to select file from Google Drive');
    } finally {
      setLoading(null);
    }
  };

  const handleDropbox = async () => {
    setLoading('dropbox');
    setError(null);

    try {
      const dropboxFiles = await openDropboxOAuth(multiSelect);

      if (dropboxFiles.length === 0) { setLoading(null); return; }

      const downloadedFiles = [];
      for (const dbFile of dropboxFiles) {
        const { downloadFromDropbox } = await import('@/lib/cloud-storage');
        const result = await downloadFromDropbox(dbFile.accessToken, dbFile.path, dbFile.fileName);
        downloadedFiles.push(result);
      }

      onFilesReady(downloadedFiles);
    } catch (err: any) {
      console.error('Dropbox error:', err);
      setError(err.message || 'Failed to select file from Dropbox');
    } finally {
      setLoading(null);
    }
  };

  const isSmall = size === 'sm';

  return (
    <div>
      <div className="flex items-center justify-center gap-3">
        <button
          onClick={handleGoogleDrive}
          disabled={!!loading}
          className={`flex items-center gap-2 rounded-lg border border-slate-200 bg-white hover:bg-slate-50 hover:border-slate-300 transition-all duration-200 disabled:opacity-50 disabled:cursor-not-allowed ${isSmall ? 'px-3 py-1.5 text-xs' : 'px-4 py-2 text-sm'}`}
          title={config.googleDriveEnabled ? 'Select from Google Drive' : 'Google Drive not configured'}
        >
          {loading === 'drive' ? (
            <svg className="w-4 h-4 animate-spin" viewBox="0 0 24 24" fill="none">
              <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
              <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z" />
            </svg>
          ) : (
            <svg className="w-4 h-4" viewBox="0 0 87.3 78" xmlns="http://www.w3.org/2000/svg">
              <path d="M6.6 66.85l3.85 6.65c.8 1.4 1.95 2.5 3.3 3.3l13.75-23.8H0c0 1.55.4 3.1 1.2 4.5l5.4 9.35z" fill="#0066DA"/>
              <path d="M43.65 25.15L29.9 1.35C28.55 2.15 27.4 3.25 26.6 4.65L1.2 48.2c-.8 1.4-1.2 2.95-1.2 4.5h27.5l16.15-27.55z" fill="#00AC47"/>
              <path d="M73.55 76.8c1.35-.8 2.5-1.9 3.3-3.3l1.6-2.75 7.65-13.25c.8-1.4 1.2-2.95 1.2-4.5H59.85l5.85 10.75 7.85 13.05z" fill="#EA4335"/>
              <path d="M43.65 25.15L57.4 1.35C56.05.55 54.5 0 52.9 0H34.4c-1.6 0-3.15.45-4.5 1.35l13.75 23.8z" fill="#00832D"/>
              <path d="M59.85 53H27.5L13.75 76.8c1.35.8 2.9 1.2 4.5 1.2h22.9c1.6 0 3.15-.45 4.5-1.2L59.85 53z" fill="#2684FC"/>
              <path d="M73.4 26.5l-12.7-22c-.8-1.4-1.95-2.5-3.3-3.3L43.65 25.15 59.85 53h27.45c0-1.55-.4-3.1-1.2-4.5L73.4 26.5z" fill="#FFBA00"/>
            </svg>
          )}
          <span className="font-medium text-slate-600">{loading === 'drive' ? 'Connecting...' : 'Google Drive'}</span>
          {!config.googleDriveEnabled && <span className="text-red-400 text-xs ml-1">⚠</span>}
        </button>

        <span className="text-slate-300 text-xs">or</span>

        <button
          onClick={handleDropbox}
          disabled={!!loading}
          className={`flex items-center gap-2 rounded-lg border border-slate-200 bg-white hover:bg-slate-50 hover:border-slate-300 transition-all duration-200 disabled:opacity-50 disabled:cursor-not-allowed ${isSmall ? 'px-3 py-1.5 text-xs' : 'px-4 py-2 text-sm'}`}
          title="Select from Dropbox"
        >
          {loading === 'dropbox' ? (
            <svg className="w-4 h-4 animate-spin" viewBox="0 0 24 24" fill="none">
              <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
              <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z" />
            </svg>
          ) : (
            <svg className="w-4 h-4" viewBox="0 0 24 24" fill="#0061FF">
              <path d="M7.2 4.2L0 9l5.1 4.2L12 8.4l5.1 4.8L24 9l-7.2-4.8L12 8.4 7.2 4.2zM0 14.4l7.2 4.8L12 15.6l-5.1-4.2L0 14.4zM12 15.6l4.8 3.6 7.2-4.8-5.1-3.6L12 15.6zM12 24l-4.8-3.6L12 16.8l4.8 3.6L12 24z"/>
            </svg>
          )}
          <span className="font-medium text-slate-600">{loading === 'dropbox' ? 'Connecting...' : 'Dropbox'}</span>
        </button>
      </div>

      {error && (
        <div className="mt-3 p-2.5 bg-red-50 border border-red-200 rounded-lg text-red-600 text-xs text-center animate-fade-in-up">
          {error}
        </div>
      )}
    </div>
  );
}
</file>

<file path="apps/web/src/components/ConversionProgress.tsx">
'use client';

interface ConversionProgressProps {
  progress: number;
  status: string;
  fileName: string;
  fileSize: number;
  accentColor?: string;
}

export default function ConversionProgress({ 
  progress, 
  status, 
  fileName, 
  fileSize,
  accentColor = '#2B579A' 
}: ConversionProgressProps) {
  const formatFileSize = (bytes: number): string => {
    if (bytes === 0) return '0 B';
    const k = 1024;
    const sizes = ['B', 'KB', 'MB', 'GB'];
    const i = Math.floor(Math.log(bytes) / Math.log(k));
    return parseFloat((bytes / Math.pow(k, i)).toFixed(1)) + ' ' + sizes[i];
  };

  const getStatusText = (): string => {
    switch (status) {
      case 'PENDING': return 'Preparing your file...';
      case 'PROCESSING': return 'Converting PDF to Word...';
      case 'COMPLETED': return 'Conversion complete!';
      case 'FAILED': return 'Conversion failed';
      default: return 'Processing...';
    }
  };

  const getStatusIcon = () => {
    switch (status) {
      case 'COMPLETED':
        return (
          <svg className="w-6 h-6 text-emerald-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
            <path strokeLinecap="round" strokeLinejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        );
      case 'FAILED':
        return (
          <svg className="w-6 h-6 text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
            <path strokeLinecap="round" strokeLinejoin="round" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        );
      default:
        return (
          <svg className="w-6 h-6 animate-spin" style={{ color: accentColor }} fill="none" viewBox="0 0 24 24">
            <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
            <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z" />
          </svg>
        );
    }
  };

  return (
    <div className="w-full max-w-2xl mx-auto animate-fade-in-up">
      <div className="bg-white rounded-2xl shadow-sm border border-slate-200 p-6">
        <div className="flex items-center gap-3 mb-5">
          <div 
            className="w-10 h-10 rounded-lg flex items-center justify-center flex-shrink-0"
            style={{ backgroundColor: `${accentColor}15` }}
          >
            <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke={accentColor} strokeWidth={1.5}>
              <path strokeLinecap="round" strokeLinejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
            </svg>
          </div>
          <div className="flex-1 min-w-0">
            <p className="text-sm font-medium text-slate-800 truncate">{fileName}</p>
            <p className="text-xs text-slate-400">{formatFileSize(fileSize)}</p>
          </div>
          {getStatusIcon()}
        </div>

        <div className="space-y-2">
          <div className="flex items-center justify-between text-sm">
            <span className="text-slate-600 font-medium">{getStatusText()}</span>
            <span className="text-slate-500">{progress}%</span>
          </div>
          <div className="w-full bg-slate-100 rounded-full h-2.5 overflow-hidden">
            <div
              className="h-full rounded-full transition-all duration-500 ease-out"
              style={{ 
                width: `${progress}%`,
                backgroundColor: status === 'FAILED' ? '#EF4444' : status === 'COMPLETED' ? '#10B981' : accentColor,
              }}
            />
          </div>
        </div>
      </div>
    </div>
  );
}
</file>

<file path="apps/web/src/components/DownloadButton.tsx">
'use client';

import { apiClient } from '@/lib/api';

interface DownloadButtonProps {
  conversionId: string;
  fileName: string;
  accentColor?: string;
  onReset?: () => void;
}

export default function DownloadButton({ conversionId, fileName, accentColor = '#2B579A', onReset }: DownloadButtonProps) {
  const handleDownload = () => {
    const url = apiClient.getDownloadUrl(conversionId);
    const link = document.createElement('a');
    link.href = url;
    link.download = fileName.replace('.pdf', '.docx');
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
  };

  return (
    <div className="w-full max-w-2xl mx-auto animate-fade-in-up">
      <div className="bg-white rounded-2xl shadow-sm border border-slate-200 p-8 text-center">
        <div className="w-16 h-16 mx-auto mb-4 rounded-full bg-emerald-50 flex items-center justify-center">
          <svg className="w-8 h-8 text-emerald-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
            <path strokeLinecap="round" strokeLinejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>

        <h3 className="text-xl font-bold text-slate-800 mb-2">Conversion Complete!</h3>
        <p className="text-slate-500 mb-6">Your Word document is ready to download.</p>

        <button
          onClick={handleDownload}
          className="px-8 py-3.5 text-white font-semibold rounded-xl text-base shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 transition-all duration-200 mb-4"
          style={{ backgroundColor: accentColor }}
        >
          <span className="flex items-center gap-2">
            <svg className="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
              <path strokeLinecap="round" strokeLinejoin="round" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
            </svg>
            Download Word Document
          </span>
        </button>

        {onReset && (
          <button
            onClick={onReset}
            className="block mx-auto text-slate-500 hover:text-slate-700 text-sm font-medium transition-colors"
          >
            Convert another PDF
          </button>
        )}
      </div>
    </div>
  );
}
</file>

<file path="apps/web/src/components/DropboxFilePicker.tsx">
'use client';

import { useState, useEffect, useCallback } from 'react';

interface DropboxFile {
  id: string;
  name: string;
  type: 'file' | 'folder';
  path: string;
  size: number;
  modified: string;
}

interface DropboxFilePickerProps {
  accessToken: string;
  onFileSelect: (file: DropboxFile) => void;
  onFilesSelect?: (files: DropboxFile[]) => void;
  multiSelect?: boolean;
  accentColor?: string;
}

const API_BASE = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001/api';

export default function DropboxFilePicker({
  accessToken,
  onFileSelect,
  onFilesSelect,
  multiSelect = false,
  accentColor = '#0061FF',
}: DropboxFilePickerProps) {
  const [files, setFiles] = useState<DropboxFile[]>([]);
  const [currentPath, setCurrentPath] = useState('');
  const [pathHistory, setPathHistory] = useState<string[]>(['']);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const [selectedFiles, setSelectedFiles] = useState<Set<string>>(new Set());

  const fetchFiles = useCallback(async (path: string = '') => {
    setLoading(true);
    setError(null);

    try {
      const response = await fetch(`${API_BASE}/dropbox-auth/files`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ accessToken, path }),
      });

      if (!response.ok) {
        const err = await response.json().catch(() => ({ message: 'Failed to load files' }));
        throw new Error(err.message || 'Failed to load files');
      }

      const data = await response.json();
      // Sort: folders first, then files
      const sorted = data.items.sort((a: DropboxFile, b: DropboxFile) => {
        if (a.type === 'folder' && b.type !== 'folder') return -1;
        if (a.type !== 'folder' && b.type === 'folder') return 1;
        return a.name.localeCompare(b.name);
      });
      setFiles(sorted);
    } catch (err: any) {
      setError(err.message);
    } finally {
      setLoading(false);
    }
  }, [accessToken]);

  useEffect(() => {
    fetchFiles('');
  }, [fetchFiles]);

  const navigateToFolder = (folderPath: string) => {
    setPathHistory((prev) => [...prev, folderPath]);
    setCurrentPath(folderPath);
    setSelectedFiles(new Set());
    fetchFiles(folderPath);
  };

  const goBack = () => {
    const newHistory = [...pathHistory];
    newHistory.pop();
    const parentPath = newHistory[newHistory.length - 1] || '';
    setPathHistory(newHistory);
    setCurrentPath(parentPath);
    setSelectedFiles(new Set());
    fetchFiles(parentPath);
  };

  const toggleSelect = (file: DropboxFile) => {
    if (file.type === 'folder') {
      navigateToFolder(file.path);
      return;
    }

    const newSelected = new Set(selectedFiles);

    if (multiSelect) {
      if (newSelected.has(file.id)) {
        newSelected.delete(file.id);
      } else {
        newSelected.add(file.id);
      }
      setSelectedFiles(newSelected);
    } else {
      // Single select: select and immediately confirm
      onFileSelect(file);
    }
  };

  const confirmSelection = () => {
    const selected = files.filter((f) => selectedFiles.has(f.id));
    if (onFilesSelect && selected.length > 0) {
      onFilesSelect(selected);
    }
  };

  const formatFileSize = (bytes: number): string => {
    if (bytes === 0) return '';
    const k = 1024;
    const sizes = ['B', 'KB', 'MB', 'GB'];
    const i = Math.floor(Math.log(bytes) / Math.log(k));
    return parseFloat((bytes / Math.pow(k, i)).toFixed(1)) + ' ' + sizes[i];
  };

  return (
    <div className="bg-white rounded-xl border border-slate-200 shadow-lg overflow-hidden max-w-md w-full max-h-[70vh] flex flex-col">
      {/* Header */}
      <div className="px-4 py-3 border-b border-slate-200 flex items-center gap-3" style={{ backgroundColor: `${accentColor}08` }}>
        <svg className="w-5 h-5" viewBox="0 0 24 24" fill={accentColor}>
          <path d="M7.2 4.2L0 9l5.1 4.2L12 8.4l5.1 4.8L24 9l-7.2-4.8L12 8.4 7.2 4.2zM0 14.4l7.2 4.8L12 15.6l-5.1-4.2L0 14.4zM12 15.6l4.8 3.6 7.2-4.8-5.1-3.6L12 15.6zM12 24l-4.8-3.6L12 16.8l4.8 3.6L12 24z"/>
        </svg>
        <div className="flex-1 min-w-0">
          <h3 className="text-sm font-semibold text-slate-800">Dropbox</h3>
          <p className="text-xs text-slate-400 truncate">
            {currentPath || '/'} 
          </p>
        </div>
        {pathHistory.length > 1 && (
          <button
            onClick={goBack}
            className="text-xs text-slate-500 hover:text-slate-700 font-medium flex items-center gap-1"
          >
            <svg className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
              <path strokeLinecap="round" strokeLinejoin="round" d="M15 19l-7-7 7-7" />
            </svg>
            Back
          </button>
        )}
      </div>

      {/* File list */}
      <div className="flex-1 overflow-y-auto min-h-[200px]">
        {loading ? (
          <div className="flex items-center justify-center py-12">
            <svg className="w-6 h-6 animate-spin text-slate-400" viewBox="0 0 24 24" fill="none">
              <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4" />
              <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z" />
            </svg>
          </div>
        ) : error ? (
          <div className="flex items-center justify-center py-12 text-red-500 text-sm">{error}</div>
        ) : files.length === 0 ? (
          <div className="flex items-center justify-center py-12 text-slate-400 text-sm">No PDF files found</div>
        ) : (
          <ul className="divide-y divide-slate-100">
            {files.map((file) => (
              <li
                key={file.id}
                onClick={() => toggleSelect(file)}
                className={`
                  px-4 py-3 flex items-center gap-3 cursor-pointer transition-colors
                  ${file.type === 'folder' ? 'hover:bg-slate-50' : ''}
                  ${selectedFiles.has(file.id) ? 'bg-blue-50' : 'hover:bg-slate-50'}
                `}
              >
                {/* Icon */}
                {file.type === 'folder' ? (
                  <svg className="w-5 h-5 text-yellow-500 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M2 6a2 2 0 012-2h5l2 2h5a2 2 0 012 2v6a2 2 0 01-2 2H4a2 2 0 01-2-2V6z" />
                  </svg>
                ) : (
                  <svg className="w-5 h-5 flex-shrink-0" style={{ color: accentColor }} fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5}>
                    <path strokeLinecap="round" strokeLinejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
                  </svg>
                )}

                {/* Name + size */}
                <div className="flex-1 min-w-0">
                  <p className="text-sm font-medium text-slate-700 truncate">{file.name}</p>
                  {file.type === 'file' && file.size > 0 && (
                    <p className="text-xs text-slate-400">{formatFileSize(file.size)}</p>
                  )}
                </div>

                {/* Checkbox for multi-select */}
                {multiSelect && file.type === 'file' && (
                  <div className={`
                    w-5 h-5 rounded border-2 flex items-center justify-center flex-shrink-0
                    ${selectedFiles.has(file.id) ? 'bg-blue-500 border-blue-500' : 'border-slate-300'}
                  `}>
                    {selectedFiles.has(file.id) && (
                      <svg className="w-3 h-3 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={3}>
                        <path strokeLinecap="round" strokeLinejoin="round" d="M5 13l4 4L19 7" />
                      </svg>
                    )}
                  </div>
                )}

                {/* Arrow for folders */}
                {file.type === 'folder' && (
                  <svg className="w-4 h-4 text-slate-400 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
                    <path strokeLinecap="round" strokeLinejoin="round" d="M9 5l7 7-7 7" />
                  </svg>
                )}
              </li>
            ))}
          </ul>
        )}
      </div>

      {/* Footer with confirm button */}
      {multiSelect && (
        <div className="px-4 py-3 border-t border-slate-200 bg-slate-50">
          <button
            onClick={confirmSelection}
            disabled={selectedFiles.size === 0}
            className="w-full py-2.5 text-white font-semibold rounded-lg text-sm transition-all disabled:opacity-50 disabled:cursor-not-allowed"
            style={{ backgroundColor: accentColor }}
          >
            {selectedFiles.size === 0
              ? 'Select files'
              : `Select ${selectedFiles.size} file${selectedFiles.size > 1 ? 's' : ''}`
            }
          </button>
        </div>
      )}
    </div>
  );
}
</file>

<file path="apps/web/src/components/FileUploader.tsx">
'use client';

import { useState, useCallback, useRef } from 'react';
import CloudStorageButtons from './CloudStorageButtons';

interface FileUploaderProps {
  onFileSelect: (file: File) => void;
  onCloudFileReady?: (file: { filePath: string; fileName: string; fileSize: number }) => void;
  isProcessing: boolean;
  accentColor?: string;
}

export default function FileUploader({ onFileSelect, onCloudFileReady, isProcessing, accentColor = '#2B579A' }: FileUploaderProps) {
  const [isDragOver, setIsDragOver] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const fileInputRef = useRef<HTMLInputElement>(null);

  const validateFile = (file: File): string | null => {
    if (file.type !== 'application/pdf') {
      return 'Only PDF files are allowed';
    }
    if (file.size > 50 * 1024 * 1024) {
      return 'File size must be less than 50MB';
    }
    return null;
  };

  const handleFile = useCallback((file: File) => {
    setError(null);
    const validationError = validateFile(file);
    if (validationError) {
      setError(validationError);
      return;
    }
    onFileSelect(file);
  }, [onFileSelect]);

  const handleDragOver = useCallback((e: React.DragEvent) => {
    e.preventDefault();
    e.stopPropagation();
    setIsDragOver(true);
  }, []);

  const handleDragLeave = useCallback((e: React.DragEvent) => {
    e.preventDefault();
    e.stopPropagation();
    setIsDragOver(false);
  }, []);

  const handleDrop = useCallback((e: React.DragEvent) => {
    e.preventDefault();
    e.stopPropagation();
    setIsDragOver(false);
    const files = e.dataTransfer.files;
    if (files.length > 0) {
      handleFile(files[0]);
    }
  }, [handleFile]);

  const handleInputChange = useCallback((e: React.ChangeEvent<HTMLInputElement>) => {
    const files = e.target.files;
    if (files && files.length > 0) {
      handleFile(files[0]);
    }
  }, [handleFile]);

  const handleClick = () => {
    if (!isProcessing) {
      fileInputRef.current?.click();
    }
  };

  const handleCloudFilesReady = (files: Array<{ filePath: string; fileName: string; fileSize: number }>) => {
    if (files.length > 0 && onCloudFileReady) {
      onCloudFileReady(files[0]);
    }
  };

  return (
    <div className="w-full max-w-2xl mx-auto">
      <div
        onDragOver={handleDragOver}
        onDragLeave={handleDragLeave}
        onDrop={handleDrop}
        onClick={handleClick}
        className={`
          relative rounded-2xl border-2 border-dashed p-12 text-center cursor-pointer
          transition-all duration-300 ease-in-out
          ${isProcessing ? 'pointer-events-none opacity-60' : ''}
          ${isDragOver 
            ? 'border-blue-400 bg-blue-50 scale-[1.02]' 
            : 'border-slate-300 bg-white hover:border-slate-400 hover:bg-slate-50'
          }
        `}
        style={isDragOver ? { borderColor: accentColor, backgroundColor: `${accentColor}10` } : {}}
      >
        <div className="flex flex-col items-center gap-4">
          <div 
            className="w-20 h-20 rounded-full flex items-center justify-center transition-transform duration-300"
            style={{ backgroundColor: `${accentColor}15` }}
          >
            <svg className="w-10 h-10" fill="none" viewBox="0 0 24 24" stroke={accentColor} strokeWidth={1.5}>
              <path strokeLinecap="round" strokeLinejoin="round" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12" />
            </svg>
          </div>

          <button
            type="button"
            disabled={isProcessing}
            className="px-8 py-3.5 text-white font-semibold rounded-xl text-base shadow-lg hover:shadow-xl transform hover:-translate-y-0.5 transition-all duration-200 disabled:opacity-60 disabled:cursor-not-allowed disabled:transform-none"
            style={{ backgroundColor: accentColor }}
            onClick={(e) => {
              e.stopPropagation();
              handleClick();
            }}
          >
            Select PDF file
          </button>

          <p className="text-slate-400 text-sm">or drop PDF here</p>
        </div>

        <input
          ref={fileInputRef}
          type="file"
          accept=".pdf,application/pdf"
          className="hidden"
          onChange={handleInputChange}
          disabled={isProcessing}
        />
      </div>

      {/* Cloud storage buttons */}
      <div className="mt-4">
        <CloudStorageButtons
          onFilesReady={handleCloudFilesReady}
          multiSelect={false}
          size="md"
        />
      </div>

      {error && (
        <div className="mt-4 p-3 bg-red-50 border border-red-200 rounded-lg text-red-700 text-sm text-center animate-fade-in-up">
          <span className="font-medium">Error:</span> {error}
        </div>
      )}
    </div>
  );
}
</file>

<file path="apps/web/src/components/Footer.tsx">
'use client';

export default function Footer() {
  return (
    <footer className="bg-slate-900 text-slate-400 mt-auto">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <div className="grid grid-cols-1 md:grid-cols-4 gap-8">
          <div className="col-span-1 md:col-span-2">
            <div className="flex items-center gap-2 mb-4">
              <div className="w-8 h-8 bg-gradient-to-br from-red-500 to-red-600 rounded-lg flex items-center justify-center">
                <svg className="w-5 h-5 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
                  <path strokeLinecap="round" strokeLinejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
                </svg>
              </div>
              <span className="text-xl font-bold text-white">
                PDF<span className="text-red-500">Converter</span>
              </span>
            </div>
            <p className="text-sm leading-relaxed max-w-sm">
              Every tool you need to use PDFs, at your fingertips. 
              Convert, merge, split, compress and edit your PDF files with incredible accuracy.
            </p>
          </div>

          <div>
            <h3 className="text-white font-semibold text-sm uppercase tracking-wider mb-4">Convert</h3>
            <ul className="space-y-2">
              {['PDF to Word', 'PDF to Excel', 'PDF to PowerPoint', 'Word to PDF'].map((tool) => (
                <li key={tool}>
                  <a href="#" className="text-sm hover:text-white transition-colors">{tool}</a>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h3 className="text-white font-semibold text-sm uppercase tracking-wider mb-4">Tools</h3>
            <ul className="space-y-2">
              {['Merge PDF', 'Split PDF', 'Compress PDF', 'Rotate PDF'].map((tool) => (
                <li key={tool}>
                  <a href="#" className="text-sm hover:text-white transition-colors">{tool}</a>
                </li>
              ))}
            </ul>
          </div>
        </div>

        <div className="border-t border-slate-800 mt-8 pt-8 flex flex-col sm:flex-row items-center justify-between gap-4">
          <p className="text-sm">© PDFConverter 2026 - Your PDF Editor</p>
          <div className="flex items-center gap-4">
            <a href="#" className="text-sm hover:text-white transition-colors">Privacy Policy</a>
            <a href="#" className="text-sm hover:text-white transition-colors">Terms of Service</a>
          </div>
        </div>
      </div>
    </footer>
  );
}
</file>

<file path="apps/web/src/components/Header.tsx">
'use client';

import Link from 'next/link';

export default function Header() {
  return (
    <header className="bg-white border-b border-slate-200 sticky top-0 z-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex items-center justify-between h-16">
          <Link href="/" className="flex items-center gap-2 group">
            <div className="w-8 h-8 bg-gradient-to-br from-red-500 to-red-600 rounded-lg flex items-center justify-center group-hover:scale-110 transition-transform">
              <svg className="w-5 h-5 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
                <path strokeLinecap="round" strokeLinejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
              </svg>
            </div>
            <span className="text-xl font-bold text-slate-800">
              PDF<span className="text-red-500">Converter</span>
            </span>
          </Link>

          <nav className="hidden md:flex items-center gap-6">
            <Link href="/" className="text-slate-600 hover:text-slate-900 font-medium text-sm transition-colors">
              All Tools
            </Link>
            <Link href="/pdf-to-word" className="text-slate-600 hover:text-slate-900 font-medium text-sm transition-colors">
              PDF to Word
            </Link>
            <div className="h-5 w-px bg-slate-200" />
            <button className="bg-slate-800 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-slate-700 transition-colors">
              Log In
            </button>
          </nav>

          <button className="md:hidden p-2 text-slate-600 hover:text-slate-900">
            <svg className="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
              <path strokeLinecap="round" strokeLinejoin="round" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
        </div>
      </div>
    </header>
  );
}
</file>

<file path="apps/web/src/components/PdfPagePreview.tsx">
'use client';

import { useState, useEffect } from 'react';

interface PageRange {
  id: string;
  from: number;
  to: number;
}

interface PdfPagePreviewProps {
  totalPages: number;
  ranges: PageRange[];
  rangeMode: 'custom' | 'fixed';
  fixedSize: number;
  splitMode: 'range' | 'pages' | 'size';
  pagesPerFile: number;
  accentColor: string;
  filePath: string;
  onRangeUpdate: (ranges: PageRange[]) => void;
}

const RANGE_COLORS = [
  '#E74C3C', '#3498DB', '#2ECC71', '#F39C12', '#9B59B6',
  '#1ABC9C', '#E67E22', '#34495E', '#16A085', '#C0392B',
  '#2980B9', '#27AE60', '#D35400', '#8E44AD', '#F1C40F',
];

export default function PdfPagePreview({
  totalPages,
  ranges,
  rangeMode,
  fixedSize,
  splitMode,
  pagesPerFile,
  accentColor,
  filePath,
  onRangeUpdate,
}: PdfPagePreviewProps) {
  const [pageImages, setPageImages] = useState<Record<number, string>>({});
  const [loadingPages, setLoadingPages] = useState<Set<number>>(new Set());

  // Fetch page images from the server
  useEffect(() => {
    if (!filePath || totalPages === 0) return;

    const fetchPageImages = async () => {
      const API_BASE = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001/api';
      const newImages: Record<number, string> = {};
      const newLoading = new Set<number>();

      // Only load first 50 pages for performance
      const pagesToLoad = Math.min(totalPages, 50);

      for (let p = 1; p <= pagesToLoad; p++) {
        if (pageImages[p]) {
          newImages[p] = pageImages[p];
          continue;
        }

        newLoading.add(p);
      }

      setLoadingPages(newLoading);

      // Fetch pages in batches of 5
      for (let batch = 0; batch < pagesToLoad; batch += 5) {
        const promises = [];
        for (let p = batch + 1; p <= Math.min(batch + 5, pagesToLoad); p++) {
          if (pageImages[p]) continue;

          promises.push(
            fetch(`${API_BASE}/split/render-page`, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ filePath, pageNum: p }),
            })
              .then(async (res) => {
                if (res.ok) {
                  const blob = await res.blob();
                  const url = URL.createObjectURL(blob);
                  newImages[p] = url;
                }
              })
              .catch(() => {
                // Ignore errors for individual pages
              })
          );
        }
        await Promise.all(promises);
        setPageImages((prev) => ({ ...prev, ...newImages }));
      }

      setLoadingPages(new Set());
    };

    fetchPageImages();

    // Cleanup URLs on unmount
    return () => {
      Object.values(pageImages).forEach((url) => {
        if (url.startsWith('blob:')) {
          URL.revokeObjectURL(url);
        }
      });
    };
  }, [filePath, totalPages]);

  const getPageRangeIndex = (pageNum: number): number => {
    if (splitMode !== 'range') return -1;

    if (rangeMode === 'custom') {
      for (let i = 0; i < ranges.length; i++) {
        if (pageNum >= ranges[i].from && pageNum <= ranges[i].to) return i;
      }
      return -1;
    } else {
      if (fixedSize <= 0) return -1;
      return Math.floor((pageNum - 1) / fixedSize);
    }
  };

  const getComputedRanges = (): PageRange[] => {
    if (splitMode === 'range' && rangeMode === 'custom') return ranges;

    if (splitMode === 'range' && rangeMode === 'fixed') {
      const computed: PageRange[] = [];
      if (fixedSize > 0) {
        for (let i = 0; i < totalPages; i += fixedSize) {
          computed.push({ id: `fixed-${i}`, from: i + 1, to: Math.min(i + fixedSize, totalPages) });
        }
      }
      return computed;
    }

    if (splitMode === 'pages') {
      const computed: PageRange[] = [];
      if (pagesPerFile > 0) {
        for (let i = 0; i < totalPages; i += pagesPerFile) {
          computed.push({ id: `pages-${i}`, from: i + 1, to: Math.min(i + pagesPerFile, totalPages) });
        }
      }
      return computed;
    }

    return [];
  };

  const computedRanges = getComputedRanges();

  // Group pages by range
  const groupedPages: Record<number, number[]> = {};
  const unassignedPages: number[] = [];

  for (let p = 1; p <= totalPages; p++) {
    const rangeIdx = getPageRangeIndex(p);
    if (rangeIdx >= 0) {
      if (!groupedPages[rangeIdx]) groupedPages[rangeIdx] = [];
      groupedPages[rangeIdx].push(p);
    } else if (splitMode === 'range' && rangeMode === 'custom') {
      unassignedPages.push(p);
    } else {
      // For fixed/pages mode, all pages belong to a range
      if (!groupedPages[0]) groupedPages[0] = [];
      groupedPages[0].push(p);
    }
  }

  const renderRangeGroup = (rangeIdx: number, pages: number[]) => {
    const color = RANGE_COLORS[rangeIdx % RANGE_COLORS.length];
    const range = computedRanges[rangeIdx];
    if (!range || pages.length === 0) return null;

    return (
      <div key={`range-${rangeIdx}`} className="mb-5">
        {/* Range header */}
        <div className="flex items-center gap-2 mb-2">
          <div className="w-3 h-3 rounded-full flex-shrink-0" style={{ backgroundColor: color }} />
          <span className="text-xs font-semibold" style={{ color }}>
            Range {rangeIdx + 1}
          </span>
          <span className="text-[10px] text-slate-400">
            pages {range.from}–{range.to} ({pages.length} page{pages.length !== 1 ? 's' : ''})
          </span>
        </div>

        {/* Page thumbnails */}
        <div className="flex flex-wrap gap-2 pl-5">
          {pages.map((pageNum) => (
            <div key={pageNum} className="group relative">
              <div
                className="w-[72px] rounded-lg border-2 overflow-hidden transition-all hover:scale-105 hover:shadow-lg cursor-default"
                style={{ borderColor: color }}
              >
                {/* Page image or placeholder */}
                <div className="h-[96px] bg-white flex items-center justify-center overflow-hidden">
                  {pageImages[pageNum] ? (
                    <img
                      src={pageImages[pageNum]}
                      alt={`Page ${pageNum}`}
                      className="w-full h-full object-contain"
                      loading="lazy"
                    />
                  ) : (
                    <div className="flex flex-col items-center justify-center w-full h-full" style={{ backgroundColor: `${color}08` }}>
                      {loadingPages.has(pageNum) ? (
                        <div className="w-5 h-5 border-2 border-t-transparent rounded-full animate-spin" style={{ borderColor: color, borderTopColor: 'transparent' }} />
                      ) : (
                        <span className="text-lg font-bold" style={{ color: `${color}60` }}>{pageNum}</span>
                      )}
                    </div>
                  )}
                </div>

                {/* Page number label */}
                <div className="py-1 text-center" style={{ backgroundColor: `${color}10` }}>
                  <span className="text-[10px] font-medium" style={{ color }}>{pageNum}</span>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    );
  };

  const renderUnassignedPages = (pages: number[]) => {
    if (pages.length === 0) return null;

    return (
      <div className="mb-5">
        <div className="flex items-center gap-2 mb-2">
          <div className="w-3 h-3 rounded-full bg-slate-300 flex-shrink-0" />
          <span className="text-xs font-semibold text-slate-400">Unassigned</span>
          <span className="text-[10px] text-slate-300">{pages.length} pages</span>
        </div>
        <div className="flex flex-wrap gap-2 pl-5">
          {pages.map((pageNum) => (
            <div key={`unassigned-${pageNum}`}>
              <div className="w-[72px] rounded-lg border-2 border-dashed border-slate-300 overflow-hidden">
                <div className="h-[96px] bg-slate-50 flex items-center justify-center">
                  {pageImages[pageNum] ? (
                    <img src={pageImages[pageNum]} alt={`Page ${pageNum}`} className="w-full h-full object-contain opacity-50" loading="lazy" />
                  ) : (
                    <span className="text-lg font-bold text-slate-300">{pageNum}</span>
                  )}
                </div>
                <div className="py-1 text-center bg-slate-50">
                  <span className="text-[10px] text-slate-400">{pageNum}</span>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    );
  };

  return (
    <div className="overflow-y-auto max-h-[65vh] pr-1">
      {Object.keys(groupedPages)
        .sort((a, b) => parseInt(a) - parseInt(b))
        .map((rangeIdx) => renderRangeGroup(parseInt(rangeIdx), groupedPages[parseInt(rangeIdx)]))}
      {renderUnassignedPages(unassignedPages)}

      {totalPages === 0 && (
        <div className="text-center py-12 text-slate-400 text-sm">
          Upload a PDF to preview pages
        </div>
      )}

      {totalPages > 50 && (
        <p className="text-[10px] text-slate-400 text-center py-2">
          Showing first 50 pages of {totalPages}
        </p>
      )}
    </div>
  );
}
</file>

<file path="apps/web/src/components/ToolCard.tsx">
'use client';

import Link from 'next/link';

interface ToolCardProps {
  title: string;
  description: string;
  icon: string;
  color: string;
  href: string;
  index?: number;
}

function ToolIcon({ icon, color }: { icon: string; color: string }) {
  const iconMap: Record<string, JSX.Element> = {
    'file-word': (
      <svg className="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth={1.5}>
        <path strokeLinecap="round" strokeLinejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
        <path strokeLinecap="round" strokeLinejoin="round" d="M9 13h6M9 9h6M9 17h4" />
      </svg>
    ),
    'file-excel': (
      <svg className="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth={1.5}>
        <path strokeLinecap="round" strokeLinejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
        <path strokeLinecap="round" strokeLinejoin="round" d="M8 11l2 2 4-4" />
      </svg>
    ),
    'file-powerpoint': (
      <svg className="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth={1.5}>
        <path strokeLinecap="round" strokeLinejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
        <circle cx="12" cy="14" r="3" />
      </svg>
    ),
    'file-pdf': (
      <svg className="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth={1.5}>
        <path strokeLinecap="round" strokeLinejoin="round" d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
        <path strokeLinecap="round" strokeLinejoin="round" d="M9 12h6M9 8h6M9 16h4" />
      </svg>
    ),
    'merge': (
      <svg className="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth={1.5}>
        <path strokeLinecap="round" strokeLinejoin="round" d="M8 4H5a2 2 0 00-2 2v3m18 0V6a2 2 0 00-2-2h-3m0 14h3a2 2 0 002-2v-3M3 16v3a2 2 0 002 2h3" />
        <path strokeLinecap="round" strokeLinejoin="round" d="M9 12h6M12 9v6" />
      </svg>
    ),
    'split': (
      <svg className="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth={1.5}>
        <path strokeLinecap="round" strokeLinejoin="round" d="M8 7V3m8 4V3m-8 18v-4m8 4v-4M3 8h4m10 0h4M3 16h4m10 0h4" />
        <path strokeLinecap="round" strokeLinejoin="round" d="M12 4v16" strokeDasharray="3 3" />
      </svg>
    ),
    'compress': (
      <svg className="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth={1.5}>
        <path strokeLinecap="round" strokeLinejoin="round" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
        <path strokeLinecap="round" strokeLinejoin="round" d="M5 3h14" />
      </svg>
    ),
    'rotate': (
      <svg className="w-8 h-8" viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth={1.5}>
        <path strokeLinecap="round" strokeLinejoin="round" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
      </svg>
    ),
  };

  return iconMap[icon] || iconMap['file-pdf'];
}

export default function ToolCard({ title, description, icon, color, href, index = 0 }: ToolCardProps) {
  return (
    <Link 
      href={href}
      className={`group relative bg-white rounded-2xl p-6 shadow-sm border border-slate-200 hover:shadow-xl hover:border-transparent hover:-translate-y-1 transition-all duration-300 animate-fade-in-up stagger-${index + 1}`}
      style={{
        ['--tool-color' as string]: color,
      }}
    >
      <div 
        className="absolute inset-0 rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-300 -z-10"
        style={{ background: `${color}15` }}
      />
      
      <div 
        className="w-14 h-14 rounded-xl flex items-center justify-center mb-4 group-hover:scale-110 transition-transform duration-300"
        style={{ backgroundColor: `${color}15` }}
      >
        <ToolIcon icon={icon} color={color} />
      </div>

      <h3 className="text-lg font-semibold text-slate-800 mb-1 group-hover:text-slate-900">
        {title}
      </h3>
      <p className="text-sm text-slate-500 leading-relaxed">
        {description}
      </p>

      <div className="absolute top-6 right-6 opacity-0 group-hover:opacity-100 transition-opacity duration-300">
        <svg className="w-5 h-5 text-slate-400 group-hover:text-slate-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
          <path strokeLinecap="round" strokeLinejoin="round" d="M9 5l7 7-7 7" />
        </svg>
      </div>
    </Link>
  );
}
</file>

<file path="apps/web/src/hooks/useConversion.ts">
'use client';

import { useState, useCallback, useRef, useEffect } from 'react';
import { apiClient } from '@/lib/api';

interface UseConversionOptions {
  outputFormat?: string;
}

interface UseConversionReturn {
  file: File | null;
  status: string;
  progress: number;
  conversionId: string | null;
  error: string | null;
  isProcessing: boolean;
  fileName: string;
  fileSize: number;
  handleFileSelect: (file: File) => void;
  handleCloudFileReady: (file: { filePath: string; fileName: string; fileSize: number }) => void;
  reset: () => void;
}

export function useConversion(options: UseConversionOptions = {}): UseConversionReturn {
  const outputFormat = options.outputFormat || 'docx';

  const [file, setFile] = useState<File | null>(null);
  const [fileName, setFileName] = useState<string>('');
  const [fileSize, setFileSize] = useState<number>(0);
  const [status, setStatus] = useState<string>('');
  const [progress, setProgress] = useState<number>(0);
  const [conversionId, setConversionId] = useState<string | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [isProcessing, setIsProcessing] = useState(false);
  const pollingRef = useRef<NodeJS.Timeout | null>(null);

  const clearPolling = useCallback(() => {
    if (pollingRef.current) {
      clearInterval(pollingRef.current);
      pollingRef.current = null;
    }
  }, []);

  const startPollingAndConvert = useCallback((id: string) => {
    setConversionId(id);
    setStatus('PROCESSING');
    setProgress(10);

    // Start the conversion
    apiClient.startConversion(id).catch((err) => {
      console.error('Start conversion error:', err);
    });

    // Poll for status
    clearPolling();
    pollingRef.current = setInterval(async () => {
      try {
        const result = await apiClient.getConversionStatus(id);
        setStatus(result.status);
        setProgress(result.progress);

        if (result.status === 'COMPLETED' || result.status === 'FAILED') {
          clearPolling();
          setIsProcessing(false);
          if (result.status === 'FAILED' && result.errorMessage) {
            setError(result.errorMessage);
          }
        }
      } catch (err: any) {
        console.error('Polling error:', err);
        setError(err.message || 'Failed to check conversion status');
        clearPolling();
        setIsProcessing(false);
      }
    }, 2000);
  }, [clearPolling]);

  const handleFileSelect = useCallback(async (selectedFile: File) => {
    setFile(selectedFile);
    setFileName(selectedFile.name);
    setFileSize(selectedFile.size);
    setError(null);
    setStatus('UPLOADING');
    setProgress(0);
    setIsProcessing(true);

    try {
      const uploadResult = await apiClient.uploadFile(selectedFile, outputFormat);
      setStatus('UPLOADED');
      setProgress(5);
      startPollingAndConvert(uploadResult.id);
    } catch (err: any) {
      console.error('Upload error:', err);
      setError(err.message || 'An error occurred during upload');
      setStatus('FAILED');
      setIsProcessing(false);
    }
  }, [outputFormat, startPollingAndConvert]);

  const handleCloudFileReady = useCallback(async (cloudFile: { filePath: string; fileName: string; fileSize: number }) => {
    setFile(null);
    setFileName(cloudFile.fileName);
    setFileSize(cloudFile.fileSize);
    setError(null);
    setStatus('PROCESSING');
    setProgress(5);
    setIsProcessing(true);

    try {
      // Create conversion record from server path
      const result = await apiClient.createConversionFromPath({
        filePath: cloudFile.filePath,
        fileName: cloudFile.fileName,
        fileSize: cloudFile.fileSize,
        outputFormat,
      });

      startPollingAndConvert(result.id);
    } catch (err: any) {
      console.error('Cloud conversion error:', err);
      setError(err.message || 'An error occurred during conversion');
      setStatus('FAILED');
      setIsProcessing(false);
    }
  }, [outputFormat, startPollingAndConvert]);

  const reset = useCallback(() => {
    clearPolling();
    setFile(null);
    setFileName('');
    setFileSize(0);
    setStatus('');
    setProgress(0);
    setConversionId(null);
    setError(null);
    setIsProcessing(false);
  }, [clearPolling]);

  useEffect(() => {
    return () => { clearPolling(); };
  }, [clearPolling]);

  return {
    file,
    status,
    progress,
    conversionId,
    error,
    isProcessing,
    fileName,
    fileSize,
    handleFileSelect,
    handleCloudFileReady,
    reset,
  };
}
</file>

<file path="apps/web/src/hooks/useMerge.ts">
'use client';

import { useState, useCallback, useRef, useEffect } from 'react';
import { apiClient } from '@/lib/api';

export interface MergeFile {
  id: string;
  file: File | null;       // null for cloud files
  filePath: string;         // server path for cloud files, empty for local
  fileName: string;
  fileSize: number;
  order: number;
  isCloud: boolean;         // true = file already on server
}

interface UseMergeReturn {
  files: MergeFile[];
  status: string;
  progress: number;
  mergeJobId: string | null;
  error: string | null;
  isProcessing: boolean;
  addFiles: (newFiles: File[]) => void;
  addCloudFiles: (cloudFiles: Array<{ filePath: string; fileName: string; fileSize: number }>) => void;
  removeFile: (id: string) => void;
  reorderFiles: (fromIndex: number, toIndex: number) => void;
  startMerge: () => Promise<void>;
  reset: () => void;
}

export function useMerge(): UseMergeReturn {
  const [files, setFiles] = useState<MergeFile[]>([]);
  const [status, setStatus] = useState<string>('IDLE');
  const [progress, setProgress] = useState<number>(0);
  const [mergeJobId, setMergeJobId] = useState<string | null>(null);
  const [error, setError] = useState<string | null>(null);
  const [isProcessing, setIsProcessing] = useState(false);
  const pollingRef = useRef<NodeJS.Timeout | null>(null);

  const clearPolling = useCallback(() => {
    if (pollingRef.current) {
      clearInterval(pollingRef.current);
      pollingRef.current = null;
    }
  }, []);

  // Add local files (from file picker / drag & drop)
  const addFiles = useCallback((newFiles: File[]) => {
    setFiles((prev) => {
      const updated = [...prev];
      let nextOrder = prev.length;

      newFiles.forEach((file) => {
        if (file.type !== 'application/pdf') return;
        if (file.size > 50 * 1024 * 1024) return;

        updated.push({
          id: `local-${file.name}-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`,
          file,
          filePath: '',
          fileName: file.name,
          fileSize: file.size,
          order: nextOrder++,
          isCloud: false,
        });
      });

      return updated;
    });
  }, []);

  // Add cloud files (already on server — no re-upload needed)
  const addCloudFiles = useCallback((cloudFiles: Array<{ filePath: string; fileName: string; fileSize: number }>) => {
    setFiles((prev) => {
      const updated = [...prev];
      let nextOrder = prev.length;

      cloudFiles.forEach((cf) => {
        updated.push({
          id: `cloud-${cf.fileName}-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`,
          file: null,
          filePath: cf.filePath,
          fileName: cf.fileName,
          fileSize: cf.fileSize,
          order: nextOrder++,
          isCloud: true,
        });
      });

      return updated;
    });
  }, []);

  const removeFile = useCallback((id: string) => {
    setFiles((prev) => {
      const filtered = prev.filter((f) => f.id !== id);
      return filtered.map((f, i) => ({ ...f, order: i }));
    });
  }, []);

  const reorderFiles = useCallback((fromIndex: number, toIndex: number) => {
    setFiles((prev) => {
      const updated = [...prev];
      const [moved] = updated.splice(fromIndex, 1);
      updated.splice(toIndex, 0, moved);
      return updated.map((f, i) => ({ ...f, order: i }));
    });
  }, []);

  const startMerge = useCallback(async () => {
    if (files.length < 2) {
      setError('At least 2 files are required to merge');
      return;
    }

    setIsProcessing(true);
    setStatus('UPLOADING');
    setProgress(0);
    setError(null);

    try {
      // Separate local files (need upload) from cloud files (already on server)
      const localFiles = files.filter((f) => !f.isCloud && f.file);
      const cloudFiles = files.filter((f) => f.isCloud && f.filePath);

      let uploadedFiles: Array<{ filePath: string; fileName: string; fileSize: number }> = [];

      // Upload local files if any
      if (localFiles.length > 0) {
        const uploadResult = await apiClient.uploadMergeFiles(
          localFiles.map((f) => f.file!)
        );

        // Map uploaded files — use server paths
        uploadedFiles = localFiles.map((f, index) => ({
          filePath: uploadResult.files[index]?.filePath || '',
          fileName: f.fileName,
          fileSize: f.fileSize,
        }));
      }

      // Combine uploaded local files + cloud files (already have server paths)
      const allFiles = [...uploadedFiles, ...cloudFiles.map((f) => ({
        filePath: f.filePath,
        fileName: f.fileName,
        fileSize: f.fileSize,
      }))];

      setStatus('MERGING');
      setProgress(20);

      // Start merge
      const mergeResult = await apiClient.startMerge(allFiles);

      setMergeJobId(mergeResult.id);
      setStatus('PROCESSING');
      setProgress(30);

      // Start polling
      clearPolling();
      pollingRef.current = setInterval(async () => {
        try {
          const result = await apiClient.getMergeStatus(mergeResult.id);
          setStatus(result.status);
          setProgress(result.progress);

          if (result.status === 'COMPLETED' || result.status === 'FAILED') {
            clearPolling();
            setIsProcessing(false);
            if (result.status === 'FAILED' && result.errorMessage) {
              setError(result.errorMessage);
            }
          }
        } catch (err: any) {
          console.error('Polling error:', err);
          clearPolling();
          setIsProcessing(false);
          setError(err.message);
        }
      }, 2000);
    } catch (err: any) {
      console.error('Merge error:', err);
      setError(err.message || 'An error occurred during merge');
      setStatus('FAILED');
      setIsProcessing(false);
    }
  }, [files, clearPolling]);

  const reset = useCallback(() => {
    clearPolling();
    setFiles([]);
    setStatus('IDLE');
    setProgress(0);
    setMergeJobId(null);
    setError(null);
    setIsProcessing(false);
  }, [clearPolling]);

  useEffect(() => {
    return () => {
      clearPolling();
    };
  }, [clearPolling]);

  return {
    files,
    status,
    progress,
    mergeJobId,
    error,
    isProcessing,
    addFiles,
    addCloudFiles,
    removeFile,
    reorderFiles,
    startMerge,
    reset,
  };
}
</file>

<file path="apps/web/src/lib/api.ts">
const API_BASE = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001/api';

class ApiClient {
  private baseUrl: string;
  constructor(baseUrl: string) { this.baseUrl = baseUrl; }

  async uploadFile(file: File, outputFormat: string = 'docx'): Promise<any> {
    const formData = new FormData();
    formData.append('file', file);
    formData.append('outputFormat', outputFormat);
    const response = await fetch(`${this.baseUrl}/upload`, { method: 'POST', body: formData });
    if (!response.ok) { const error = await response.json().catch(() => ({ message: 'Upload failed' })); throw new Error(error.message); }
    return response.json();
  }

  async startConversion(id: string): Promise<any> {
    const response = await fetch(`${this.baseUrl}/convert/${id}`, { method: 'POST' });
    if (!response.ok) { const error = await response.json().catch(() => ({ message: 'Conversion failed' })); throw new Error(error.message); }
    return response.json();
  }

  async getConversionStatus(id: string): Promise<any> {
    const response = await fetch(`${this.baseUrl}/convert/status/${id}`);
    if (!response.ok) { const error = await response.json().catch(() => ({ message: 'Status check failed' })); throw new Error(error.message); }
    return response.json();
  }

  getDownloadUrl(id: string): string { return `${this.baseUrl}/download/${id}`; }

  async uploadMergeFiles(files: File[]): Promise<any> {
    const formData = new FormData();
    files.forEach((file) => formData.append('files', file));
    const response = await fetch(`${this.baseUrl}/merge/upload`, { method: 'POST', body: formData });
    if (!response.ok) { const error = await response.json().catch(() => ({ message: 'Upload failed' })); throw new Error(error.message); }
    return response.json();
  }

  async startMerge(files: Array<{ filePath: string; fileName: string; fileSize: number }>): Promise<any> {
    const response = await fetch(`${this.baseUrl}/merge`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ files }) });
    if (!response.ok) { const error = await response.json().catch(() => ({ message: 'Merge failed' })); throw new Error(error.message); }
    return response.json();
  }

  async getMergeStatus(id: string): Promise<any> {
    const response = await fetch(`${this.baseUrl}/merge/status/${id}`);
    if (!response.ok) { const error = await response.json().catch(() => ({ message: 'Status check failed' })); throw new Error(error.message); }
    return response.json();
  }

  getMergeDownloadUrl(id: string): string { return `${this.baseUrl}/merge/download/${id}`; }

  async createConversionFromPath(data: any): Promise<any> {
    const response = await fetch(`${this.baseUrl}/upload/convert-from-path`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) });
    if (!response.ok) { const error = await response.json().catch(() => ({ message: 'Failed' })); throw new Error(error.message); }
    return response.json();
  }

  // ===== Split PDF =====

  async uploadSplitFile(file: File): Promise<any> {
    const formData = new FormData();
    formData.append('file', file);
    const response = await fetch(`${this.baseUrl}/split/upload`, { method: 'POST', body: formData });
    if (!response.ok) { const error = await response.json().catch(() => ({ message: 'Upload failed' })); throw new Error(error.message); }
    return response.json();
  }

  async analyzeSplitFile(filePath: string): Promise<any> {
    const response = await fetch(`${this.baseUrl}/split/analyze`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ filePath }) });
    if (!response.ok) { const error = await response.json().catch(() => ({ message: 'Analyze failed' })); throw new Error(error.message); }
    return response.json();
  }

  async startSplit(data: any): Promise<any> {
    const response = await fetch(`${this.baseUrl}/split`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) });
    if (!response.ok) { const error = await response.json().catch(() => ({ message: 'Split failed' })); throw new Error(error.message); }
    return response.json();
  }

  async getSplitStatus(id: string): Promise<any> {
    const response = await fetch(`${this.baseUrl}/split/status/${id}`);
    if (!response.ok) { const error = await response.json().catch(() => ({ message: 'Status check failed' })); throw new Error(error.message); }
    return response.json();
  }

  getSplitDownloadUrl(jobId: string, fileIndex: number): string { return `${this.baseUrl}/split/download/${jobId}/${fileIndex}`; }

  getSplitDownloadAllUrl(jobId: string): string { return `${this.baseUrl}/split/download-all/${jobId}`; }
}

export const apiClient = new ApiClient(API_BASE);
</file>

<file path="apps/web/src/lib/cloud-storage.ts">
const API_BASE = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001/api';

// ========== Script Loader ==========

function loadScript(src: string): Promise<void> {
  return new Promise((resolve, reject) => {
    if (document.querySelector(`script[src="${src}"]`)) {
      resolve();
      return;
    }
    const script = document.createElement('script');
    script.src = src;
    script.async = true;
    script.onload = () => resolve();
    script.onerror = () => reject(new Error(`Failed to load script: ${src}`));
    document.head.appendChild(script);
  });
}

// ========== Google Drive Pre-load ==========

let googleLoaded = false;
let googleLoading: Promise<void> | null = null;

export async function preloadGoogleSDK(): Promise<void> {
  if (googleLoaded) return;
  if (googleLoading) return googleLoading;

  googleLoading = Promise.all([
    loadScript('https://apis.google.com/js/api.js'),
    loadScript('https://accounts.google.com/gsi/client'),
  ]).then(async () => {
    await new Promise<void>((resolve, reject) => {
      window.gapi.load('picker', {
        callback: () => { googleLoaded = true; resolve(); },
        onerror: () => reject(new Error('Failed to load Google Picker API')),
      });
    });
  }).catch((err) => {
    console.warn('Failed to pre-load Google SDK:', err);
    googleLoading = null;
  });

  return googleLoading;
}

// ========== Type Declarations ==========

declare global {
  interface Window {
    gapi: any;
    google: any;
  }
}

// ========== Google Drive Picker ==========

export async function openGoogleDrivePicker(
  clientId: string,
  apiKey: string,
  multiSelect: boolean = false
): Promise<Array<{ fileId: string; fileName: string; accessToken: string }>> {
  await preloadGoogleSDK();

  const accessToken = await new Promise<string>((resolve, reject) => {
    const tokenClient = window.google.accounts.oauth2.initTokenClient({
      client_id: clientId,
      scope: 'https://www.googleapis.com/auth/drive.readonly',
      callback: (response: any) => {
        if (response.error) {
          reject(new Error(response.error));
          return;
        }
        resolve(response.access_token);
      },
      error_callback: (error: any) => {
        reject(new Error(error.message || 'Google auth failed'));
      },
    });
    tokenClient.requestAccessToken({ prompt: '' });
  });

  const files = await new Promise<Array<{ fileId: string; fileName: string; accessToken: string }>>((resolve, reject) => {
    const view = new window.google.picker.View(window.google.picker.ViewId.DOCS);
    view.setMimeTypes('application/pdf');

    let builder = new window.google.picker.PickerBuilder()
      .addView(view)
      .setOAuthToken(accessToken)
      .setDeveloperKey(apiKey)
      .setCallback((data: any) => {
        if (data.action === window.google.picker.Action.PICKED) {
          const selected = data.docs.map((doc: any) => ({
            fileId: doc.id,
            fileName: doc.name,
            accessToken,
          }));
          resolve(selected);
        } else if (data.action === window.google.picker.Action.CANCEL) {
          resolve([]);
        }
      });

    if (multiSelect) {
      builder = builder.enableFeature(window.google.picker.Feature.MULTISELECT_ENABLED);
    }

    builder.build().setVisible(true);
  });

  return files;
}

// ========== Dropbox OAuth Redirect Flow ==========

export function openDropboxOAuth(multiSelect: boolean = false): Promise<Array<{ path: string; fileName: string; accessToken: string }>> {
  return new Promise((resolve, reject) => {
    fetch(`${API_BASE}/dropbox-auth/url`)
      .then((res) => res.json())
      .then((data) => {
        if (!data.authUrl) {
          reject(new Error('Failed to get Dropbox auth URL. Is DROPBOX_CLIENT_ID configured?'));
          return;
        }

        const modeParam = `&mode=${multiSelect ? 'multi' : 'single'}`;
        const authUrl = data.authUrl + modeParam;
        const width = 600;
        const height = 700;
        const left = window.screenX + (window.outerWidth - width) / 2;
        const top = window.screenY + (window.outerHeight - height) / 2;

        const authWindow = window.open(
          authUrl,
          'dropbox-auth',
          `width=${width},height=${height},left=${left},top=${top},toolbar=no,menubar=no`
        );

        if (!authWindow) {
          reject(new Error('Popup blocked. Please allow popups for this site.'));
          return;
        }

        const handleMessage = (event: MessageEvent) => {
          if (event.data?.type === 'dropbox-file-selected') {
            window.removeEventListener('message', handleMessage);
            const files = event.data.files.map((f: any) => ({
              path: f.path,
              fileName: f.name,
              accessToken: f.accessToken,
            }));
            resolve(files);
          }
        };

        window.addEventListener('message', handleMessage);

        setTimeout(() => {
          window.removeEventListener('message', handleMessage);
          reject(new Error('Dropbox selection timed out'));
        }, 300000);
      })
      .catch((err) => reject(err));
  });
}

// ========== Server Proxy Downloads ==========

export async function downloadFromDrive(
  accessToken: string,
  fileId: string,
  fileName: string
): Promise<{ filePath: string; fileName: string; fileSize: number }> {
  const response = await fetch(`${API_BASE}/cloud/download/drive`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ accessToken, fileId, fileName }),
  });

  if (!response.ok) {
    const error = await response.json().catch(() => ({ message: 'Download failed' }));
    throw new Error(error.message || `Download failed: ${response.status}`);
  }

  return response.json();
}

export async function downloadFromDropbox(
  accessToken: string,
  filePath: string,
  fileName: string
): Promise<{ filePath: string; fileName: string; fileSize: number }> {
  const response = await fetch(`${API_BASE}/dropbox-auth/download`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ accessToken, path: filePath }),
  });

  if (!response.ok) {
    const error = await response.json().catch(() => ({ message: 'Download failed' }));
    throw new Error(error.message || `Download failed: ${response.status}`);
  }

  return response.json();
}

// ========== Configuration Check ==========

export function getCloudConfig() {
  return {
    googleClientId: process.env.NEXT_PUBLIC_GOOGLE_CLIENT_ID || '',
    googleApiKey: process.env.NEXT_PUBLIC_GOOGLE_API_KEY || '',
    dropboxEnabled: true, // Always enabled now — uses OAuth redirect, no SDK needed
    googleDriveEnabled: !!(process.env.NEXT_PUBLIC_GOOGLE_CLIENT_ID && process.env.NEXT_PUBLIC_GOOGLE_API_KEY),
  };
}
</file>

<file path="apps/web/.env.local">
NEXT_PUBLIC_API_URL=http://localhost:3001/api
NEXT_PUBLIC_GOOGLE_CLIENT_ID=406612409410-c8qjli2g6ct4bp5ig160n8dp3kvs6fam.apps.googleusercontent.com
NEXT_PUBLIC_GOOGLE_API_KEY=AIzaSyBePxn8ziso0rWo0j_TvSM8H2qn0FqT8KY
NEXT_PUBLIC_DROPBOX_APP_KEY=tm9jcjkvfatvqm6
</file>

<file path="apps/web/next-env.d.ts">
/// <reference types="next" />
/// <reference types="next/image-types/global" />

// NOTE: This file should not be edited
// see https://nextjs.org/docs/app/building-your-application/configuring/typescript for more information.
</file>

<file path="apps/web/next.config.js">
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {
    NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001/api',
    NEXT_PUBLIC_GOOGLE_CLIENT_ID: process.env.NEXT_PUBLIC_GOOGLE_CLIENT_ID || '',
    NEXT_PUBLIC_GOOGLE_API_KEY: process.env.NEXT_PUBLIC_GOOGLE_API_KEY || '',
    NEXT_PUBLIC_DROPBOX_APP_KEY: process.env.NEXT_PUBLIC_DROPBOX_APP_KEY || '',
  },
};

module.exports = nextConfig;
</file>

<file path="apps/web/package.json">
{
  "name": "@pdf-converter/web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3000",
    "build": "next build",
    "start": "next start",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@pdf-converter/shared": "*",
    "next": "^14.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.4.0",
    "@testing-library/react": "^14.2.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "@types/react": "^18.2.48",
    "@types/react-dom": "^18.2.18",
    "autoprefixer": "^10.4.17",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "postcss": "^8.4.33",
    "tailwindcss": "^3.4.1",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
</file>

<file path="apps/web/postcss.config.js">
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
</file>

<file path="apps/web/tailwind.config.js">
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#eff6ff',
          100: '#dbeafe',
          200: '#bfdbfe',
          300: '#93c5fd',
          400: '#60a5fa',
          500: '#3b82f6',
          600: '#2563eb',
          700: '#1d4ed8',
          800: '#1e40af',
          900: '#1e3a8a',
        },
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', '-apple-system', 'sans-serif'],
      },
    },
  },
  plugins: [],
};
</file>

<file path="apps/web/tsconfig.json">
{
  "compilerOptions": {
    "target": "es5",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
</file>

<file path="packages/shared/src/constants.ts">
export const MAX_FILE_SIZE = 50 * 1024 * 1024; // 50MB
export const ALLOWED_MIME_TYPES = ['application/pdf'];
export const ALLOWED_EXTENSIONS = ['.pdf'];
export const UPLOAD_DIR = './uploads';
export const OUTPUT_DIR = './outputs';

export const API_ROUTES = {
  UPLOAD: '/api/upload',
  CONVERT: (id: string) => `/api/convert/${id}`,
  STATUS: (id: string) => `/api/status/${id}`,
  DOWNLOAD: (id: string) => `/api/download/${id}`,
} as const;

export const TOOLS = [
  {
    id: 'pdf-to-word',
    title: 'PDF to Word',
    description: 'Easily convert your PDF files into easy to edit DOC and DOCX documents.',
    icon: 'file-word',
    color: '#2B579A',
    href: '/pdf-to-word',
  },
  {
    id: 'pdf-to-excel',
    title: 'PDF to Excel',
    description: 'Pull data straight from PDFs into Excel spreadsheets.',
    icon: 'file-excel',
    color: '#217346',
    href: '/pdf-to-excel',
  },
  {
    id: 'pdf-to-ppt',
    title: 'PDF to PowerPoint',
    description: 'Convert your PDFs to PowerPoint presentations.',
    icon: 'file-powerpoint',
    color: '#D24726',
    href: '/pdf-to-ppt',
  },
  {
    id: 'word-to-pdf',
    title: 'Word to PDF',
    description: 'Make DOC and DOCX files easy to read by converting them to PDF.',
    icon: 'file-pdf',
    color: '#E74C3C',
    href: '/word-to-pdf',
  },
  {
    id: 'merge-pdf',
    title: 'Merge PDF',
    description: 'Combine PDFs in the order you want with the easiest PDF merger available.',
    icon: 'merge',
    color: '#E67E22',
    href: '/merge-pdf',
  },
  {
    id: 'split-pdf',
    title: 'Split PDF',
    description: 'Separate one page or a whole set for easy conversion into independent PDF files.',
    icon: 'split',
    color: '#9B59B6',
    href: '/split-pdf',
  },
  {
    id: 'compress-pdf',
    title: 'Compress PDF',
    description: 'Reduce file size while optimizing for maximal PDF quality.',
    icon: 'compress',
    color: '#1ABC9C',
    href: '/compress-pdf',
  },
  {
    id: 'rotate-pdf',
    title: 'Rotate PDF',
    description: 'Rotate your PDFs the way you need them.',
    icon: 'rotate',
    color: '#3498DB',
    href: '/rotate-pdf',
  },
] as const;
</file>

<file path="packages/shared/src/index.ts">
export * from './types';
export * from './constants';
</file>

<file path="packages/shared/src/types.ts">
export enum ConversionStatus {
  PENDING = 'PENDING',
  PROCESSING = 'PROCESSING',
  COMPLETED = 'COMPLETED',
  FAILED = 'FAILED',
}

export interface Conversion {
  id: string;
  userId?: string;
  originalName: string;
  originalSize: number;
  originalPath: string;
  outputPath?: string;
  status: ConversionStatus;
  progress: number;
  errorMessage?: string;
  createdAt: string;
  updatedAt: string;
}

export interface UploadResponse {
  id: string;
  originalName: string;
  originalSize: number;
  status: ConversionStatus;
}

export interface ConversionStatusResponse {
  id: string;
  status: ConversionStatus;
  progress: number;
  errorMessage?: string;
  downloadUrl?: string;
}

export interface ApiError {
  message: string;
  statusCode: number;
  details?: string;
}
</file>

<file path="packages/shared/package.json">
{
  "name": "@pdf-converter/shared",
  "version": "1.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts"
}
</file>

<file path="prisma/schema.prisma">
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Conversion {
  id           String   @id @default(cuid())
  userId       String?
  originalName String
  originalSize Int
  originalPath String
  outputPath   String?
  outputFormat String   @default("docx")
  status       String   @default("PENDING")
  progress     Int      @default(0)
  errorMessage String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@index([status])
  @@index([createdAt])
  @@map("conversions")
}

model MergeJob {
  id           String      @id @default(cuid())
  userId       String?
  status       String      @default("PENDING")
  progress     Int         @default(0)
  outputPath   String?
  errorMessage String?
  fileCount    Int         @default(0)
  createdAt    DateTime    @default(now())
  updatedAt    DateTime    @updatedAt
  files        MergeFile[]

  @@index([status])
  @@index([createdAt])
  @@map("merge_jobs")
}

model MergeFile {
  id         String    @id @default(cuid())
  mergeJobId String
  mergeJob   MergeJob  @relation(fields: [mergeJobId], references: [id], onDelete: Cascade)
  filePath   String
  fileName   String
  fileSize   Int
  order      Int

  @@map("merge_files")
}

model SplitJob {
  id           String   @id @default(cuid())
  originalPath String
  originalName String
  fileSize     Int
  totalPages   Int      @default(0)
  splitMode    String   @default("range")
  ranges       String?
  fixedSize    Int?
  sizeLimit    Int?
  pagesPerFile Int?
  status       String   @default("PENDING")
  progress     Int      @default(0)
  outputFiles  String?
  errorMessage String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@index([status])
  @@index([createdAt])
  @@map("split_jobs")
}
</file>

<file path=".gitignore">
node_modules/
.env
.next/
dist/
uploads/
*.log
.DS_Store
coverage/
</file>

<file path="convert_to_excel.py">
#!/usr/bin/env python3
"""PDF to Excel conversion script using pdfplumber + openpyxl."""

import sys
import os

def convert_pdf_to_excel(input_path, output_path):
    try:
        import pdfplumber
        from openpyxl import Workbook
        
        wb = Workbook()
        ws = wb.active
        ws.title = "Extracted Data"
        
        total_rows = 0
        
        with pdfplumber.open(input_path) as pdf:
            total_pages = len(pdf.pages)
            
            for page_num, page in enumerate(pdf.pages):
                # Try to extract tables first
                tables = page.extract_tables()
                
                if tables:
                    for table in tables:
                        if not table:
                            continue
                        
                        # Add a page separator row
                        if total_rows > 0:
                            total_rows += 1
                        
                        for row in table:
                            if row:
                                # Replace None with empty string
                                cleaned_row = [str(cell) if cell is not None else '' for cell in row]
                                ws.append(cleaned_row)
                                total_rows += 1
                else:
                    # No tables found — extract text and put it line by line
                    text = page.extract_text()
                    if text:
                        if total_rows > 0:
                            total_rows += 1
                        
                        for line in text.split('\n'):
                            # Split by multiple spaces to create columns
                            columns = [col.strip() for col in line.split('  ') if col.strip()]
                            if columns:
                                ws.append(columns)
                                total_rows += 1
                            elif line.strip():
                                ws.append([line.strip()])
                                total_rows += 1
        
        if total_rows == 0:
            # Try extracting all text as a fallback
            with pdfplumber.open(input_path) as pdf:
                for page in pdf.pages:
                    text = page.extract_text()
                    if text:
                        for line in text.split('\n'):
                            ws.append([line])
                            total_rows += 1
        
        if total_rows == 0:
            print("WARNING: No data extracted from PDF")
            # Still save an empty workbook
            ws.append(["No data could be extracted from this PDF"])
        
        # Auto-adjust column widths
        for col in ws.columns:
            max_length = 0
            column_letter = col[0].column_letter
            for cell in col:
                try:
                    if cell.value:
                        max_length = max(max_length, len(str(cell.value)))
                except:
                    pass
            adjusted_width = min(max_length + 2, 50)
            ws.column_dimensions[column_letter].width = adjusted_width
        
        wb.save(output_path)
        
        if os.path.exists(output_path) and os.path.getsize(output_path) > 0:
            print(f"SUCCESS: {output_path} ({total_rows} rows)")
            sys.exit(0)
        else:
            print("ERROR: Output file not created or empty")
            sys.exit(1)
            
    except Exception as e:
        print(f"ERROR: {str(e)}")
        sys.exit(1)

if __name__ == "__main__":
    if len(sys.argv) != 3:
        print("Usage: convert_to_excel.py <input_pdf> <output_xlsx>")
        sys.exit(1)
    
    input_path = sys.argv[1]
    output_path = sys.argv[2]
    
    if not os.path.exists(input_path):
        print(f"ERROR: Input file not found: {input_path}")
        sys.exit(1)
    
    convert_pdf_to_excel(input_path, output_path)
</file>

<file path="convert.py">
#!/usr/bin/env python3
"""PDF to DOCX conversion script using pdf2docx."""

import sys
import os

def convert_pdf_to_docx(input_path, output_path):
    try:
        from pdf2docx import Converter
        
        cv = Converter(input_path)
        cv.convert(output_path)
        cv.close()
        
        if os.path.exists(output_path) and os.path.getsize(output_path) > 0:
            print(f"SUCCESS: {output_path}")
            sys.exit(0)
        else:
            print(f"ERROR: Output file not created or empty")
            sys.exit(1)
    except Exception as e:
        print(f"ERROR: {str(e)}")
        sys.exit(1)

if __name__ == "__main__":
    if len(sys.argv) != 3:
        print("Usage: convert.py <input_pdf> <output_docx>")
        sys.exit(1)
    
    input_path = sys.argv[1]
    output_path = sys.argv[2]
    
    if not os.path.exists(input_path):
        print(f"ERROR: Input file not found: {input_path}")
        sys.exit(1)
    
    convert_pdf_to_docx(input_path, output_path)
</file>

<file path="docker-compose.yml">
version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: pdf_converter_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pdf_converter
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: pdf_converter_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
</file>

<file path="package.json">
{
  "name": "pdf-converter",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "build:web": "npm run build --workspace=apps/web",
    "build:api": "npm run build --workspace=apps/api",
    "test": "npm run test --workspaces"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
</file>

</files>
